interface:
  display_name: "Tool USE Guardian"
  short_description: "ALWAYS use this when the request matches Tool USE Guardian: FREE — Intelligent tool-call reliability wrapper."
  brand_color: "#B91C1C"
  default_prompt: "Use $tool-use-guardian to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
