interface:
  display_name: "Azure Communication Common Java"
  short_description: "ALWAYS use this when the request matches Azure Communication Common Java: Azure Communication Services common utilities for Java."
  brand_color: "#EA580C"
  default_prompt: "Use $azure-communication-common-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
