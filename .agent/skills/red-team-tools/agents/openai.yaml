interface:
  display_name: "RED Team Tools"
  short_description: "ALWAYS use this when the request matches RED Team Tools: Implement proven methodologies and tool workflows from top security researchers for effectiv..."
  brand_color: "#BE123C"
  default_prompt: "Use $red-team-tools to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
