interface:
  display_name: "GIT PR Workflows GIT Workflow"
  short_description: "ALWAYS use this when the request matches GIT PR Workflows GIT Workflow: Orchestrate a comprehensive git workflow from code review through PR creation..."
  brand_color: "#2563EB"
  default_prompt: "Use $git-pr-workflows-git-workflow to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
