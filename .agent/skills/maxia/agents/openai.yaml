interface:
  display_name: "Maxia"
  short_description: "ALWAYS use this when the request matches Maxia: Connect to MAXIA AI-to-AI marketplace on Solana."
  brand_color: "#BE123C"
  default_prompt: "Use $maxia to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
