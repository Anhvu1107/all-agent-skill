interface:
  display_name: "LLM APP Patterns"
  short_description: "ALWAYS use this when the request matches LLM APP Patterns: Production-ready patterns for building LLM applications, inspired by [Dify](https://github..."
  brand_color: "#0F172A"
  default_prompt: "Use $llm-app-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
