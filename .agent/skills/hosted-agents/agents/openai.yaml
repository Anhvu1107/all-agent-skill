interface:
  display_name: "Hosted Agents"
  short_description: "ALWAYS use this when the request matches Hosted Agents: Build background agents in sandboxed environments."
  brand_color: "#BE123C"
  default_prompt: "Use $hosted-agents to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
