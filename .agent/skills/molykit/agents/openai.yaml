interface:
  display_name: "Molykit"
  short_description: "ALWAYS use this when the request matches Molykit: CRITICAL: Use for MolyKit AI chat toolkit."
  brand_color: "#BE123C"
  default_prompt: "Use $molykit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
