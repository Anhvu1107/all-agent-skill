interface:
  display_name: "OSS Hunter"
  short_description: "ALWAYS use this when the request matches OSS Hunter: Automatically hunt for high-impact OSS contribution opportunities in trending repositories."
  brand_color: "#374151"
  default_prompt: "Use $oss-hunter to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
