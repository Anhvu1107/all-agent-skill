interface:
  display_name: "LEX"
  short_description: "ALWAYS use this when the request matches LEX: Centralized 'Truth Engine' for cross-jurisdictional legal context (US, EU, CA) and contract scaffolding."
  brand_color: "#1D4ED8"
  default_prompt: "Use $lex to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
