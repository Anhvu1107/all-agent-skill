interface:
  display_name: "Email Systems"
  short_description: "ALWAYS use this when the request matches Email Systems: Email has the highest ROI of any marketing channel."
  brand_color: "#BE123C"
  default_prompt: "Use $email-systems to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
