interface:
  display_name: "Azure Containerregistry PY"
  short_description: "ALWAYS use this when the request matches Azure Containerregistry PY: Azure Container Registry SDK for Python."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-containerregistry-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
