interface:
  display_name: "Screenshots"
  short_description: "ALWAYS use this when the request matches Screenshots: Generate marketing screenshots of your app using Playwright."
  brand_color: "#0F766E"
  default_prompt: "Use $screenshots to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
