interface:
  display_name: "Project Skill Audit"
  short_description: "ALWAYS use this when the request matches Project Skill Audit: Audit a project and recommend the highest-value skills to add or update."
  brand_color: "#EA580C"
  default_prompt: "Use $project-skill-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
