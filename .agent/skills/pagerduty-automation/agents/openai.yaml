interface:
  display_name: "Pagerduty Automation"
  short_description: "ALWAYS use this when the request matches Pagerduty Automation: Automate PagerDuty tasks via Rube MCP (Composio): manage incidents, services, schedule..."
  brand_color: "#B91C1C"
  default_prompt: "Use $pagerduty-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
