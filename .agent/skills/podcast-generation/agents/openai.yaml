interface:
  display_name: "Podcast Generation"
  short_description: "ALWAYS use this when the request matches Podcast Generation: Generate real audio narratives from text content using Azure OpenAI's Realtime API."
  brand_color: "#EA580C"
  default_prompt: "Use $podcast-generation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
