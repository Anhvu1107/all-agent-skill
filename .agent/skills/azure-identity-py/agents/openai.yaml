interface:
  display_name: "Azure Identity PY"
  short_description: "ALWAYS use this when the request matches Azure Identity PY: Azure Identity SDK for Python authentication."
  brand_color: "#15803D"
  default_prompt: "Use $azure-identity-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
