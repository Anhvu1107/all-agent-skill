interface:
  display_name: "AI DEV Jobs MCP"
  short_description: "ALWAYS use this when the request matches AI DEV Jobs MCP: Search 8,400+ AI and ML jobs across 489 companies, inspect listings and employers, match ro..."
  brand_color: "#EA580C"
  default_prompt: "Use $ai-dev-jobs-mcp to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
