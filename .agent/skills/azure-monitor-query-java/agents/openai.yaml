interface:
  display_name: "Azure Monitor Query Java"
  short_description: "ALWAYS use this when the user mentions Azure Monitor Query Java, asks to build, debug, review, document, automate, test, configure, migrate, or make..."
  brand_color: "#2563EB"
  default_prompt: "Use $azure-monitor-query-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
