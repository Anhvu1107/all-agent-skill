interface:
  display_name: "Rehabilitation Analyzer"
  short_description: "ALWAYS use this when the user mentions Rehabilitation Analyzer, asks to build, debug, review, document, automate, test, configure, migrate, or make d..."
  brand_color: "#7C3AED"
  default_prompt: "Use $rehabilitation-analyzer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
