interface:
  display_name: "Claude Ally Health"
  short_description: "ALWAYS use this when the request matches Claude Ally Health: A health assistant skill for medical information analysis, symptom tracking, and wellnes..."
  brand_color: "#15803D"
  default_prompt: "Use $claude-ally-health to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
