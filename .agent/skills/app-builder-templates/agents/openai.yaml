interface:
  display_name: "APP Builder Templates"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `app-builder-templates`, the nested skill path form for `templates`, or the equivalent..."
  brand_color: "#2563EB"
  default_prompt: "Use $app-builder-templates to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
