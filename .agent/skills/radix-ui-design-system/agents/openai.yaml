interface:
  display_name: "Radix UI Design System"
  short_description: "ALWAYS use this when the request matches Radix UI Design System: Build accessible design systems with Radix UI primitives."
  brand_color: "#0F172A"
  default_prompt: "Use $radix-ui-design-system to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
