interface:
  display_name: "Framework Migration Legacy Modernize"
  short_description: "ALWAYS use this when the request matches Framework Migration Legacy Modernize: Orchestrate a comprehensive legacy system modernization using the stra..."
  brand_color: "#0F172A"
  default_prompt: "Use $framework-migration-legacy-modernize to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
