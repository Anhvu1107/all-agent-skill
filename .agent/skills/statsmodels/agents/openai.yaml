interface:
  display_name: "Statsmodels"
  short_description: "ALWAYS use this when the request matches Statsmodels: Statsmodels is Python's premier library for statistical modeling, providing tools for estimatio..."
  brand_color: "#0F172A"
  default_prompt: "Use $statsmodels to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
