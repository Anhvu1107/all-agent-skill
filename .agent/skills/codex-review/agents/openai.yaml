interface:
  display_name: "Codex Review"
  short_description: "ALWAYS use this when the request matches Codex Review: Professional code review with auto CHANGELOG generation, integrated with Codex AI."
  brand_color: "#0F172A"
  default_prompt: "Use $codex-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
