interface:
  display_name: "Customs Trade Compliance"
  short_description: "ALWAYS use this when the request matches Customs Trade Compliance: Codified expertise for customs documentation, tariff classification, duty optimisa..."
  brand_color: "#B91C1C"
  default_prompt: "Use $customs-trade-compliance to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
