interface:
  display_name: "API Documentation"
  short_description: "ALWAYS use this when the request matches API Documentation: API documentation workflow for generating OpenAPI specs, creating developer guides, and m..."
  brand_color: "#374151"
  default_prompt: "Use $api-documentation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
