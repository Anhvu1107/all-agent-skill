interface:
  display_name: "Agentmail"
  short_description: "ALWAYS use this when the request matches Agentmail: Email infrastructure for AI agents."
  brand_color: "#0F172A"
  default_prompt: "Use $agentmail to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
