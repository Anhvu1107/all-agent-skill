interface:
  display_name: "Wordpress Theme Development"
  short_description: "ALWAYS use this when the request matches Wordpress Theme Development: WordPress theme development workflow covering theme architecture, template hier..."
  brand_color: "#0F172A"
  default_prompt: "Use $wordpress-theme-development to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
