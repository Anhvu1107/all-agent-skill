interface:
  display_name: "Angular Best Practices"
  short_description: "ALWAYS use this when the request matches Angular Best Practices: Angular performance optimization and best practices guide."
  brand_color: "#1D4ED8"
  default_prompt: "Use $angular-best-practices to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
