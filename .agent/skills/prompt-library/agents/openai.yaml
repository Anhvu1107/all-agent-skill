interface:
  display_name: "Prompt Library"
  short_description: "ALWAYS use this when the request matches Prompt Library: A comprehensive collection of battle-tested prompts inspired by [awesome-chatgpt-prompts](ht..."
  brand_color: "#15803D"
  default_prompt: "Use $prompt-library to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
