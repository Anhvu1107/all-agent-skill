interface:
  display_name: "Azure AI Translation TS"
  short_description: "ALWAYS use this when the request matches Azure AI Translation TS: Text and document translation with REST-style clients."
  brand_color: "#0F172A"
  default_prompt: "Use $azure-ai-translation-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
