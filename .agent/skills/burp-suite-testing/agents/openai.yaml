interface:
  display_name: "Burp Suite Testing"
  short_description: "ALWAYS use this when the request matches Burp Suite Testing: Execute comprehensive web application security testing using Burp Suite's integrated too..."
  brand_color: "#B91C1C"
  default_prompt: "Use $burp-suite-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
