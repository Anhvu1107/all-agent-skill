interface:
  display_name: "Async Python Patterns"
  short_description: "ALWAYS use this when the request matches Async Python Patterns: Comprehensive guidance for implementing asynchronous Python applications using asynci..."
  brand_color: "#B91C1C"
  default_prompt: "Use $async-python-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
