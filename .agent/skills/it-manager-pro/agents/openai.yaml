interface:
  display_name: "IT Manager PRO"
  short_description: "ALWAYS use this when the request matches IT Manager PRO: Elite IT Management Advisor specializing in data-driven strategy, executive communication, a..."
  brand_color: "#15803D"
  default_prompt: "Use $it-manager-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
