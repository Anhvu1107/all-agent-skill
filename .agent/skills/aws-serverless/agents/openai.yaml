interface:
  display_name: "AWS Serverless"
  short_description: "ALWAYS use this when the request matches AWS Serverless: Specialized skill for building production-ready serverless applications on AWS."
  brand_color: "#B91C1C"
  default_prompt: "Use $aws-serverless to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
