interface:
  display_name: "Baseline UI"
  short_description: "ALWAYS use this when the request matches Baseline UI: Validates animation durations, enforces typography scale, checks component accessibility, and p..."
  brand_color: "#7C3AED"
  default_prompt: "Use $baseline-ui to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
