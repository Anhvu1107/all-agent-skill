interface:
  display_name: "Circleci Automation"
  short_description: "ALWAYS use this when the request matches Circleci Automation: Automate CircleCI tasks via Rube MCP (Composio): trigger pipelines, monitor workflows/j..."
  brand_color: "#0F766E"
  default_prompt: "Use $circleci-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
