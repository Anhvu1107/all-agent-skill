interface:
  display_name: "CC Skill Backend Patterns"
  short_description: "ALWAYS use this when the request matches CC Skill Backend Patterns: Backend architecture patterns, API design, database optimization, and server-side..."
  brand_color: "#374151"
  default_prompt: "Use $cc-skill-backend-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
