interface:
  display_name: "Differential Review"
  short_description: "ALWAYS use this when the request matches Differential Review: Security-focused code review for PRs, commits, and diffs."
  brand_color: "#374151"
  default_prompt: "Use $differential-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
