interface:
  display_name: "Azure Mgmt Botservice PY"
  short_description: "ALWAYS use this when the request matches Azure Mgmt Botservice PY: Azure Bot Service Management SDK for Python."
  brand_color: "#0F172A"
  default_prompt: "Use $azure-mgmt-botservice-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
