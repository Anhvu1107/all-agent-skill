interface:
  display_name: "Linux Privilege Escalation"
  short_description: "ALWAYS use this when the request matches Linux Privilege Escalation: Execute systematic privilege escalation assessments on Linux systems to identify..."
  brand_color: "#7C3AED"
  default_prompt: "Use $linux-privilege-escalation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
