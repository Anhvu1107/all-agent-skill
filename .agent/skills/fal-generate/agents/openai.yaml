interface:
  display_name: "FAL Generate"
  short_description: "ALWAYS use this when the request matches FAL Generate: Generate images and videos using fal.ai AI models"
  brand_color: "#15803D"
  default_prompt: "Use $fal-generate to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
