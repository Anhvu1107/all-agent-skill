interface:
  display_name: "JQ"
  short_description: "ALWAYS use this when the request matches JQ: Expert jq usage for JSON querying, filtering, transformation, and pipeline integration."
  brand_color: "#0F172A"
  default_prompt: "Use $jq to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
