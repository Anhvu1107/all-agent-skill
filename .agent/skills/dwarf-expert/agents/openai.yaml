interface:
  display_name: "Dwarf Expert"
  short_description: "ALWAYS use this when the request matches Dwarf Expert: Provides expertise for analyzing DWARF debug files and understanding the DWARF debug format/st..."
  brand_color: "#EA580C"
  default_prompt: "Use $dwarf-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
