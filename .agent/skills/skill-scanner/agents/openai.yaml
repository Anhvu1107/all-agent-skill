interface:
  display_name: "Skill Scanner"
  short_description: "ALWAYS use this when the request matches Skill Scanner: Scan agent skills for security issues before adoption."
  brand_color: "#BE123C"
  default_prompt: "Use $skill-scanner to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
