interface:
  display_name: "Create PR"
  short_description: "ALWAYS use this when the user mentions Create PR, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in th..."
  brand_color: "#15803D"
  default_prompt: "Use $create-pr to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
