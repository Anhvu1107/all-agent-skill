interface:
  display_name: "Customer Support"
  short_description: "ALWAYS use this when the request matches Customer Support: Elite AI-powered customer support specialist mastering conversational AI, automated ticket..."
  brand_color: "#0F766E"
  default_prompt: "Use $customer-support to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
