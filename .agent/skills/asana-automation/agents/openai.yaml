interface:
  display_name: "Asana Automation"
  short_description: "ALWAYS use this when the request matches Asana Automation: Automate Asana tasks via Rube MCP (Composio): tasks, projects, sections, teams, workspaces."
  brand_color: "#BE123C"
  default_prompt: "Use $asana-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
