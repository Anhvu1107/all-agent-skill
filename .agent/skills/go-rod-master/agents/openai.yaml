interface:
  display_name: "GO ROD Master"
  short_description: "ALWAYS use this when the request matches GO ROD Master: Comprehensive guide for browser automation and web scraping with go-rod (Chrome DevTools Prot..."
  brand_color: "#0F766E"
  default_prompt: "Use $go-rod-master to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
