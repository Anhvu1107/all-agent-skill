interface:
  display_name: "Youtube Summarizer"
  short_description: "ALWAYS use this when the request matches Youtube Summarizer: Extract transcripts from YouTube videos and generate comprehensive, detailed summaries u..."
  brand_color: "#2563EB"
  default_prompt: "Use $youtube-summarizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
