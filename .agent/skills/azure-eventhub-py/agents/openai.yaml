interface:
  display_name: "Azure Eventhub PY"
  short_description: "ALWAYS use this when the request matches Azure Eventhub PY: Azure Event Hubs SDK for Python streaming."
  brand_color: "#0F172A"
  default_prompt: "Use $azure-eventhub-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
