interface:
  display_name: "FP Async"
  short_description: "ALWAYS use this when the request matches FP Async: Practical async patterns using TaskEither - clean pipelines instead of try/catch hell, with real A..."
  brand_color: "#374151"
  default_prompt: "Use $fp-async to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
