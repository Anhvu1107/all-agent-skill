interface:
  display_name: "Idea Darwin"
  short_description: "ALWAYS use this when the request matches Idea Darwin: Darwinian idea evolution engine — toss rough ideas onto an evolution island, let them compete..."
  brand_color: "#2563EB"
  default_prompt: "Use $idea-darwin to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
