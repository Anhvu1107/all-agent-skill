interface:
  display_name: "Libreoffice Writer"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `libreoffice-writer`, the nested skill path form for `writer`, or the equivalent slash..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $libreoffice-writer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
