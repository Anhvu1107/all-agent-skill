interface:
  display_name: "Tmux"
  short_description: "ALWAYS use this when the request matches Tmux: Expert tmux session, window, and pane management for terminal multiplexing, persistent remote workflow..."
  brand_color: "#7C3AED"
  default_prompt: "Use $tmux to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
