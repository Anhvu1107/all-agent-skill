interface:
  display_name: "Azure Resource Manager Playwright Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Resource Manager Playwright Dotnet: Azure Resource Manager SDK for Microsoft Playwright Testing in .NE..."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-resource-manager-playwright-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
