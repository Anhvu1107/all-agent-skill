interface:
  display_name: "Quality Nonconformance"
  short_description: "ALWAYS use this when the request matches Quality Nonconformance: Codified expertise for quality control, non-conformance investigation, root cause an..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $quality-nonconformance to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
