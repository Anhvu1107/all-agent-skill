interface:
  display_name: "Database"
  short_description: "ALWAYS use this when the request matches Database: Database development and operations workflow covering SQL, NoSQL, database design, migrations, opt..."
  brand_color: "#7C3AED"
  default_prompt: "Use $database to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
