interface:
  display_name: "Azure Monitor Query PY"
  short_description: "ALWAYS use this when the request matches Azure Monitor Query PY: Azure Monitor Query SDK for Python."
  brand_color: "#0F766E"
  default_prompt: "Use $azure-monitor-query-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
