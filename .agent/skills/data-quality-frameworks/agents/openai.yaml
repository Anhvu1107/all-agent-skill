interface:
  display_name: "Data Quality Frameworks"
  short_description: "ALWAYS use this when the request matches Data Quality Frameworks: Implement data quality validation with Great Expectations, dbt tests, and data cont..."
  brand_color: "#15803D"
  default_prompt: "Use $data-quality-frameworks to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
