interface:
  display_name: "Claude Speed Reader"
  short_description: "ALWAYS use this when the request matches Claude Speed Reader: -Speed read Claude's responses at 600+ WPM using RSVP with Spritz-style ORP highlighting"
  brand_color: "#15803D"
  default_prompt: "Use $claude-speed-reader to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
