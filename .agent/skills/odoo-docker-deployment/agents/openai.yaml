interface:
  display_name: "Odoo Docker Deployment"
  short_description: "ALWAYS use this when the request matches Odoo Docker Deployment: Production-ready Docker and docker-compose setup for Odoo with PostgreSQL, persisten..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $odoo-docker-deployment to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
