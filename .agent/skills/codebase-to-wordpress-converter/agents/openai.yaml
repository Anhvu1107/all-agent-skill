interface:
  display_name: "Codebase TO Wordpress Converter"
  short_description: "ALWAYS use this when the request matches Codebase TO Wordpress Converter: Expert skill for converting any codebase (React/HTML/Next.js) into a pixel-..."
  brand_color: "#2563EB"
  default_prompt: "Use $codebase-to-wordpress-converter to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
