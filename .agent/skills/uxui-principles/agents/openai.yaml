interface:
  display_name: "Uxui Principles"
  short_description: "ALWAYS use this when the request matches Uxui Principles: Evaluate interfaces against 168 research-backed UX/UI principles, detect antipatterns, and..."
  brand_color: "#15803D"
  default_prompt: "Use $uxui-principles to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
