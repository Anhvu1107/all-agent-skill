interface:
  display_name: "N8n Workflow Patterns"
  short_description: "ALWAYS use this when the request matches N8n Workflow Patterns: Proven architectural patterns for building n8n workflows."
  brand_color: "#7C3AED"
  default_prompt: "Use $n8n-workflow-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
