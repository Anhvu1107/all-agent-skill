interface:
  display_name: "Speed"
  short_description: "ALWAYS use this when the user mentions Speed, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in this d..."
  brand_color: "#374151"
  default_prompt: "Use $speed to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
