interface:
  display_name: "Scanning Tools"
  short_description: "ALWAYS use this when the request matches Scanning Tools: Master essential security scanning tools for network discovery, vulnerability assessment, we..."
  brand_color: "#EA580C"
  default_prompt: "Use $scanning-tools to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
