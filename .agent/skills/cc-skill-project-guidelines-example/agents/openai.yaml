interface:
  display_name: "CC Skill Project Guidelines Example"
  short_description: "ALWAYS use this when the user mentions CC Skill Project Guidelines Example, asks to build, debug, review, document, automate, test, configure, migrat..."
  brand_color: "#BE123C"
  default_prompt: "Use $cc-skill-project-guidelines-example to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
