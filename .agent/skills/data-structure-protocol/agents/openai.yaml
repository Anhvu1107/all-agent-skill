interface:
  display_name: "Data Structure Protocol"
  short_description: "ALWAYS use this when the request matches Data Structure Protocol: Give agents persistent structural memory of a codebase — navigate dependencies, tra..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $data-structure-protocol to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
