interface:
  display_name: "API Testing Observability API Mock"
  short_description: "ALWAYS use this when the request matches API Testing Observability API Mock: You are an API mocking expert specializing in realistic mock services fo..."
  brand_color: "#0F766E"
  default_prompt: "Use $api-testing-observability-api-mock to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
