interface:
  display_name: "Javascript PRO"
  short_description: "ALWAYS use this when the request matches Javascript PRO: Master modern JavaScript with ES6+, async patterns, and Node.js APIs."
  brand_color: "#EA580C"
  default_prompt: "Use $javascript-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
