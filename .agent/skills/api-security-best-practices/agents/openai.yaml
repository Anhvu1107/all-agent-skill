interface:
  display_name: "API Security Best Practices"
  short_description: "ALWAYS use this when the request matches API Security Best Practices: Implement secure API design patterns including authentication, authorization, i..."
  brand_color: "#7C3AED"
  default_prompt: "Use $api-security-best-practices to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
