interface:
  display_name: "Shopify Automation"
  short_description: "ALWAYS use this when the request matches Shopify Automation: Automate Shopify tasks via Rube MCP (Composio): products, orders, customers, inventory..."
  brand_color: "#BE123C"
  default_prompt: "Use $shopify-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
