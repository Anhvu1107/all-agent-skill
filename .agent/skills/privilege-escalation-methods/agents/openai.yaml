interface:
  display_name: "Privilege Escalation Methods"
  short_description: "ALWAYS use this when the request matches Privilege Escalation Methods: Provide comprehensive techniques for escalating privileges from a low-privileg..."
  brand_color: "#B91C1C"
  default_prompt: "Use $privilege-escalation-methods to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
