interface:
  display_name: "Pricing Strategy"
  short_description: "ALWAYS use this when the request matches Pricing Strategy: Design pricing, packaging, and monetization strategies based on value, customer willingnes..."
  brand_color: "#374151"
  default_prompt: "Use $pricing-strategy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
