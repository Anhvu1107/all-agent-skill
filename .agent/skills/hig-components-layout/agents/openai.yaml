interface:
  display_name: "HIG Components Layout"
  short_description: "ALWAYS use this when the request matches HIG Components Layout: Apple Human Interface Guidelines for layout and navigation components."
  brand_color: "#7C3AED"
  default_prompt: "Use $hig-components-layout to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
