interface:
  display_name: "AWS Secrets Rotation"
  short_description: "ALWAYS use this when the request matches AWS Secrets Rotation: Automate AWS secrets rotation for RDS, API keys, and credentials"
  brand_color: "#EA580C"
  default_prompt: "Use $aws-secrets-rotation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
