interface:
  display_name: "Security Audit"
  short_description: "ALWAYS use this when the request matches Security Audit: Comprehensive security auditing workflow covering web application testing, API security, pen..."
  brand_color: "#7C3AED"
  default_prompt: "Use $security-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
