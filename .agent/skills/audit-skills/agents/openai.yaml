interface:
  display_name: "Audit Skills"
  short_description: "ALWAYS use this when the request matches Audit Skills: Expert security auditor for AI Skills and Bundles."
  brand_color: "#7C3AED"
  default_prompt: "Use $audit-skills to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
