interface:
  display_name: "Azure Cosmos TS"
  short_description: "ALWAYS use this when the request matches Azure Cosmos TS: Azure Cosmos DB JavaScript/TypeScript SDK (@azure/cosmos) for data plane operations."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-cosmos-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
