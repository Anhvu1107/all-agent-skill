interface:
  display_name: "Azure Storage Blob Rust"
  short_description: "ALWAYS use this when the user mentions Azure Storage Blob Rust, asks to build, debug, review, document, automate, test, configure, migrate, or make d..."
  brand_color: "#15803D"
  default_prompt: "Use $azure-storage-blob-rust to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
