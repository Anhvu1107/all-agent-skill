interface:
  display_name: "UX Audit"
  short_description: "ALWAYS use this when the request matches UX Audit: Audit screens against Nielsen's heuristics and mobile UX best practices using the StyleSeed Toss d..."
  brand_color: "#0F172A"
  default_prompt: "Use $ux-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
