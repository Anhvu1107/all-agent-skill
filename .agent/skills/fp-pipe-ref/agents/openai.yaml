interface:
  display_name: "FP Pipe REF"
  short_description: "ALWAYS use this when the user mentions FP Pipe REF, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in..."
  brand_color: "#374151"
  default_prompt: "Use $fp-pipe-ref to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
