interface:
  display_name: "Conductor Implement"
  short_description: "ALWAYS use this when the request matches Conductor Implement: Execute tasks from a track's implementation plan following TDD workflow"
  brand_color: "#7C3AED"
  default_prompt: "Use $conductor-implement to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
