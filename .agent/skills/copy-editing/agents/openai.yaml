interface:
  display_name: "Copy Editing"
  short_description: "ALWAYS use this when the request matches Copy Editing: You are an expert copy editor specializing in marketing and conversion copy."
  brand_color: "#15803D"
  default_prompt: "Use $copy-editing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
