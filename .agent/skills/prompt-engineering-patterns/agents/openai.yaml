interface:
  display_name: "Prompt Engineering Patterns"
  short_description: "ALWAYS use this when the request matches Prompt Engineering Patterns: Master advanced prompt engineering techniques to maximize LLM performance, reli..."
  brand_color: "#0F172A"
  default_prompt: "Use $prompt-engineering-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
