interface:
  display_name: "WEB Performance Optimization"
  short_description: "ALWAYS use this when the request matches WEB Performance Optimization: Optimize website and web application performance including loading speed, Core..."
  brand_color: "#EA580C"
  default_prompt: "Use $web-performance-optimization to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
