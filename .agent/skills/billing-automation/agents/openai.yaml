interface:
  display_name: "Billing Automation"
  short_description: "ALWAYS use this when the request matches Billing Automation: Master automated billing systems including recurring billing, invoice generation, dunnin..."
  brand_color: "#BE123C"
  default_prompt: "Use $billing-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
