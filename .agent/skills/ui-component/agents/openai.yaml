interface:
  display_name: "UI Component"
  short_description: "ALWAYS use this when the request matches UI Component: Generate a new UI component that follows StyleSeed Toss conventions for structure, tokens, acc..."
  brand_color: "#7C3AED"
  default_prompt: "Use $ui-component to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
