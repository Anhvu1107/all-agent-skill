interface:
  display_name: "APP Store Changelog"
  short_description: "ALWAYS use this when the request matches APP Store Changelog: Generate user-facing App Store release notes from git history since the last tag."
  brand_color: "#EA580C"
  default_prompt: "Use $app-store-changelog to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
