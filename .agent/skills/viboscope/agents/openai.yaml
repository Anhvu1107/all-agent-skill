interface:
  display_name: "Viboscope"
  short_description: "ALWAYS use this when the request matches Viboscope: Psychological compatibility matching — find cofounders, collaborators, and friends through valida..."
  brand_color: "#0F766E"
  default_prompt: "Use $viboscope to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
