interface:
  display_name: "Stitch Loop"
  short_description: "ALWAYS use this when the request matches Stitch Loop: Teaches agents to iteratively build websites using Stitch with an autonomous baton-passing loop..."
  brand_color: "#0F766E"
  default_prompt: "Use $stitch-loop to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
