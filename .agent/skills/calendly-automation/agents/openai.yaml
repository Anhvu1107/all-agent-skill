interface:
  display_name: "Calendly Automation"
  short_description: "ALWAYS use this when the request matches Calendly Automation: Automate Calendly scheduling, event management, invitee tracking, availability checks..."
  brand_color: "#7C3AED"
  default_prompt: "Use $calendly-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
