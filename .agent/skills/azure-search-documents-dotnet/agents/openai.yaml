interface:
  display_name: "Azure Search Documents Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Search Documents Dotnet: Azure AI Search SDK for .NET (Azure.Search.Documents)."
  brand_color: "#2563EB"
  default_prompt: "Use $azure-search-documents-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
