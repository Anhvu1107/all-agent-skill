interface:
  display_name: "Sales Enablement"
  short_description: "ALWAYS use this when the request matches Sales Enablement: Create sales collateral such as decks, one-pagers, objection docs, demo scripts, playbooks..."
  brand_color: "#BE123C"
  default_prompt: "Use $sales-enablement to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
