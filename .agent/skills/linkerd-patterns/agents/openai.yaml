interface:
  display_name: "Linkerd Patterns"
  short_description: "ALWAYS use this when the request matches Linkerd Patterns: Production patterns for Linkerd service mesh - the lightweight, security-first service mes..."
  brand_color: "#BE123C"
  default_prompt: "Use $linkerd-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
