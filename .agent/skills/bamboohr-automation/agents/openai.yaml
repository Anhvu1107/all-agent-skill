interface:
  display_name: "Bamboohr Automation"
  short_description: "ALWAYS use this when the request matches Bamboohr Automation: Automate BambooHR tasks via Rube MCP (Composio): employees, time-off, benefits, depende..."
  brand_color: "#B91C1C"
  default_prompt: "Use $bamboohr-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
