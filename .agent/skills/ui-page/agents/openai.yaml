interface:
  display_name: "UI Page"
  short_description: "ALWAYS use this when the request matches UI Page: Scaffold a new mobile-first page using StyleSeed Toss layout patterns, section rhythm, and existing..."
  brand_color: "#B91C1C"
  default_prompt: "Use $ui-page to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
