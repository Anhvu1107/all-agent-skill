interface:
  display_name: "Claude D3js Skill"
  short_description: "ALWAYS use this when the request matches Claude D3js Skill: This skill provides guidance for creating sophisticated, interactive data visualisations..."
  brand_color: "#15803D"
  default_prompt: "Use $claude-d3js-skill to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
