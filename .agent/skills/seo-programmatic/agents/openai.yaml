interface:
  display_name: "SEO Programmatic"
  short_description: "ALWAYS use this when the request matches SEO Programmatic: Plan and audit programmatic SEO pages generated at scale from structured data."
  brand_color: "#BE123C"
  default_prompt: "Use $seo-programmatic to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
