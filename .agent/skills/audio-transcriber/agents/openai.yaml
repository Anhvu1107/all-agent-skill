interface:
  display_name: "Audio Transcriber"
  short_description: "ALWAYS use this when the request matches Audio Transcriber: Transform audio recordings into professional Markdown documentation with intelligent summ..."
  brand_color: "#BE123C"
  default_prompt: "Use $audio-transcriber to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
