interface:
  display_name: "Game Development Game Design"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `game-development-game-design`, the nested skill path form for `game-design`, or the e..."
  brand_color: "#BE123C"
  default_prompt: "Use $game-development-game-design to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
