interface:
  display_name: "Claude Win11 Speckit Update Skill"
  short_description: "ALWAYS use this when the user mentions Claude Win11 Speckit Update Skill, asks to build, debug, review, document, automate, test, configure, migrate..."
  brand_color: "#B91C1C"
  default_prompt: "Use $claude-win11-speckit-update-skill to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
