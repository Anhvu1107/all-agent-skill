interface:
  display_name: "Coda Automation"
  short_description: "ALWAYS use this when the request matches Coda Automation: Automate Coda tasks via Rube MCP (Composio): manage docs, pages, tables, rows, formulas, pe..."
  brand_color: "#BE123C"
  default_prompt: "Use $coda-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
