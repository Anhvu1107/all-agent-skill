interface:
  display_name: "Base"
  short_description: "ALWAYS use this when the request matches Base: Database management, forms, reports, and data operations with LibreOffice Base."
  brand_color: "#0F766E"
  default_prompt: "Use $base to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
