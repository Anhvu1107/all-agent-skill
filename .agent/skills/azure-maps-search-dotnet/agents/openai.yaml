interface:
  display_name: "Azure Maps Search Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure Maps Search Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or make..."
  brand_color: "#7C3AED"
  default_prompt: "Use $azure-maps-search-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
