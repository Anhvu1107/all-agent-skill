interface:
  display_name: "Signup Flow CRO"
  short_description: "ALWAYS use this when the request matches Signup Flow CRO: You are an expert in optimizing signup and registration flows."
  brand_color: "#0F172A"
  default_prompt: "Use $signup-flow-cro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
