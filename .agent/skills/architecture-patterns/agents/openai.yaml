interface:
  display_name: "Architecture Patterns"
  short_description: "ALWAYS use this when the request matches Architecture Patterns: Master proven backend architecture patterns including Clean Architecture, Hexagonal A..."
  brand_color: "#EA580C"
  default_prompt: "Use $architecture-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
