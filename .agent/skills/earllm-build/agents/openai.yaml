interface:
  display_name: "Earllm Build"
  short_description: "ALWAYS use this when the request matches Earllm Build: Build, maintain, and extend the EarLLM One Android project — a Kotlin/Compose app that connect..."
  brand_color: "#EA580C"
  default_prompt: "Use $earllm-build to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
