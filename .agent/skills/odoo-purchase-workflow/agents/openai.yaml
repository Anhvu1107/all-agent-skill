interface:
  display_name: "Odoo Purchase Workflow"
  short_description: "ALWAYS use this when the request matches Odoo Purchase Workflow: Expert guide for Odoo Purchase: RFQ → PO → Receipt → Vendor Bill workflow, purchase..."
  brand_color: "#374151"
  default_prompt: "Use $odoo-purchase-workflow to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
