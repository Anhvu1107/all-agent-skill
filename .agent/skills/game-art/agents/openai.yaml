interface:
  display_name: "Game ART"
  short_description: "ALWAYS use this when the user mentions Game ART, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in thi..."
  brand_color: "#EA580C"
  default_prompt: "Use $game-art to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
