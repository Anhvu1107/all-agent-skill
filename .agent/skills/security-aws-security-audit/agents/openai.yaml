interface:
  display_name: "Security AWS Security Audit"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `security-aws-security-audit`, the nested skill path form for `aws-security-audit`, or..."
  brand_color: "#BE123C"
  default_prompt: "Use $security-aws-security-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
