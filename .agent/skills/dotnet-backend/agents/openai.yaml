interface:
  display_name: "Dotnet Backend"
  short_description: "ALWAYS use this when the request matches Dotnet Backend: Build ASP.NET Core 8+ backend services with EF Core, auth, background jobs, and production A..."
  brand_color: "#0F172A"
  default_prompt: "Use $dotnet-backend to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
