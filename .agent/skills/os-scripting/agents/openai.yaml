interface:
  display_name: "OS Scripting"
  short_description: "ALWAYS use this when the request matches OS Scripting: Operating system and shell scripting troubleshooting workflow for Linux, macOS, and Windows."
  brand_color: "#B91C1C"
  default_prompt: "Use $os-scripting to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
