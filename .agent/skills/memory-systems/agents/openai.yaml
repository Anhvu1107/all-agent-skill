interface:
  display_name: "Memory Systems"
  short_description: "ALWAYS use this when the request matches Memory Systems: Design short-term, long-term, and graph-based memory architectures."
  brand_color: "#374151"
  default_prompt: "Use $memory-systems to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
