interface:
  display_name: "Mobile Games"
  short_description: "ALWAYS use this when the request matches Mobile Games: Mobile game development principles."
  brand_color: "#7C3AED"
  default_prompt: "Use $mobile-games to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
