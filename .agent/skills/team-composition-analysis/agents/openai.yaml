interface:
  display_name: "Team Composition Analysis"
  short_description: "ALWAYS use this when the request matches Team Composition Analysis: Design optimal team structures, hiring plans, compensation strategies, and equity..."
  brand_color: "#0F172A"
  default_prompt: "Use $team-composition-analysis to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
