interface:
  display_name: "Conductor NEW Track"
  short_description: "ALWAYS use this when the request matches Conductor NEW Track: Create a new track with specification and phased implementation plan"
  brand_color: "#0F766E"
  default_prompt: "Use $conductor-new-track to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
