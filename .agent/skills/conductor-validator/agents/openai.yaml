interface:
  display_name: "Conductor Validator"
  short_description: "ALWAYS use this when the request matches Conductor Validator: Validates Conductor project artifacts for completeness, consistency, and correctness."
  brand_color: "#EA580C"
  default_prompt: "Use $conductor-validator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
