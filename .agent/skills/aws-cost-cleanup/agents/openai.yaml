interface:
  display_name: "AWS Cost Cleanup"
  short_description: "ALWAYS use this when the request matches AWS Cost Cleanup: Automated cleanup of unused AWS resources to reduce costs"
  brand_color: "#EA580C"
  default_prompt: "Use $aws-cost-cleanup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
