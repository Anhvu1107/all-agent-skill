interface:
  display_name: "Distributed Tracing"
  short_description: "ALWAYS use this when the request matches Distributed Tracing: Implement distributed tracing with Jaeger and Tempo for request flow visibility across..."
  brand_color: "#BE123C"
  default_prompt: "Use $distributed-tracing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
