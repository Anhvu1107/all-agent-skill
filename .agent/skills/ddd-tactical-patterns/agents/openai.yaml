interface:
  display_name: "DDD Tactical Patterns"
  short_description: "ALWAYS use this when the request matches DDD Tactical Patterns: Apply DDD tactical patterns in code using entities, value objects, aggregates, reposi..."
  brand_color: "#EA580C"
  default_prompt: "Use $ddd-tactical-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
