interface:
  display_name: "Azure Storage Queue PY"
  short_description: "ALWAYS use this when the request matches Azure Storage Queue PY: Azure Queue Storage SDK for Python."
  brand_color: "#7C3AED"
  default_prompt: "Use $azure-storage-queue-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
