interface:
  display_name: "Varlock Claude Skill"
  short_description: "ALWAYS use this when the request matches Varlock Claude Skill: Secure environment variable management ensuring secrets are never exposed in Claude se..."
  brand_color: "#0F172A"
  default_prompt: "Use $varlock-claude-skill to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
