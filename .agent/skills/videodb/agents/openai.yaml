interface:
  display_name: "Videodb"
  short_description: "ALWAYS use this when the request matches Videodb: Video and audio perception, indexing, and editing."
  brand_color: "#EA580C"
  default_prompt: "Use $videodb to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
