interface:
  display_name: "Xlsx"
  short_description: "ALWAYS use this when the primary input or output is a spreadsheet, workbook, CSV, or tabular deliverable that needs formulas, formatting, cleanup, re..."
  brand_color: "#BE123C"
  default_prompt: "Use $xlsx to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
