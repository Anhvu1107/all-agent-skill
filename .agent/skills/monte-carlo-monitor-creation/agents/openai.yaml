interface:
  display_name: "Monte Carlo Monitor Creation"
  short_description: "ALWAYS use this when the request matches Monte Carlo Monitor Creation: Guides creation of Monte Carlo monitors via MCP tools, producing monitors-as-c..."
  brand_color: "#0F766E"
  default_prompt: "Use $monte-carlo-monitor-creation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
