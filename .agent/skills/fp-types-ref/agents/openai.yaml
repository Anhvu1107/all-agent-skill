interface:
  display_name: "FP Types REF"
  short_description: "ALWAYS use this when the user mentions FP Types REF, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in..."
  brand_color: "#0F172A"
  default_prompt: "Use $fp-types-ref to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
