interface:
  display_name: "Intercom Automation"
  short_description: "ALWAYS use this when the request matches Intercom Automation: Automate Intercom tasks via Rube MCP (Composio): conversations, contacts, companies, se..."
  brand_color: "#0F172A"
  default_prompt: "Use $intercom-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
