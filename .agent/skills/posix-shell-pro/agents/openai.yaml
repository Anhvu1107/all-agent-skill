interface:
  display_name: "Posix Shell PRO"
  short_description: "ALWAYS use this when the request matches Posix Shell PRO: Expert in strict POSIX sh scripting for maximum portability across Unix-like systems."
  brand_color: "#7C3AED"
  default_prompt: "Use $posix-shell-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
