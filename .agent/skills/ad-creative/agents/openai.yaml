interface:
  display_name: "AD Creative"
  short_description: "ALWAYS use this when the request matches AD Creative: Create, iterate, and scale paid ad creative for Google Ads, Meta, LinkedIn, TikTok, and similar..."
  brand_color: "#0F766E"
  default_prompt: "Use $ad-creative to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
