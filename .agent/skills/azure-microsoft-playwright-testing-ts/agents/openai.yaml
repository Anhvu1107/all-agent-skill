interface:
  display_name: "Azure Microsoft Playwright Testing TS"
  short_description: "ALWAYS use this when the request matches Azure Microsoft Playwright Testing TS: Run Playwright tests at scale with cloud-hosted browsers and integrat..."
  brand_color: "#374151"
  default_prompt: "Use $azure-microsoft-playwright-testing-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
