interface:
  display_name: "FP Taskeither REF"
  short_description: "ALWAYS use this when the user mentions FP Taskeither REF, asks to build, debug, review, document, automate, test, configure, migrate, or make decisio..."
  brand_color: "#BE123C"
  default_prompt: "Use $fp-taskeither-ref to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
