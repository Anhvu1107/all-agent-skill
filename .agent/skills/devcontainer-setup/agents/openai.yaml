interface:
  display_name: "Devcontainer Setup"
  short_description: "ALWAYS use this when the request matches Devcontainer Setup: Creates devcontainers with Claude Code, language-specific tooling (Python/Node/Rust/Go)..."
  brand_color: "#374151"
  default_prompt: "Use $devcontainer-setup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
