interface:
  display_name: "Incident Response Incident Response"
  short_description: "ALWAYS use this when the request matches Incident Response Incident Response: Use when working with incident response incident response"
  brand_color: "#7C3AED"
  default_prompt: "Use $incident-response-incident-response to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
