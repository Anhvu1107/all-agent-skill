interface:
  display_name: "Internal Comms Community"
  short_description: "ALWAYS use this when the request matches Internal Comms Community: To write internal communications, use this skill for:"
  brand_color: "#7C3AED"
  default_prompt: "Use $internal-comms-community to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
