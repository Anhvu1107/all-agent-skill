interface:
  display_name: "Ffuf Claude Skill"
  short_description: "ALWAYS use this when the user mentions Ffuf Claude Skill, asks to build, debug, review, document, automate, test, configure, migrate, or make decisio..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $ffuf-claude-skill to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
