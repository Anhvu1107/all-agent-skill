interface:
  display_name: "Jira Automation"
  short_description: "ALWAYS use this when the request matches Jira Automation: Automate Jira tasks via Rube MCP (Composio): issues, projects, sprints, boards, comments, u..."
  brand_color: "#0F766E"
  default_prompt: "Use $jira-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
