interface:
  display_name: "Acceptance Orchestrator"
  short_description: "ALWAYS use this when the request matches Acceptance Orchestrator: Use when a coding task should be driven end-to-end from issue intake through implem..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $acceptance-orchestrator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
