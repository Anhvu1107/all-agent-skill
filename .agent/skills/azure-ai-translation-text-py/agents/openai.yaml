interface:
  display_name: "Azure AI Translation Text PY"
  short_description: "ALWAYS use this when the request matches Azure AI Translation Text PY: Azure AI Text Translation SDK for real-time text translation, transliteration..."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-ai-translation-text-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
