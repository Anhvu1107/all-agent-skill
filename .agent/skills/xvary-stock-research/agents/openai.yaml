interface:
  display_name: "Xvary Stock Research"
  short_description: "ALWAYS use this when the request matches Xvary Stock Research: Thesis-driven equity analysis from public SEC EDGAR and market data; /analyze, /score..."
  brand_color: "#15803D"
  default_prompt: "Use $xvary-stock-research to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
