interface:
  display_name: "Social Proof Architect"
  short_description: "ALWAYS use this when the request matches Social Proof Architect: One sentence - what this skill does and when to invoke it"
  brand_color: "#B91C1C"
  default_prompt: "Use $social-proof-architect to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
