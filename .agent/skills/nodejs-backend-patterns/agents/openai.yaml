interface:
  display_name: "Nodejs Backend Patterns"
  short_description: "ALWAYS use this when the request matches Nodejs Backend Patterns: Comprehensive guidance for building scalable, maintainable, and production-ready No..."
  brand_color: "#B91C1C"
  default_prompt: "Use $nodejs-backend-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
