interface:
  display_name: "Headline Psychologist"
  short_description: "ALWAYS use this when the request matches Headline Psychologist: One sentence - what this skill does and when to invoke it"
  brand_color: "#0F172A"
  default_prompt: "Use $headline-psychologist to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
