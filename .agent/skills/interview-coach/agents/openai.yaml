interface:
  display_name: "Interview Coach"
  short_description: "ALWAYS use this when the request matches Interview Coach: Full job search coaching system — JD decoding, resume, storybank, mock interviews, transcri..."
  brand_color: "#0F172A"
  default_prompt: "Use $interview-coach to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
