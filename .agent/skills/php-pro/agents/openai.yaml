interface:
  display_name: "PHP PRO"
  short_description: "ALWAYS use this when the request matches PHP PRO: Write idiomatic PHP code with generators, iterators, SPL data structures, and modern OOP features."
  brand_color: "#374151"
  default_prompt: "Use $php-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
