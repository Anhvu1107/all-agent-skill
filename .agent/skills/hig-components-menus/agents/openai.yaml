interface:
  display_name: "HIG Components Menus"
  short_description: "ALWAYS use this when the request matches HIG Components Menus: Check for .claude/apple-design-context.md before asking questions."
  brand_color: "#B91C1C"
  default_prompt: "Use $hig-components-menus to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
