interface:
  display_name: "Azure Mgmt Botservice Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Mgmt Botservice Dotnet: Azure Resource Manager SDK for Bot Service in .NET."
  brand_color: "#15803D"
  default_prompt: "Use $azure-mgmt-botservice-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
