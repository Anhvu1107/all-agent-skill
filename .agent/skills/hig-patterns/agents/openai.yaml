interface:
  display_name: "HIG Patterns"
  short_description: "ALWAYS use this when the request matches HIG Patterns: Apple Human Interface Guidelines interaction and UX patterns."
  brand_color: "#374151"
  default_prompt: "Use $hig-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
