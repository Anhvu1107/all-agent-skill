interface:
  display_name: "Azure Appconfiguration PY"
  short_description: "ALWAYS use this when the request matches Azure Appconfiguration PY: Azure App Configuration SDK for Python."
  brand_color: "#0F766E"
  default_prompt: "Use $azure-appconfiguration-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
