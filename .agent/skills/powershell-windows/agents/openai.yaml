interface:
  display_name: "Powershell Windows"
  short_description: "ALWAYS use this when the user mentions Powershell Windows, asks to build, debug, review, document, automate, test, configure, migrate, or make decisi..."
  brand_color: "#0F766E"
  default_prompt: "Use $powershell-windows to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
