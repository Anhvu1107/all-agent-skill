interface:
  display_name: "Microsoft Teams Automation"
  short_description: "ALWAYS use this when the request matches Microsoft Teams Automation: Automate Microsoft Teams tasks via Rube MCP (Composio): send messages, manage ch..."
  brand_color: "#BE123C"
  default_prompt: "Use $microsoft-teams-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
