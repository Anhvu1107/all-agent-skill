interface:
  display_name: "Miro Automation"
  short_description: "ALWAYS use this when the request matches Miro Automation: Automate Miro tasks via Rube MCP (Composio): boards, items, sticky notes, frames, sharing..."
  brand_color: "#0F766E"
  default_prompt: "Use $miro-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
