interface:
  display_name: "Observability Monitoring SLO Implement"
  short_description: "ALWAYS use this when the request matches Observability Monitoring SLO Implement: You are an SLO (Service Level Objective) expert specializing in impl..."
  brand_color: "#2563EB"
  default_prompt: "Use $observability-monitoring-slo-implement to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
