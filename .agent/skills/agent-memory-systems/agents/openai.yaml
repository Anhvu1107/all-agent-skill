interface:
  display_name: "Agent Memory Systems"
  short_description: "ALWAYS use this when the request matches Agent Memory Systems: Memory is the cornerstone of intelligent agents."
  brand_color: "#0F172A"
  default_prompt: "Use $agent-memory-systems to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
