interface:
  display_name: "Twilio Communications"
  short_description: "ALWAYS use this when the request matches Twilio Communications: Build communication features with Twilio: SMS messaging, voice calls, WhatsApp Busine..."
  brand_color: "#0F766E"
  default_prompt: "Use $twilio-communications to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
