interface:
  display_name: "Odoo Accounting Setup"
  short_description: "ALWAYS use this when the request matches Odoo Accounting Setup: Expert guide for configuring Odoo Accounting: chart of accounts, journals, fiscal pos..."
  brand_color: "#15803D"
  default_prompt: "Use $odoo-accounting-setup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
