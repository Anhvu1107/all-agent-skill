interface:
  display_name: "Wordpress Woocommerce Development"
  short_description: "ALWAYS use this when the request matches Wordpress Woocommerce Development: WooCommerce store development workflow covering store setup, payment inte..."
  brand_color: "#EA580C"
  default_prompt: "Use $wordpress-woocommerce-development to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
