interface:
  display_name: "Error Detective"
  short_description: "ALWAYS use this when the request matches Error Detective: Search logs and codebases for error patterns, stack traces, and anomalies."
  brand_color: "#EA580C"
  default_prompt: "Use $error-detective to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
