# TDD Decision Guide

Use TDD aggressively for:

- bug fixes with known reproduction
- parsers, validators, business rules, state transitions

Do not force it when:

- the task is mostly exploratory visual design
- the system boundary is still undefined
