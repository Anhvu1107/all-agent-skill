interface:
  display_name: "Pipecat Friday Agent"
  short_description: "ALWAYS use this when the request matches Pipecat Friday Agent: Build a low-latency, Iron Man-inspired tactical voice assistant (F.R.I.D.A.Y.) using P..."
  brand_color: "#BE123C"
  default_prompt: "Use $pipecat-friday-agent to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
