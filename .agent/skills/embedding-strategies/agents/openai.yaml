interface:
  display_name: "Embedding Strategies"
  short_description: "ALWAYS use this when the request matches Embedding Strategies: Guide to selecting and optimizing embedding models for vector search applications."
  brand_color: "#BE123C"
  default_prompt: "Use $embedding-strategies to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
