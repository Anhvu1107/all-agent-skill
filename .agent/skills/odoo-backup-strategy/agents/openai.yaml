interface:
  display_name: "Odoo Backup Strategy"
  short_description: "ALWAYS use this when the request matches Odoo Backup Strategy: Complete Odoo backup and restore strategy: database dumps, filestore backup, automated..."
  brand_color: "#15803D"
  default_prompt: "Use $odoo-backup-strategy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
