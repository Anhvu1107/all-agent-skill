interface:
  display_name: "Azure AI Voicelive PY"
  short_description: "ALWAYS use this when the request matches Azure AI Voicelive PY: Build real-time voice AI applications with bidirectional WebSocket communication."
  brand_color: "#0F172A"
  default_prompt: "Use $azure-ai-voicelive-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
