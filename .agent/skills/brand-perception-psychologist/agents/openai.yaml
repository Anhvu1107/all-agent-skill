interface:
  display_name: "Brand Perception Psychologist"
  short_description: "ALWAYS use this when the request matches Brand Perception Psychologist: One sentence - what this skill does and when to invoke it"
  brand_color: "#B91C1C"
  default_prompt: "Use $brand-perception-psychologist to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
