interface:
  display_name: "Stride Analysis Patterns"
  short_description: "ALWAYS use this when the request matches Stride Analysis Patterns: Apply STRIDE methodology to systematically identify threats."
  brand_color: "#EA580C"
  default_prompt: "Use $stride-analysis-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
