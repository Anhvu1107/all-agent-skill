interface:
  display_name: "Spark Optimization"
  short_description: "ALWAYS use this when the request matches Spark Optimization: Optimize Apache Spark jobs with partitioning, caching, shuffle optimization, and memory..."
  brand_color: "#0F766E"
  default_prompt: "Use $spark-optimization to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
