interface:
  display_name: "RAG Implementation"
  short_description: "ALWAYS use this when the request matches RAG Implementation: RAG (Retrieval-Augmented Generation) implementation workflow covering embedding selectio..."
  brand_color: "#7C3AED"
  default_prompt: "Use $rag-implementation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
