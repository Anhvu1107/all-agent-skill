interface:
  display_name: "Site Architecture"
  short_description: "ALWAYS use this when the request matches Site Architecture: Plan or restructure website hierarchy, navigation, URL patterns, breadcrumbs, and interna..."
  brand_color: "#B91C1C"
  default_prompt: "Use $site-architecture to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
