interface:
  display_name: "Inngest"
  short_description: "ALWAYS use this when the request matches Inngest: Inngest expert for serverless-first background jobs, event-driven workflows, and durable execution..."
  brand_color: "#0F766E"
  default_prompt: "Use $inngest to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
