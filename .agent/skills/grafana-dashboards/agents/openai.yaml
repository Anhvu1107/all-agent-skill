interface:
  display_name: "Grafana Dashboards"
  short_description: "ALWAYS use this when the request matches Grafana Dashboards: Create and manage production-ready Grafana dashboards for comprehensive system observabi..."
  brand_color: "#15803D"
  default_prompt: "Use $grafana-dashboards to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
