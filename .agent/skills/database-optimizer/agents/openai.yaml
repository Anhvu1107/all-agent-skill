interface:
  display_name: "Database Optimizer"
  short_description: "ALWAYS use this when the request matches Database Optimizer: Expert database optimizer specializing in modern performance tuning, query optimization..."
  brand_color: "#0F766E"
  default_prompt: "Use $database-optimizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
