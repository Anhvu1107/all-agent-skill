interface:
  display_name: "Azure Storage File Share PY"
  short_description: "ALWAYS use this when the request matches Azure Storage File Share PY: Azure Storage File Share SDK for Python."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-storage-file-share-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
