interface:
  display_name: "Writing Skills"
  short_description: "ALWAYS use this when the request matches Writing Skills: Use when creating new skills, editing existing skills, or verifying skills work before deplo..."
  brand_color: "#0F766E"
  default_prompt: "Use $writing-skills to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
