interface:
  display_name: "Analyze Project"
  short_description: "ALWAYS use this when the request matches Analyze Project: Forensic root cause analyzer for Antigravity sessions."
  brand_color: "#374151"
  default_prompt: "Use $analyze-project to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
