interface:
  display_name: "Game Development PC Games"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `game-development-pc-games`, the nested skill path form for `pc-games`, or the equival..."
  brand_color: "#BE123C"
  default_prompt: "Use $game-development-pc-games to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
