interface:
  display_name: "Astropy"
  short_description: "ALWAYS use this when the request matches Astropy: Astropy is the core Python package for astronomy, providing essential functionality for astronomica..."
  brand_color: "#EA580C"
  default_prompt: "Use $astropy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
