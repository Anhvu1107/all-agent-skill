interface:
  display_name: "Mailchimp Automation"
  short_description: "ALWAYS use this when the request matches Mailchimp Automation: Automate Mailchimp email marketing including campaigns, audiences, subscribers, segmen..."
  brand_color: "#EA580C"
  default_prompt: "Use $mailchimp-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
