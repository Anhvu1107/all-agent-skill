interface:
  display_name: "Manifest"
  short_description: "ALWAYS use this when the request matches Manifest: Install and configure the Manifest observability plugin for your agents."
  brand_color: "#EA580C"
  default_prompt: "Use $manifest to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
