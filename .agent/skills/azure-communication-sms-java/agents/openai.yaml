interface:
  display_name: "Azure Communication SMS Java"
  short_description: "ALWAYS use this when the request matches Azure Communication SMS Java: Send SMS messages with Azure Communication Services SMS Java SDK."
  brand_color: "#15803D"
  default_prompt: "Use $azure-communication-sms-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
