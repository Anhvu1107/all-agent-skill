interface:
  display_name: "Data Storytelling"
  short_description: "ALWAYS use this when the request matches Data Storytelling: Transform raw data into compelling narratives that drive decisions and inspire action."
  brand_color: "#BE123C"
  default_prompt: "Use $data-storytelling to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
