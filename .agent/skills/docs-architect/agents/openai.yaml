interface:
  display_name: "Docs Architect"
  short_description: "ALWAYS use this when the request matches Docs Architect: Creates comprehensive technical documentation from existing codebases."
  brand_color: "#2563EB"
  default_prompt: "Use $docs-architect to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
