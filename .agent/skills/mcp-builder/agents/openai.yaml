interface:
  display_name: "MCP Builder"
  short_description: "ALWAYS use this when the user wants to build, extend, debug, or wire up an MCP server, tool connector, tool schema, or agent-facing external capabili..."
  brand_color: "#15803D"
  default_prompt: "Use $mcp-builder to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
