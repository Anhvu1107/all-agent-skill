interface:
  display_name: "Frontend UI Dark TS"
  short_description: "ALWAYS use this when the request matches Frontend UI Dark TS: A modern dark-themed React UI system using Tailwind CSS and Framer Motion."
  brand_color: "#B91C1C"
  default_prompt: "Use $frontend-ui-dark-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
