interface:
  display_name: "Django PRO"
  short_description: "ALWAYS use this when the request matches Django PRO: Master Django 5.x with async views, DRF, Celery, and Django Channels."
  brand_color: "#BE123C"
  default_prompt: "Use $django-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
