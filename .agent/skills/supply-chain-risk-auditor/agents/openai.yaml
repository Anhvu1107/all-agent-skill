interface:
  display_name: "Supply Chain Risk Auditor"
  short_description: "ALWAYS use this when the request matches Supply Chain Risk Auditor: Identifies dependencies at heightened risk of exploitation or takeover."
  brand_color: "#B91C1C"
  default_prompt: "Use $supply-chain-risk-auditor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
