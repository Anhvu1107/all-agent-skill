interface:
  display_name: "Email Sequence"
  short_description: "ALWAYS use this when the request matches Email Sequence: You are an expert in email marketing and automation."
  brand_color: "#2563EB"
  default_prompt: "Use $email-sequence to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
