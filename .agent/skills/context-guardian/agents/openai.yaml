interface:
  display_name: "Context Guardian"
  short_description: "ALWAYS use this when the request matches Context Guardian: Guardiao de contexto que preserva dados criticos antes da compactacao automatica."
  brand_color: "#BE123C"
  default_prompt: "Use $context-guardian to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
