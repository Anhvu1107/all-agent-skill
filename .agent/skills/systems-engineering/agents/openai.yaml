interface:
  display_name: "Systems Engineering"
  short_description: "ALWAYS use this when the request sounds like a platform, robotics stack, digital twin, telemetry-heavy system, operator workflow, simulation-first pr..."
  brand_color: "#BE123C"
  default_prompt: "Use $systems-engineering to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
