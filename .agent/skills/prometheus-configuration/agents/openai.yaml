interface:
  display_name: "Prometheus Configuration"
  short_description: "ALWAYS use this when the request matches Prometheus Configuration: Complete guide to Prometheus setup, metric collection, scrape configuration, and r..."
  brand_color: "#2563EB"
  default_prompt: "Use $prometheus-configuration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
