interface:
  display_name: "Wiki Architect"
  short_description: "ALWAYS use this when the request matches Wiki Architect: You are a documentation architect that produces structured wiki catalogues and onboarding gu..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $wiki-architect to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
