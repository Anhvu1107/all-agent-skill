interface:
  display_name: "Makepad Shaders"
  short_description: "ALWAYS use this when the request matches Makepad Shaders: CRITICAL: Use for Makepad shader system."
  brand_color: "#B91C1C"
  default_prompt: "Use $makepad-shaders to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
