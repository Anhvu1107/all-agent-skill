interface:
  display_name: "Protect MCP Governance"
  short_description: "ALWAYS use this when the request matches Protect MCP Governance: Agent governance skill for MCP tool calls — Cedar policy authoring, shadow-to-enforc..."
  brand_color: "#EA580C"
  default_prompt: "Use $protect-mcp-governance to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
