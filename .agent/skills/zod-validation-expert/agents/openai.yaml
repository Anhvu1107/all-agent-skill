interface:
  display_name: "ZOD Validation Expert"
  short_description: "ALWAYS use this when the request matches ZOD Validation Expert: Expert in Zod — TypeScript-first schema validation."
  brand_color: "#BE123C"
  default_prompt: "Use $zod-validation-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
