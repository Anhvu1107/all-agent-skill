interface:
  display_name: "Linkedin Automation"
  short_description: "ALWAYS use this when the request matches Linkedin Automation: Automate LinkedIn tasks via Rube MCP (Composio): create posts, manage profile, company..."
  brand_color: "#374151"
  default_prompt: "Use $linkedin-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
