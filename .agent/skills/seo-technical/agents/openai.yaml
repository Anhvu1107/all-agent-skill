interface:
  display_name: "SEO Technical"
  short_description: "ALWAYS use this when the request matches SEO Technical: Audit technical SEO across crawlability, indexability, security, URLs, mobile, Core Web Vital..."
  brand_color: "#EA580C"
  default_prompt: "Use $seo-technical to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
