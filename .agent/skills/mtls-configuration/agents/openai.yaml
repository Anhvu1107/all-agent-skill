interface:
  display_name: "Mtls Configuration"
  short_description: "ALWAYS use this when the request matches Mtls Configuration: Configure mutual TLS (mTLS) for zero-trust service-to-service communication."
  brand_color: "#0F766E"
  default_prompt: "Use $mtls-configuration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
