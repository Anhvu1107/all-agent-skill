interface:
  display_name: "Linux Shell Scripting"
  short_description: "ALWAYS use this when the request matches Linux Shell Scripting: Provide production-ready shell script templates for common Linux system administratio..."
  brand_color: "#0F172A"
  default_prompt: "Use $linux-shell-scripting to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
