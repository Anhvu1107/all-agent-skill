interface:
  display_name: "AI Studio Image"
  short_description: "ALWAYS use this when the request matches AI Studio Image: Geracao de imagens humanizadas via Google AI Studio (Gemini)."
  brand_color: "#0F172A"
  default_prompt: "Use $ai-studio-image to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
