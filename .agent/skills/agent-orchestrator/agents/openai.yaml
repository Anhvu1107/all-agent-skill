interface:
  display_name: "Agent Orchestrator"
  short_description: "ALWAYS use this when the request matches Agent Orchestrator: Meta-skill que orquestra todos os agentes do ecossistema."
  brand_color: "#0F172A"
  default_prompt: "Use $agent-orchestrator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
