interface:
  display_name: "Langfuse"
  short_description: "ALWAYS use this when the request matches Langfuse: Expert in Langfuse - the open-source LLM observability platform."
  brand_color: "#B91C1C"
  default_prompt: "Use $langfuse to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
