interface:
  display_name: "Azure Keyvault Certificates Rust"
  short_description: "ALWAYS use this when the request matches Azure Keyvault Certificates Rust: Azure Key Vault Certificates SDK for Rust."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-keyvault-certificates-rust to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
