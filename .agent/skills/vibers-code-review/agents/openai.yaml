interface:
  display_name: "Vibers Code Review"
  short_description: "ALWAYS use this when the request matches Vibers Code Review: Human review workflow for AI-generated GitHub projects with spec-based feedback, securit..."
  brand_color: "#374151"
  default_prompt: "Use $vibers-code-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
