interface:
  display_name: "AI MD"
  short_description: "ALWAYS use this when the request matches AI MD: Convert human-written CLAUDE.md into AI-native structured-label format."
  brand_color: "#1D4ED8"
  default_prompt: "Use $ai-md to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
