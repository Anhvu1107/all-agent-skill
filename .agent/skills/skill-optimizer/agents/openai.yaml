interface:
  display_name: "Skill Optimizer"
  short_description: "ALWAYS use this when the request matches Skill Optimizer: Diagnose and optimize Agent Skills (SKILL.md) with real session data and research-backed st..."
  brand_color: "#B91C1C"
  default_prompt: "Use $skill-optimizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
