interface:
  display_name: "FP Option REF"
  short_description: "ALWAYS use this when the user mentions FP Option REF, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions i..."
  brand_color: "#0F766E"
  default_prompt: "Use $fp-option-ref to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
