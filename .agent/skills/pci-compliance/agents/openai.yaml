interface:
  display_name: "PCI Compliance"
  short_description: "ALWAYS use this when the request matches PCI Compliance: Master PCI DSS (Payment Card Industry Data Security Standard) compliance for secure payment..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $pci-compliance to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
