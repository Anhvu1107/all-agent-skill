interface:
  display_name: "Identity Mirror"
  short_description: "ALWAYS use this when the request matches Identity Mirror: One sentence - what this skill does and when to invoke it"
  brand_color: "#374151"
  default_prompt: "Use $identity-mirror to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
