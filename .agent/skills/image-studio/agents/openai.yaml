interface:
  display_name: "Image Studio"
  short_description: "ALWAYS use this when the request matches Image Studio: Studio de geracao de imagens inteligente — roteamento automatico entre ai-studio-image (fotos..."
  brand_color: "#15803D"
  default_prompt: "Use $image-studio to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
