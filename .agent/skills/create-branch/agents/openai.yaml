interface:
  display_name: "Create Branch"
  short_description: "ALWAYS use this when the request matches Create Branch: Create a git branch following Sentry naming conventions."
  brand_color: "#BE123C"
  default_prompt: "Use $create-branch to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
