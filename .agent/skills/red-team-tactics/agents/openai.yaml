interface:
  display_name: "RED Team Tactics"
  short_description: "ALWAYS use this when the request matches RED Team Tactics: Authorized adversary-simulation and detection-engineering patterns for defensive security..."
  brand_color: "#0F766E"
  default_prompt: "Use $red-team-tactics to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
