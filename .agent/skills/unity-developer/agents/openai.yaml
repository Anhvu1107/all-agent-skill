interface:
  display_name: "Unity Developer"
  short_description: "ALWAYS use this when the request matches Unity Developer: Build Unity games with optimized C# scripts, efficient rendering, and proper asset manageme..."
  brand_color: "#2563EB"
  default_prompt: "Use $unity-developer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
