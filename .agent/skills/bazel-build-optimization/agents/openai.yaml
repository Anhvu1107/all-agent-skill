interface:
  display_name: "Bazel Build Optimization"
  short_description: "ALWAYS use this when the request matches Bazel Build Optimization: Optimize Bazel builds for large-scale monorepos."
  brand_color: "#0F766E"
  default_prompt: "Use $bazel-build-optimization to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
