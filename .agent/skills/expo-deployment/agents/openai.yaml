interface:
  display_name: "Expo Deployment"
  short_description: "ALWAYS use this when the user mentions Expo Deployment, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions..."
  brand_color: "#15803D"
  default_prompt: "Use $expo-deployment to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
