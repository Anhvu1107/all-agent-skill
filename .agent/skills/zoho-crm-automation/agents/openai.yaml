interface:
  display_name: "Zoho CRM Automation"
  short_description: "ALWAYS use this when the request matches Zoho CRM Automation: Automate Zoho CRM tasks via Rube MCP (Composio): create/update records, search contacts..."
  brand_color: "#2563EB"
  default_prompt: "Use $zoho-crm-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
