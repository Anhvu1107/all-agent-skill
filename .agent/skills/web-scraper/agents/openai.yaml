interface:
  display_name: "WEB Scraper"
  short_description: "ALWAYS use this when the request matches WEB Scraper: Web scraping inteligente multi-estrategia."
  brand_color: "#374151"
  default_prompt: "Use $web-scraper to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
