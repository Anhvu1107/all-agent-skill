---
name: game-development-web-games
description: ALWAYS use this when the user or an upstream archive refers to `game-development-web-games`, the nested skill path form for `web-games`, or the equivalent slash path. This is a namespace-preserving alias that routes to `web-games` while retaining strict validation, Senior Master execution, and Codex review standards.
---

# Game Development WEB Games

## Selective Reading Rule

Start with:

- `references/senior-master-standard.md`
- `references/usage-routing.md`
- `references/quality-checklist.md`
- `references/alias-target.md`

Then load the canonical target skill only when the task needs the full implementation guidance.

## Alias Contract

- Treat `game-development-web-games` as a compatibility and search alias for `web-games`.
- Use canonical skill `web-games` for the substantive workflow.
- Do not duplicate large upstream instructions here; this folder exists to preserve nested ZIP path slugs such as `game/development/web/games` while keeping the runtime bundle deduplicated.
- Apply the same Senior Master standard and Codex strict review gate before final output.
