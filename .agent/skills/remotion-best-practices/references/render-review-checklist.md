# Render Review Checklist

- confirm composition dimensions, fps, and duration are intentional
- verify timing math instead of eyeballing frame counts
- check asset loading, fonts, and audio behavior before final render claims
- keep heavy logic out of frame-by-frame paths when it can be precomputed
