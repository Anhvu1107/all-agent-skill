interface:
  display_name: "Context Management Context Restore"
  short_description: "ALWAYS use this when the request matches Context Management Context Restore: Use when working with context management context restore"
  brand_color: "#EA580C"
  default_prompt: "Use $context-management-context-restore to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
