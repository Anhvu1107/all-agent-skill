interface:
  display_name: "Apify Ecommerce"
  short_description: "ALWAYS use this when the request matches Apify Ecommerce: Extract product data, prices, reviews, and seller information from any e-commerce platform..."
  brand_color: "#0F766E"
  default_prompt: "Use $apify-ecommerce to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
