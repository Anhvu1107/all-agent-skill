interface:
  display_name: "Alpha Vantage"
  short_description: "ALWAYS use this when the request matches Alpha Vantage: Access 20+ years of global financial data: equities, options, forex, crypto, commodities, eco..."
  brand_color: "#2563EB"
  default_prompt: "Use $alpha-vantage to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
