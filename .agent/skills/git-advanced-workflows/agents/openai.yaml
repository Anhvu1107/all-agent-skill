interface:
  display_name: "GIT Advanced Workflows"
  short_description: "ALWAYS use this when the request matches GIT Advanced Workflows: Master advanced Git techniques to maintain clean history, collaborate effectively, a..."
  brand_color: "#BE123C"
  default_prompt: "Use $git-advanced-workflows to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
