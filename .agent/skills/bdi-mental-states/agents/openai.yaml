interface:
  display_name: "BDI Mental States"
  short_description: "ALWAYS use this when the request matches BDI Mental States: This skill should be used when the user asks to \"model agent mental states\", \"implement B..."
  brand_color: "#BE123C"
  default_prompt: "Use $bdi-mental-states to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
