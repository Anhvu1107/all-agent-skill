interface:
  display_name: "React Native Architecture"
  short_description: "ALWAYS use this when the request matches React Native Architecture: Production-ready patterns for React Native development with Expo, including navig..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $react-native-architecture to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
