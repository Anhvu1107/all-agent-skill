interface:
  display_name: "Junta Leiloeiros"
  short_description: "ALWAYS use this when the request involves Brazilian Junta Comercial auctioneer data, scraping leiloeiro records, querying the SQLite dataset, running..."
  brand_color: "#7C3AED"
  default_prompt: "Use $junta-leiloeiros to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
