interface:
  display_name: "Autonomous Agents"
  short_description: "ALWAYS use this when the request matches Autonomous Agents: Autonomous agents are AI systems that can independently decompose goals, plan actions, ex..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $autonomous-agents to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
