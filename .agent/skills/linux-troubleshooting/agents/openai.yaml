interface:
  display_name: "Linux Troubleshooting"
  short_description: "ALWAYS use this when the request matches Linux Troubleshooting: Linux system troubleshooting workflow for diagnosing and resolving system issues, per..."
  brand_color: "#374151"
  default_prompt: "Use $linux-troubleshooting to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
