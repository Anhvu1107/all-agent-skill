interface:
  display_name: "Azure Resource Manager Redis Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Resource Manager Redis Dotnet: Azure Resource Manager SDK for Redis in .NET."
  brand_color: "#15803D"
  default_prompt: "Use $azure-resource-manager-redis-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
