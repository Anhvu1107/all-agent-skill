interface:
  display_name: "SEO Keyword Strategist"
  short_description: "ALWAYS use this when the request matches SEO Keyword Strategist: Analyzes keyword usage in provided content, calculates density, suggests semantic va..."
  brand_color: "#BE123C"
  default_prompt: "Use $seo-keyword-strategist to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
