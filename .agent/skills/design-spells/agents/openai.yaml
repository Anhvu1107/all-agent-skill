interface:
  display_name: "Design Spells"
  short_description: "ALWAYS use this when the request matches Design Spells: Curated micro-interactions and design details that add \"magic\" and personality to websites an..."
  brand_color: "#374151"
  default_prompt: "Use $design-spells to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
