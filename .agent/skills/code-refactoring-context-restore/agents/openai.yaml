interface:
  display_name: "Code Refactoring Context Restore"
  short_description: "ALWAYS use this when the request matches Code Refactoring Context Restore: Use when working with code refactoring context restore"
  brand_color: "#0F766E"
  default_prompt: "Use $code-refactoring-context-restore to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
