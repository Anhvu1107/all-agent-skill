interface:
  display_name: "Azure Communication Callautomation Java"
  short_description: "ALWAYS use this when the request matches Azure Communication Callautomation Java: Build server-side call automation workflows including IVR systems..."
  brand_color: "#7C3AED"
  default_prompt: "Use $azure-communication-callautomation-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
