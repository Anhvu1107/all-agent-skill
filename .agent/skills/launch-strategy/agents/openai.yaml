interface:
  display_name: "Launch Strategy"
  short_description: "ALWAYS use this when the request matches Launch Strategy: You are an expert in SaaS product launches and feature announcements."
  brand_color: "#EA580C"
  default_prompt: "Use $launch-strategy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
