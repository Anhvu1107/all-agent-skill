interface:
  display_name: "Indexing Issue Auditor"
  short_description: "ALWAYS use this when the request matches Indexing Issue Auditor: High-level technical SEO and site architecture auditor."
  brand_color: "#374151"
  default_prompt: "Use $indexing-issue-auditor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
