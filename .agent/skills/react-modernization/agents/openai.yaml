interface:
  display_name: "React Modernization"
  short_description: "ALWAYS use this when the request matches React Modernization: Master React version upgrades, class to hooks migration, concurrent features adoption..."
  brand_color: "#2563EB"
  default_prompt: "Use $react-modernization to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
