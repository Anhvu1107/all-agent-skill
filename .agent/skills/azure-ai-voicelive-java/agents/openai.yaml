interface:
  display_name: "Azure AI Voicelive Java"
  short_description: "ALWAYS use this when the user mentions Azure AI Voicelive Java, asks to build, debug, review, document, automate, test, configure, migrate, or make d..."
  brand_color: "#EA580C"
  default_prompt: "Use $azure-ai-voicelive-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
