interface:
  display_name: "FP Either REF"
  short_description: "ALWAYS use this when the user mentions FP Either REF, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions i..."
  brand_color: "#374151"
  default_prompt: "Use $fp-either-ref to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
