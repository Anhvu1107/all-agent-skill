interface:
  display_name: "Google Sheets Automation"
  short_description: "ALWAYS use this when the request matches Google Sheets Automation: Lightweight Google Sheets integration with standalone OAuth authentication."
  brand_color: "#0F172A"
  default_prompt: "Use $google-sheets-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
