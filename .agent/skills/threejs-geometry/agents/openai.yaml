interface:
  display_name: "Threejs Geometry"
  short_description: "ALWAYS use this when the request matches Threejs Geometry: Three.js geometry creation - built-in shapes, BufferGeometry, custom geometry, instancing."
  brand_color: "#0F172A"
  default_prompt: "Use $threejs-geometry to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
