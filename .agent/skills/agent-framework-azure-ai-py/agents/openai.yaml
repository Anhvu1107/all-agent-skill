interface:
  display_name: "Agent Framework Azure AI PY"
  short_description: "ALWAYS use this when the request matches Agent Framework Azure AI PY: Build persistent agents on Azure AI Foundry using the Microsoft Agent Framework..."
  brand_color: "#7C3AED"
  default_prompt: "Use $agent-framework-azure-ai-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
