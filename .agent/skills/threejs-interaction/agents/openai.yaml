interface:
  display_name: "Threejs Interaction"
  short_description: "ALWAYS use this when the request matches Threejs Interaction: Three.js interaction - raycasting, controls, mouse/touch input, object selection."
  brand_color: "#0F766E"
  default_prompt: "Use $threejs-interaction to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
