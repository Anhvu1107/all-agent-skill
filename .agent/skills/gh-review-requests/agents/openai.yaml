interface:
  display_name: "GH Review Requests"
  short_description: "ALWAYS use this when the request matches GH Review Requests: Fetch unread GitHub notifications for open PRs where review is requested from a specifie..."
  brand_color: "#B91C1C"
  default_prompt: "Use $gh-review-requests to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
