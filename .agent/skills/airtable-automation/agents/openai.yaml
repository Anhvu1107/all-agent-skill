interface:
  display_name: "Airtable Automation"
  short_description: "ALWAYS use this when the request matches Airtable Automation: Automate Airtable tasks via Rube MCP (Composio): records, bases, tables, fields, views."
  brand_color: "#2563EB"
  default_prompt: "Use $airtable-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
