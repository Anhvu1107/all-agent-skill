interface:
  display_name: "Security Scanning Security Dependencies"
  short_description: "ALWAYS use this when the request matches Security Scanning Security Dependencies: You are a security expert specializing in dependency vulnerability..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $security-scanning-security-dependencies to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
