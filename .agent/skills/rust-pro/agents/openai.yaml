interface:
  display_name: "Rust PRO"
  short_description: "ALWAYS use this when the request matches Rust PRO: Master Rust 1.75+ with modern async patterns, advanced type system features, and production-ready..."
  brand_color: "#7C3AED"
  default_prompt: "Use $rust-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
