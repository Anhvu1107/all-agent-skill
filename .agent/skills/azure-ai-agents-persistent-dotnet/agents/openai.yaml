interface:
  display_name: "Azure AI Agents Persistent Dotnet"
  short_description: "ALWAYS use this when the request matches Azure AI Agents Persistent Dotnet: Azure AI Agents Persistent SDK for .NET."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-ai-agents-persistent-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
