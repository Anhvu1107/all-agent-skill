interface:
  display_name: "SEO AEO Schema Generator"
  short_description: "ALWAYS use this when the request matches SEO AEO Schema Generator: Generates valid JSON-LD structured data for 10 schema types with rich result eligi..."
  brand_color: "#0F766E"
  default_prompt: "Use $seo-aeo-schema-generator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
