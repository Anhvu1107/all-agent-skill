interface:
  display_name: "PR Writer"
  short_description: "ALWAYS use this when the request matches PR Writer: Create pull requests following Sentry's engineering practices."
  brand_color: "#EA580C"
  default_prompt: "Use $pr-writer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
