interface:
  display_name: "React Component Performance"
  short_description: "ALWAYS use this when the request matches React Component Performance: Diagnose slow React components and suggest targeted performance fixes."
  brand_color: "#2563EB"
  default_prompt: "Use $react-component-performance to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
