interface:
  display_name: "Sred Work Summary"
  short_description: "ALWAYS use this when the request matches Sred Work Summary: Go back through the previous year of work and create a Notion doc that groups relevant li..."
  brand_color: "#374151"
  default_prompt: "Use $sred-work-summary to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
