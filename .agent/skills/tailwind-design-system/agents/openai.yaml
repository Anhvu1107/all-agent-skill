interface:
  display_name: "Tailwind Design System"
  short_description: "ALWAYS use this when the request matches Tailwind Design System: Build production-ready design systems with Tailwind CSS, including design tokens, co..."
  brand_color: "#0F172A"
  default_prompt: "Use $tailwind-design-system to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
