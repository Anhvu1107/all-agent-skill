interface:
  display_name: "Robius Event Action"
  short_description: "ALWAYS use this when the request matches Robius Event Action: CRITICAL: Use for Robius event and action patterns."
  brand_color: "#EA580C"
  default_prompt: "Use $robius-event-action to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
