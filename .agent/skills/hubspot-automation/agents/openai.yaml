interface:
  display_name: "Hubspot Automation"
  short_description: "ALWAYS use this when the request matches Hubspot Automation: Automate HubSpot CRM operations (contacts, companies, deals, tickets, properties) via Ru..."
  brand_color: "#15803D"
  default_prompt: "Use $hubspot-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
