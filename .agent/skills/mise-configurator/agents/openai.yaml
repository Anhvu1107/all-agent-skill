interface:
  display_name: "Mise Configurator"
  short_description: "ALWAYS use this when the request matches Mise Configurator: Generate production-ready mise.toml setups for local development, CI/CD pipelines, and to..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $mise-configurator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
