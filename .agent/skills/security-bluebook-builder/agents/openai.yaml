interface:
  display_name: "Security Bluebook Builder"
  short_description: "ALWAYS use this when the request matches Security Bluebook Builder: Build a minimal but real security policy for sensitive apps."
  brand_color: "#2563EB"
  default_prompt: "Use $security-bluebook-builder to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
