interface:
  display_name: "Agentphone"
  short_description: "ALWAYS use this when the request matches Agentphone: Build AI phone agents with AgentPhone API."
  brand_color: "#EA580C"
  default_prompt: "Use $agentphone to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
