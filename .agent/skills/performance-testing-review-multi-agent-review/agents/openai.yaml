interface:
  display_name: "Performance Testing Review Multi Agent Review"
  short_description: "ALWAYS use this when the request matches Performance Testing Review Multi Agent Review: Use when working with performance testing review multi agent..."
  brand_color: "#374151"
  default_prompt: "Use $performance-testing-review-multi-agent-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
