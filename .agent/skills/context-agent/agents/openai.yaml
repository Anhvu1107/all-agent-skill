interface:
  display_name: "Context Agent"
  short_description: "ALWAYS use this when the request matches Context Agent: Agente de contexto para continuidade entre sessoes."
  brand_color: "#B91C1C"
  default_prompt: "Use $context-agent to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
