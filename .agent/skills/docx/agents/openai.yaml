interface:
  display_name: "Docx"
  short_description: "ALWAYS use this when the user needs a Word document created, edited, inspected, validated, templated, or transformed, including tracked changes, comm..."
  brand_color: "#BE123C"
  default_prompt: "Use $docx to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
