interface:
  display_name: "Trust Calibrator"
  short_description: "ALWAYS use this when the request matches Trust Calibrator: One sentence - what this skill does and when to invoke it"
  brand_color: "#B91C1C"
  default_prompt: "Use $trust-calibrator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
