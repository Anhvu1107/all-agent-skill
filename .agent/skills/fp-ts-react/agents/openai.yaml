interface:
  display_name: "FP TS React"
  short_description: "ALWAYS use this when the request matches FP TS React: Practical patterns for using fp-ts with React - hooks, state, forms, data fetching."
  brand_color: "#0F766E"
  default_prompt: "Use $fp-ts-react to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
