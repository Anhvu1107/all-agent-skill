interface:
  display_name: "Bash Linux"
  short_description: "ALWAYS use this when the user mentions Bash Linux, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in t..."
  brand_color: "#B91C1C"
  default_prompt: "Use $bash-linux to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
