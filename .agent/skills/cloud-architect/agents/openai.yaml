interface:
  display_name: "Cloud Architect"
  short_description: "ALWAYS use this when the request matches Cloud Architect: Expert cloud architect specializing in AWS/Azure/GCP multi-cloud infrastructure design, adv..."
  brand_color: "#BE123C"
  default_prompt: "Use $cloud-architect to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
