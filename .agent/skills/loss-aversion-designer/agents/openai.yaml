interface:
  display_name: "Loss Aversion Designer"
  short_description: "ALWAYS use this when the request matches Loss Aversion Designer: One sentence - what this skill does and when to invoke it"
  brand_color: "#0F172A"
  default_prompt: "Use $loss-aversion-designer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
