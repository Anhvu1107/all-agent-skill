interface:
  display_name: "Azure AI Agents Persistent Java"
  short_description: "ALWAYS use this when the request matches Azure AI Agents Persistent Java: Azure AI Agents Persistent SDK for Java."
  brand_color: "#15803D"
  default_prompt: "Use $azure-ai-agents-persistent-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
