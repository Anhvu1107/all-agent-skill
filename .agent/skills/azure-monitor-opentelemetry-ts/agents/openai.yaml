interface:
  display_name: "Azure Monitor Opentelemetry TS"
  short_description: "ALWAYS use this when the request matches Azure Monitor Opentelemetry TS: Auto-instrument Node.js applications with distributed tracing, metrics, and..."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-monitor-opentelemetry-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
