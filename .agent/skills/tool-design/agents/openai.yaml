interface:
  display_name: "Tool Design"
  short_description: "ALWAYS use this when the request matches Tool Design: Build tools that agents can use effectively, including architectural reduction patterns."
  brand_color: "#0F766E"
  default_prompt: "Use $tool-design to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
