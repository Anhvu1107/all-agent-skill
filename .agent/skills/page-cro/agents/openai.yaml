interface:
  display_name: "Page CRO"
  short_description: "ALWAYS use this when the request matches Page CRO: Analyze and optimize individual pages for conversion performance."
  brand_color: "#15803D"
  default_prompt: "Use $page-cro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
