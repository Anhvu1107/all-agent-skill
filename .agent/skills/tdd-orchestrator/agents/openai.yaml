interface:
  display_name: "TDD Orchestrator"
  short_description: "ALWAYS use this when the request matches TDD Orchestrator: Master TDD orchestrator specializing in red-green-refactor discipline, multi-agent workflo..."
  brand_color: "#0F172A"
  default_prompt: "Use $tdd-orchestrator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
