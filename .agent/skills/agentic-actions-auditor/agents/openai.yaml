interface:
  display_name: "Agentic Actions Auditor"
  short_description: "ALWAYS use this when the request matches Agentic Actions Auditor: Audits GitHub Actions workflows for security vulnerabilities in AI agent integratio..."
  brand_color: "#0F766E"
  default_prompt: "Use $agentic-actions-auditor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
