interface:
  display_name: "Agentfolio"
  short_description: "ALWAYS use this when the request matches Agentfolio: Skill for discovering and researching autonomous AI agents, tools, and ecosystems using the Agen..."
  brand_color: "#0F766E"
  default_prompt: "Use $agentfolio to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
