interface:
  display_name: "LLM Structured Output"
  short_description: "ALWAYS use this when the request matches LLM Structured Output: Get reliable JSON, enums, and typed objects from LLMs using response_format, tool_use..."
  brand_color: "#7C3AED"
  default_prompt: "Use $llm-structured-output to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
