interface:
  display_name: "Sales Automator"
  short_description: "ALWAYS use this when the request matches Sales Automator: Draft cold emails, follow-ups, and proposal templates."
  brand_color: "#B91C1C"
  default_prompt: "Use $sales-automator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
