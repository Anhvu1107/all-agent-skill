interface:
  display_name: "Browser Automation"
  short_description: "ALWAYS use this when the request matches Browser Automation: Browser automation powers web testing, scraping, and AI agent interactions."
  brand_color: "#B91C1C"
  default_prompt: "Use $browser-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
