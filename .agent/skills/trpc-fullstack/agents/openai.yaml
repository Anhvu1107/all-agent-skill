interface:
  display_name: "Trpc Fullstack"
  short_description: "ALWAYS use this when the request matches Trpc Fullstack: Build end-to-end type-safe APIs with tRPC — routers, procedures, middleware, subscriptions..."
  brand_color: "#BE123C"
  default_prompt: "Use $trpc-fullstack to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
