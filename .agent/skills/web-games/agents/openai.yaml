interface:
  display_name: "WEB Games"
  short_description: "ALWAYS use this when the request matches WEB Games: Web browser game development principles."
  brand_color: "#BE123C"
  default_prompt: "Use $web-games to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
