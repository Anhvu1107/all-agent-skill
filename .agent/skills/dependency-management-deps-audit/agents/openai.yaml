interface:
  display_name: "Dependency Management Deps Audit"
  short_description: "ALWAYS use this when the request matches Dependency Management Deps Audit: You are a dependency security expert specializing in vulnerability scannin..."
  brand_color: "#2563EB"
  default_prompt: "Use $dependency-management-deps-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
