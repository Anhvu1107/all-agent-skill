interface:
  display_name: "Startup Metrics Framework"
  short_description: "ALWAYS use this when the request matches Startup Metrics Framework: Comprehensive guide to tracking, calculating, and optimizing key performance metr..."
  brand_color: "#0F766E"
  default_prompt: "Use $startup-metrics-framework to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
