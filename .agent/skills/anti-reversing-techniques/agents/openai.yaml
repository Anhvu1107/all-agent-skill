interface:
  display_name: "Anti Reversing Techniques"
  short_description: "ALWAYS use this when the request matches Anti Reversing Techniques: AUTHORIZED USE ONLY: This skill contains dual-use security techniques."
  brand_color: "#BE123C"
  default_prompt: "Use $anti-reversing-techniques to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
