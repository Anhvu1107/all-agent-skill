interface:
  display_name: "SEO Content Planner"
  short_description: "ALWAYS use this when the request matches SEO Content Planner: Creates comprehensive content outlines and topic clusters for SEO."
  brand_color: "#B91C1C"
  default_prompt: "Use $seo-content-planner to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
