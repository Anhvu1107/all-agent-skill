interface:
  display_name: "Broken Authentication"
  short_description: "ALWAYS use this when the request matches Broken Authentication: Identify and exploit authentication and session management vulnerabilities in web app..."
  brand_color: "#EA580C"
  default_prompt: "Use $broken-authentication to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
