interface:
  display_name: "LLM Prompt Optimizer"
  short_description: "ALWAYS use this when the request matches LLM Prompt Optimizer: Use when improving prompts for any LLM."
  brand_color: "#0F766E"
  default_prompt: "Use $llm-prompt-optimizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
