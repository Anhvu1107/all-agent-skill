interface:
  display_name: "Popup CRO"
  short_description: "ALWAYS use this when the request matches Popup CRO: Create and optimize popups, modals, overlays, slide-ins, and banners to increase conversions with..."
  brand_color: "#B91C1C"
  default_prompt: "Use $popup-cro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
