interface:
  display_name: "Bash Scripting"
  short_description: "ALWAYS use this when the request matches Bash Scripting: Bash scripting workflow for creating production-ready shell scripts with defensive patterns..."
  brand_color: "#EA580C"
  default_prompt: "Use $bash-scripting to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
