interface:
  display_name: "HIG Foundations"
  short_description: "ALWAYS use this when the request matches HIG Foundations: Apple Human Interface Guidelines design foundations."
  brand_color: "#0F766E"
  default_prompt: "Use $hig-foundations to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
