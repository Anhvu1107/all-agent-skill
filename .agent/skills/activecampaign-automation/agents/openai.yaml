interface:
  display_name: "Activecampaign Automation"
  short_description: "ALWAYS use this when the request matches Activecampaign Automation: Automate ActiveCampaign tasks via Rube MCP (Composio): manage contacts, tags, lis..."
  brand_color: "#15803D"
  default_prompt: "Use $activecampaign-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
