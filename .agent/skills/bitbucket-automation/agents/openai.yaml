interface:
  display_name: "Bitbucket Automation"
  short_description: "ALWAYS use this when the request matches Bitbucket Automation: Automate Bitbucket repositories, pull requests, branches, issues, and workspace manage..."
  brand_color: "#B91C1C"
  default_prompt: "Use $bitbucket-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
