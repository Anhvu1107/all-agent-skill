interface:
  display_name: "Close Automation"
  short_description: "ALWAYS use this when the request matches Close Automation: Automate Close CRM tasks via Rube MCP (Composio): create leads, manage calls/SMS, handle t..."
  brand_color: "#7C3AED"
  default_prompt: "Use $close-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
