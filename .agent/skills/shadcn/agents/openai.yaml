interface:
  display_name: "Shadcn"
  short_description: "ALWAYS use this when adding, customizing, debugging, or composing shadcn/ui components, registries, themes, or CLI-driven design-system setup in a pr..."
  brand_color: "#0F172A"
  default_prompt: "Use $shadcn to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
