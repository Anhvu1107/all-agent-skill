interface:
  display_name: "Azure Mgmt Mongodbatlas Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Mgmt Mongodbatlas Dotnet: Manage MongoDB Atlas Organizations as Azure ARM resources with unified billi..."
  brand_color: "#EA580C"
  default_prompt: "Use $azure-mgmt-mongodbatlas-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
