interface:
  display_name: "Discord Automation"
  short_description: "ALWAYS use this when the request matches Discord Automation: Automate Discord tasks via Rube MCP (Composio): messages, channels, roles, webhooks, rea..."
  brand_color: "#374151"
  default_prompt: "Use $discord-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
