interface:
  display_name: "Swiftui Liquid Glass"
  short_description: "ALWAYS use this when the request matches Swiftui Liquid Glass: Implement or review SwiftUI Liquid Glass APIs with correct fallbacks and modifier orde..."
  brand_color: "#374151"
  default_prompt: "Use $swiftui-liquid-glass to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
