interface:
  display_name: "Weightloss Analyzer"
  short_description: "ALWAYS use this when the user mentions Weightloss Analyzer, asks to build, debug, review, document, automate, test, configure, migrate, or make decis..."
  brand_color: "#7C3AED"
  default_prompt: "Use $weightloss-analyzer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
