interface:
  display_name: "Frontend Mobile Security XSS Scan"
  short_description: "ALWAYS use this when the request matches Frontend Mobile Security XSS Scan: You are a frontend security specialist focusing on Cross-Site Scripting (..."
  brand_color: "#374151"
  default_prompt: "Use $frontend-mobile-security-xss-scan to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
