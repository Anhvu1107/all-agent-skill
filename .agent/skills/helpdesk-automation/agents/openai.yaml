interface:
  display_name: "Helpdesk Automation"
  short_description: "ALWAYS use this when the request matches Helpdesk Automation: Automate HelpDesk tasks via Rube MCP (Composio): list tickets, manage views, use canned..."
  brand_color: "#15803D"
  default_prompt: "Use $helpdesk-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
