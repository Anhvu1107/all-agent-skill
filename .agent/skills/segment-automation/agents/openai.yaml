interface:
  display_name: "Segment Automation"
  short_description: "ALWAYS use this when the request matches Segment Automation: Automate Segment tasks via Rube MCP (Composio): track events, identify users, manage gro..."
  brand_color: "#EA580C"
  default_prompt: "Use $segment-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
