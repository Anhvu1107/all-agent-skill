interface:
  display_name: "Advanced Evaluation"
  short_description: "ALWAYS use this when the request matches Advanced Evaluation: This skill should be used when the user asks to \"implement LLM-as-judge\", \"compare mode..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $advanced-evaluation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
