interface:
  display_name: "Cred Omega"
  short_description: "ALWAYS use this when the request matches Cred Omega: CISO operacional enterprise para gestao total de credenciais e segredos."
  brand_color: "#1D4ED8"
  default_prompt: "Use $cred-omega to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
