interface:
  display_name: "Electron Development"
  short_description: "ALWAYS use this when the request matches Electron Development: Master Electron desktop app development with secure IPC, contextIsolation, preload scr..."
  brand_color: "#EA580C"
  default_prompt: "Use $electron-development to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
