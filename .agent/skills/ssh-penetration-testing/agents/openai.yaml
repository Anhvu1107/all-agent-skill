interface:
  display_name: "SSH Penetration Testing"
  short_description: "ALWAYS use this when the request matches SSH Penetration Testing: Conduct comprehensive SSH security assessments including enumeration, credential at..."
  brand_color: "#7C3AED"
  default_prompt: "Use $ssh-penetration-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
