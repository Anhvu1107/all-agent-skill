interface:
  display_name: "Screen Reader Testing"
  short_description: "ALWAYS use this when the request matches Screen Reader Testing: Practical guide to testing web applications with screen readers for comprehensive acc..."
  brand_color: "#0F172A"
  default_prompt: "Use $screen-reader-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
