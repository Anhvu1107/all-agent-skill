interface:
  display_name: "BOX Automation"
  short_description: "ALWAYS use this when the request matches BOX Automation: Automate Box operations including file upload/download, content search, folder management, c..."
  brand_color: "#0F766E"
  default_prompt: "Use $box-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
