interface:
  display_name: "Mlops Engineer"
  short_description: "ALWAYS use this when the request matches Mlops Engineer: Build comprehensive ML pipelines, experiment tracking, and model registries with MLflow, Kub..."
  brand_color: "#0F172A"
  default_prompt: "Use $mlops-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
