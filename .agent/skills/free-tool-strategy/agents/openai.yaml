interface:
  display_name: "Free Tool Strategy"
  short_description: "ALWAYS use this when the request matches Free Tool Strategy: You are an expert in engineering-as-marketing strategy."
  brand_color: "#BE123C"
  default_prompt: "Use $free-tool-strategy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
