interface:
  display_name: "Tutorial Engineer"
  short_description: "ALWAYS use this when the request matches Tutorial Engineer: Creates step-by-step tutorials and educational content from code."
  brand_color: "#BE123C"
  default_prompt: "Use $tutorial-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
