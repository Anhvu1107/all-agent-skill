interface:
  display_name: "Gitops Workflow"
  short_description: "ALWAYS use this when the request matches Gitops Workflow: Complete guide to implementing GitOps workflows with ArgoCD and Flux for automated Kubernet..."
  brand_color: "#0F172A"
  default_prompt: "Use $gitops-workflow to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
