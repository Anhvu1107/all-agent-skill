interface:
  display_name: "HIG Components Status"
  short_description: "ALWAYS use this when the request matches HIG Components Status: Apple HIG guidance for status and progress UI components including progress indicator..."
  brand_color: "#B91C1C"
  default_prompt: "Use $hig-components-status to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
