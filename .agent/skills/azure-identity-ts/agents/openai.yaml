interface:
  display_name: "Azure Identity TS"
  short_description: "ALWAYS use this when the request matches Azure Identity TS: Authenticate to Azure services with various credential types."
  brand_color: "#374151"
  default_prompt: "Use $azure-identity-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
