interface:
  display_name: "Odoo Security Rules"
  short_description: "ALWAYS use this when the request matches Odoo Security Rules: Expert in Odoo access control: ir.model.access.csv, record rules (ir.rule), groups, and..."
  brand_color: "#374151"
  default_prompt: "Use $odoo-security-rules to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
