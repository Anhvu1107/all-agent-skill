interface:
  display_name: "Observability Engineer"
  short_description: "ALWAYS use this when the request matches Observability Engineer: Build production-ready monitoring, logging, and tracing systems."
  brand_color: "#1D4ED8"
  default_prompt: "Use $observability-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
