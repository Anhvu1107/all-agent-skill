interface:
  display_name: "Pydantic Models PY"
  short_description: "ALWAYS use this when the request matches Pydantic Models PY: Create Pydantic models following the multi-model pattern for clean API contracts."
  brand_color: "#EA580C"
  default_prompt: "Use $pydantic-models-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
