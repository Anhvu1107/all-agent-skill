interface:
  display_name: "Redesign Existing Projects"
  short_description: "ALWAYS use this when the request matches Redesign Existing Projects: Use when upgrading existing websites or apps by auditing generic UI patterns and..."
  brand_color: "#BE123C"
  default_prompt: "Use $redesign-existing-projects to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
