interface:
  display_name: "Shopify Development"
  short_description: "ALWAYS use this when the request matches Shopify Development: Build Shopify apps, extensions, themes using GraphQL Admin API, Shopify CLI, Polaris UI..."
  brand_color: "#EA580C"
  default_prompt: "Use $shopify-development to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
