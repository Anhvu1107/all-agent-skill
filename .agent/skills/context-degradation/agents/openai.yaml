interface:
  display_name: "Context Degradation"
  short_description: "ALWAYS use this when the request matches Context Degradation: Language models exhibit predictable degradation patterns as context length increases."
  brand_color: "#15803D"
  default_prompt: "Use $context-degradation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
