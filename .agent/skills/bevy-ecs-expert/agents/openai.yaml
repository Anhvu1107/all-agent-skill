interface:
  display_name: "Bevy ECS Expert"
  short_description: "ALWAYS use this when the request matches Bevy ECS Expert: Master Bevy's Entity Component System (ECS) in Rust, covering Systems, Queries, Resources..."
  brand_color: "#0F766E"
  default_prompt: "Use $bevy-ecs-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
