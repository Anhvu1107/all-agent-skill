interface:
  display_name: "C4 Code"
  short_description: "ALWAYS use this when the request matches C4 Code: Expert C4 Code-level documentation specialist."
  brand_color: "#EA580C"
  default_prompt: "Use $c4-code to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
