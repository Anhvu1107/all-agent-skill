interface:
  display_name: "Supabase Automation"
  short_description: "ALWAYS use this when the request matches Supabase Automation: Automate Supabase database queries, table management, project administration, storage..."
  brand_color: "#374151"
  default_prompt: "Use $supabase-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
