interface:
  display_name: "Wiki QA"
  short_description: "ALWAYS use this when the request matches Wiki QA: Answer repository questions grounded entirely in source code evidence."
  brand_color: "#1D4ED8"
  default_prompt: "Use $wiki-qa to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
