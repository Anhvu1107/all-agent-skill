interface:
  display_name: "Laravel Security Audit"
  short_description: "ALWAYS use this when the request matches Laravel Security Audit: Security auditor for Laravel applications."
  brand_color: "#0F172A"
  default_prompt: "Use $laravel-security-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
