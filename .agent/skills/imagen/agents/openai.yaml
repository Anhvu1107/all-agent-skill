interface:
  display_name: "Imagen"
  short_description: "ALWAYS use this when the request matches Imagen: AI image generation skill powered by Google Gemini, enabling seamless visual content creation for UI..."
  brand_color: "#7C3AED"
  default_prompt: "Use $imagen to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
