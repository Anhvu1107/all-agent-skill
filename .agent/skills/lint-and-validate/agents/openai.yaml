interface:
  display_name: "Lint AND Validate"
  short_description: "ALWAYS use this when the request matches Lint AND Validate: Automatic quality control, linting, and static analysis procedures."
  brand_color: "#374151"
  default_prompt: "Use $lint-and-validate to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
