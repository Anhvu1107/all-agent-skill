interface:
  display_name: "React Best Practices"
  short_description: "ALWAYS use this when the request matches React Best Practices: Comprehensive performance optimization guide for React and Next.js applications, maint..."
  brand_color: "#7C3AED"
  default_prompt: "Use $react-best-practices to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
