interface:
  display_name: "N8n Node Configuration"
  short_description: "ALWAYS use this when the request matches N8n Node Configuration: Operation-aware node configuration guidance."
  brand_color: "#B91C1C"
  default_prompt: "Use $n8n-node-configuration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
