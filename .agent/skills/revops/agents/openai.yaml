interface:
  display_name: "Revops"
  short_description: "ALWAYS use this when the request matches Revops: Design and improve revenue operations, lead lifecycle rules, scoring, routing, handoffs, and CRM pro..."
  brand_color: "#7C3AED"
  default_prompt: "Use $revops to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
