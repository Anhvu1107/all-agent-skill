interface:
  display_name: "Makepad Skills"
  short_description: "ALWAYS use this when the request matches Makepad Skills: Makepad UI development skills for Rust apps: setup, patterns, shaders, packaging, and troubl..."
  brand_color: "#B91C1C"
  default_prompt: "Use $makepad-skills to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
