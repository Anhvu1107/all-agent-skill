interface:
  display_name: "Food Database Query"
  short_description: "ALWAYS use this when the user mentions Food Database Query, asks to build, debug, review, document, automate, test, configure, migrate, or make decis..."
  brand_color: "#EA580C"
  default_prompt: "Use $food-database-query to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
