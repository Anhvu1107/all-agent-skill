interface:
  display_name: "Azure Compute Batch Java"
  short_description: "ALWAYS use this when the user mentions Azure Compute Batch Java, asks to build, debug, review, document, automate, test, configure, migrate, or make..."
  brand_color: "#0F766E"
  default_prompt: "Use $azure-compute-batch-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
