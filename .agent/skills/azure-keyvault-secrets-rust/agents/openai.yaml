interface:
  display_name: "Azure Keyvault Secrets Rust"
  short_description: "ALWAYS use this when the request matches Azure Keyvault Secrets Rust: Azure Key Vault Secrets SDK for Rust."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-keyvault-secrets-rust to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
