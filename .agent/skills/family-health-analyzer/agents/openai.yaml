interface:
  display_name: "Family Health Analyzer"
  short_description: "ALWAYS use this when the user mentions Family Health Analyzer, asks to build, debug, review, document, automate, test, configure, migrate, or make de..."
  brand_color: "#B91C1C"
  default_prompt: "Use $family-health-analyzer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
