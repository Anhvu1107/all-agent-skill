interface:
  display_name: "N8n Code Python"
  short_description: "ALWAYS use this when the request matches N8n Code Python: Write Python code in n8n Code nodes."
  brand_color: "#374151"
  default_prompt: "Use $n8n-code-python to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
