interface:
  display_name: "Goal Analyzer"
  short_description: "ALWAYS use this when the request matches Goal Analyzer: 分析健康目标数据、识别目标模式、评估目标进度,并提供个性化目标管理建议。支持与营养、运动、睡眠等健康数据的关联分析。"
  brand_color: "#374151"
  default_prompt: "Use $goal-analyzer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
