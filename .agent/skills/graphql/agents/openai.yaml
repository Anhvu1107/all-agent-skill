interface:
  display_name: "Graphql"
  short_description: "ALWAYS use this when the request matches Graphql: GraphQL gives clients exactly the data they need - no more, no less."
  brand_color: "#B91C1C"
  default_prompt: "Use $graphql to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
