interface:
  display_name: "Incident Runbook Templates"
  short_description: "ALWAYS use this when the request matches Incident Runbook Templates: Production-ready templates for incident response runbooks covering detection, tr..."
  brand_color: "#BE123C"
  default_prompt: "Use $incident-runbook-templates to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
