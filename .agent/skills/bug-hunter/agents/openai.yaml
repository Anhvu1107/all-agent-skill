interface:
  display_name: "BUG Hunter"
  short_description: "ALWAYS use this when the request matches BUG Hunter: Systematically finds and fixes bugs using proven debugging techniques."
  brand_color: "#BE123C"
  default_prompt: "Use $bug-hunter to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
