interface:
  display_name: "Astro"
  short_description: "ALWAYS use this when the request matches Astro: Build content-focused websites with Astro — zero JS by default, islands architecture, multi-framework..."
  brand_color: "#EA580C"
  default_prompt: "Use $astro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
