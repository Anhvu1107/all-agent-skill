interface:
  display_name: "Claude Settings Audit"
  short_description: "ALWAYS use this when the request matches Claude Settings Audit: Analyze a repository to generate recommended Claude Code settings.json permissions."
  brand_color: "#B91C1C"
  default_prompt: "Use $claude-settings-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
