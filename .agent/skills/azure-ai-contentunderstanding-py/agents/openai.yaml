interface:
  display_name: "Azure AI Contentunderstanding PY"
  short_description: "ALWAYS use this when the request matches Azure AI Contentunderstanding PY: Azure AI Content Understanding SDK for Python."
  brand_color: "#0F172A"
  default_prompt: "Use $azure-ai-contentunderstanding-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
