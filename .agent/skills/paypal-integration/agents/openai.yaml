interface:
  display_name: "Paypal Integration"
  short_description: "ALWAYS use this when the request matches Paypal Integration: Master PayPal payment integration including Express Checkout, IPN handling, recurring bi..."
  brand_color: "#15803D"
  default_prompt: "Use $paypal-integration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
