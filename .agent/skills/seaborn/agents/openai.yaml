interface:
  display_name: "Seaborn"
  short_description: "ALWAYS use this when the request matches Seaborn: Seaborn is a Python visualization library for creating publication-quality statistical graphics."
  brand_color: "#BE123C"
  default_prompt: "Use $seaborn to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
