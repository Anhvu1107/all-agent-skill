interface:
  display_name: "Incident Response Smart FIX"
  short_description: "ALWAYS use this when the request matches Incident Response Smart FIX: [Extended thinking: This workflow implements a sophisticated debugging and reso..."
  brand_color: "#374151"
  default_prompt: "Use $incident-response-smart-fix to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
