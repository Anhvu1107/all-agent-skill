interface:
  display_name: "Csharp PRO"
  short_description: "ALWAYS use this when the request matches Csharp PRO: Write modern C# code with advanced features like records, pattern matching, and async/await."
  brand_color: "#7C3AED"
  default_prompt: "Use $csharp-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
