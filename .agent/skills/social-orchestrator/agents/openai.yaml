interface:
  display_name: "Social Orchestrator"
  short_description: "ALWAYS use this when the request matches Social Orchestrator: Orquestrador unificado de canais sociais — coordena Instagram, Telegram e WhatsApp em u..."
  brand_color: "#B91C1C"
  default_prompt: "Use $social-orchestrator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
