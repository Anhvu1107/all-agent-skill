interface:
  display_name: "Event Store Design"
  short_description: "ALWAYS use this when the request matches Event Store Design: Design and implement event stores for event-sourced systems."
  brand_color: "#0F766E"
  default_prompt: "Use $event-store-design to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
