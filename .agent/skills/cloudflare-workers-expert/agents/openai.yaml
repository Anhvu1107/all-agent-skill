interface:
  display_name: "Cloudflare Workers Expert"
  short_description: "ALWAYS use this when the request matches Cloudflare Workers Expert: Expert in Cloudflare Workers and the Edge Computing ecosystem."
  brand_color: "#BE123C"
  default_prompt: "Use $cloudflare-workers-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
