interface:
  display_name: "Superpowers LAB"
  short_description: "ALWAYS use this when the request matches Superpowers LAB: Lab environment for Claude superpowers"
  brand_color: "#0F766E"
  default_prompt: "Use $superpowers-lab to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
