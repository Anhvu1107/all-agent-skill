interface:
  display_name: "Algolia Search"
  short_description: "ALWAYS use this when the request matches Algolia Search: Expert patterns for Algolia search implementation, indexing strategies, React InstantSearch..."
  brand_color: "#2563EB"
  default_prompt: "Use $algolia-search to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
