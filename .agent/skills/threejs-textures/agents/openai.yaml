interface:
  display_name: "Threejs Textures"
  short_description: "ALWAYS use this when the request matches Threejs Textures: Three.js textures - texture types, UV mapping, environment maps, texture settings."
  brand_color: "#0F766E"
  default_prompt: "Use $threejs-textures to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
