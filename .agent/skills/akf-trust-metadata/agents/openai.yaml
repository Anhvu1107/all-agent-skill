interface:
  display_name: "AKF Trust Metadata"
  short_description: "ALWAYS use this when the user mentions AKF Trust Metadata, asks to build, debug, review, document, automate, test, configure, migrate, or make decisi..."
  brand_color: "#B91C1C"
  default_prompt: "Use $akf-trust-metadata to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
