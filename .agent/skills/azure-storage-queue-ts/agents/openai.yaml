interface:
  display_name: "Azure Storage Queue TS"
  short_description: "ALWAYS use this when the request matches Azure Storage Queue TS: Azure Queue Storage JavaScript/TypeScript SDK (@azure/storage-queue) for message que..."
  brand_color: "#374151"
  default_prompt: "Use $azure-storage-queue-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
