interface:
  display_name: "Bdistill Knowledge Extraction"
  short_description: "ALWAYS use this when the request matches Bdistill Knowledge Extraction: Extract structured domain knowledge from AI models in-session or from local o..."
  brand_color: "#EA580C"
  default_prompt: "Use $bdistill-knowledge-extraction to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
