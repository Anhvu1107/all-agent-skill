interface:
  display_name: "Azure Resource Manager Mysql Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Resource Manager Mysql Dotnet: Azure MySQL Flexible Server SDK for .NET."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-resource-manager-mysql-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
