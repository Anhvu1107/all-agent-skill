interface:
  display_name: "Database Migrations Migration Observability"
  short_description: "ALWAYS use this when the request matches Database Migrations Migration Observability: Migration monitoring, CDC, and observability infrastructure"
  brand_color: "#374151"
  default_prompt: "Use $database-migrations-migration-observability to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
