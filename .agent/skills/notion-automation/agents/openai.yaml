interface:
  display_name: "Notion Automation"
  short_description: "ALWAYS use this when the request matches Notion Automation: Automate Notion tasks via Rube MCP (Composio): pages, databases, blocks, comments, users."
  brand_color: "#EA580C"
  default_prompt: "Use $notion-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
