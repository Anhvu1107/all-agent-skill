interface:
  display_name: "Avoid AI Writing"
  short_description: "ALWAYS use this when the request matches Avoid AI Writing: Audit and rewrite content to remove 21 categories of AI writing patterns with a 43-entry r..."
  brand_color: "#B91C1C"
  default_prompt: "Use $avoid-ai-writing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
