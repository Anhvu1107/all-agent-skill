interface:
  display_name: "Axiom"
  short_description: "ALWAYS use this when the request matches Axiom: First-principles assumption auditor."
  brand_color: "#0F172A"
  default_prompt: "Use $axiom to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
