interface:
  display_name: "Azure Mgmt Fabric PY"
  short_description: "ALWAYS use this when the request matches Azure Mgmt Fabric PY: Azure Fabric Management SDK for Python."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-mgmt-fabric-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
