interface:
  display_name: "Threat Mitigation Mapping"
  short_description: "ALWAYS use this when the request matches Threat Mitigation Mapping: Map identified threats to appropriate security controls and mitigations."
  brand_color: "#0F172A"
  default_prompt: "Use $threat-mitigation-mapping to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
