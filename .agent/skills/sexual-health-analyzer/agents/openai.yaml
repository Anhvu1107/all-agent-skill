interface:
  display_name: "Sexual Health Analyzer"
  short_description: "ALWAYS use this when the user mentions Sexual Health Analyzer, asks to build, debug, review, document, automate, test, configure, migrate, or make de..."
  brand_color: "#0F766E"
  default_prompt: "Use $sexual-health-analyzer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
