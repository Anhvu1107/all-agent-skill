interface:
  display_name: "Calc"
  short_description: "ALWAYS use this when the request matches Calc: Spreadsheet creation, format conversion (ODS/XLSX/CSV), formulas, data automation with LibreOffice Cal..."
  brand_color: "#2563EB"
  default_prompt: "Use $calc to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
