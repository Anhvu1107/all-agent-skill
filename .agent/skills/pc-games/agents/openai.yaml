interface:
  display_name: "PC Games"
  short_description: "ALWAYS use this when the request matches PC Games: PC and console game development principles."
  brand_color: "#BE123C"
  default_prompt: "Use $pc-games to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
