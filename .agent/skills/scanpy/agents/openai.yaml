interface:
  display_name: "Scanpy"
  short_description: "ALWAYS use this when the request matches Scanpy: Scanpy is a scalable Python toolkit for analyzing single-cell RNA-seq data, built on AnnData."
  brand_color: "#374151"
  default_prompt: "Use $scanpy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
