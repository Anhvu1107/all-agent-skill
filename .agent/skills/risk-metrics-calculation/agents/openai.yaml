interface:
  display_name: "Risk Metrics Calculation"
  short_description: "ALWAYS use this when the request matches Risk Metrics Calculation: Calculate portfolio risk metrics including VaR, CVaR, Sharpe, Sortino, and drawdow..."
  brand_color: "#B91C1C"
  default_prompt: "Use $risk-metrics-calculation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
