interface:
  display_name: "Behavioral Modes"
  short_description: "ALWAYS use this when the request matches Behavioral Modes: AI operational modes (brainstorm, implement, debug, review, teach, ship, orchestrate)."
  brand_color: "#2563EB"
  default_prompt: "Use $behavioral-modes to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
