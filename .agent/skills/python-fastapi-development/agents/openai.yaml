interface:
  display_name: "Python Fastapi Development"
  short_description: "ALWAYS use this when the request matches Python Fastapi Development: Python FastAPI backend development with async patterns, SQLAlchemy, Pydantic, au..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $python-fastapi-development to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
