interface:
  display_name: "Vercel AI SDK Expert"
  short_description: "ALWAYS use this when the user mentions Vercel AI SDK Expert, asks to build, debug, review, document, automate, test, configure, migrate, or make deci..."
  brand_color: "#BE123C"
  default_prompt: "Use $vercel-ai-sdk-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
