interface:
  display_name: "Kaizen"
  short_description: "ALWAYS use this when the request matches Kaizen: Guide for continuous improvement, error proofing, and standardization."
  brand_color: "#0F766E"
  default_prompt: "Use $kaizen to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
