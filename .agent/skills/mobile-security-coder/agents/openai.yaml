interface:
  display_name: "Mobile Security Coder"
  short_description: "ALWAYS use this when the request matches Mobile Security Coder: Expert in secure mobile coding practices specializing in input validation, WebView se..."
  brand_color: "#EA580C"
  default_prompt: "Use $mobile-security-coder to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
