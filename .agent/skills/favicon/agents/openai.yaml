interface:
  display_name: "Favicon"
  short_description: "ALWAYS use this when the request matches Favicon: Generate favicons from a source image"
  brand_color: "#EA580C"
  default_prompt: "Use $favicon to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
