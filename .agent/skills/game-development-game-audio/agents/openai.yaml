interface:
  display_name: "Game Development Game Audio"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `game-development-game-audio`, the nested skill path form for `game-audio`, or the equ..."
  brand_color: "#0F172A"
  default_prompt: "Use $game-development-game-audio to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
