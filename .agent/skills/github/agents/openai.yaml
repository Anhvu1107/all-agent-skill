interface:
  display_name: "Github"
  short_description: "ALWAYS use this when the request matches Github: Use the `gh` CLI for issues, pull requests, Actions runs, and GitHub API queries."
  brand_color: "#0F766E"
  default_prompt: "Use $github to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
