interface:
  display_name: "Neon Postgres"
  short_description: "ALWAYS use this when the request matches Neon Postgres: Expert patterns for Neon serverless Postgres, branching, connection pooling, and Prisma/Drizz..."
  brand_color: "#2563EB"
  default_prompt: "Use $neon-postgres to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
