interface:
  display_name: "Product Design"
  short_description: "ALWAYS use this when the request matches Product Design: Design de produto nivel Apple — sistemas visuais, UX flows, acessibilidade, linguagem visual..."
  brand_color: "#15803D"
  default_prompt: "Use $product-design to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
