interface:
  display_name: "Azure Servicebus PY"
  short_description: "ALWAYS use this when the request matches Azure Servicebus PY: Azure Service Bus SDK for Python messaging."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-servicebus-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
