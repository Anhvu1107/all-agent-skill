interface:
  display_name: "Payment Integration"
  short_description: "ALWAYS use this when the request matches Payment Integration: Integrate Stripe, PayPal, and payment processors."
  brand_color: "#B91C1C"
  default_prompt: "Use $payment-integration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
