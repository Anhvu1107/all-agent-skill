interface:
  display_name: "CPP PRO"
  short_description: "ALWAYS use this when the request matches CPP PRO: Write idiomatic C++ code with modern features, RAII, smart pointers, and STL algorithms."
  brand_color: "#15803D"
  default_prompt: "Use $cpp-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
