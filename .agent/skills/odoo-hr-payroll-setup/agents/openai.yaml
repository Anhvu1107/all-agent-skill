interface:
  display_name: "Odoo HR Payroll Setup"
  short_description: "ALWAYS use this when the request matches Odoo HR Payroll Setup: Expert guide for Odoo HR and Payroll: salary structures, payslip rules, leave policie..."
  brand_color: "#EA580C"
  default_prompt: "Use $odoo-hr-payroll-setup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
