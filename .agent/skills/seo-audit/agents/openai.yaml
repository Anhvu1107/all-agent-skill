interface:
  display_name: "SEO Audit"
  short_description: "ALWAYS use this when the request matches SEO Audit: Diagnose and audit SEO issues affecting crawlability, indexation, rankings, and organic performan..."
  brand_color: "#7C3AED"
  default_prompt: "Use $seo-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
