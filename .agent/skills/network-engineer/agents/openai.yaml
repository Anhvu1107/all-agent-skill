interface:
  display_name: "Network Engineer"
  short_description: "ALWAYS use this when the request matches Network Engineer: Expert network engineer specializing in modern cloud networking, security architectures, a..."
  brand_color: "#0F766E"
  default_prompt: "Use $network-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
