interface:
  display_name: "Azure AI Document Intelligence Dotnet"
  short_description: "ALWAYS use this when the request matches Azure AI Document Intelligence Dotnet: Azure AI Document Intelligence SDK for .NET."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-ai-document-intelligence-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
