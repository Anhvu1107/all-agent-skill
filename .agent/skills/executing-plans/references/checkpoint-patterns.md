# Checkpoint Patterns

Good checkpoints happen:

- after each milestone-sized change
- before risky migrations
- before merge or handoff

Each checkpoint should have one proving action: test, build, runtime check, or diff review.
