interface:
  display_name: "Executing Plans"
  short_description: "ALWAYS use this when an approved plan, milestone list, or task sequence already exists and the job now is disciplined execution instead of strategy i..."
  brand_color: "#BE123C"
  default_prompt: "Use $executing-plans to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
