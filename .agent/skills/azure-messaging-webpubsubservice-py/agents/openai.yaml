interface:
  display_name: "Azure Messaging Webpubsubservice PY"
  short_description: "ALWAYS use this when the request matches Azure Messaging Webpubsubservice PY: Azure Web PubSub Service SDK for Python."
  brand_color: "#1D4ED8"
  default_prompt: "Use $azure-messaging-webpubsubservice-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
