interface:
  display_name: "Framework Migration Deps Upgrade"
  short_description: "ALWAYS use this when the request matches Framework Migration Deps Upgrade: You are a dependency management expert specializing in safe, incremental u..."
  brand_color: "#B91C1C"
  default_prompt: "Use $framework-migration-deps-upgrade to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
