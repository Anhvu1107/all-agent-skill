interface:
  display_name: "Threejs Animation"
  short_description: "ALWAYS use this when the request matches Threejs Animation: Three.js animation - keyframe animation, skeletal animation, morph targets, animation mix..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $threejs-animation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
