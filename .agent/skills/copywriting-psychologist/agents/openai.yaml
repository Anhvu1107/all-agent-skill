interface:
  display_name: "Copywriting Psychologist"
  short_description: "ALWAYS use this when the request matches Copywriting Psychologist: One sentence - what this skill does and when to invoke it"
  brand_color: "#0F172A"
  default_prompt: "Use $copywriting-psychologist to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
