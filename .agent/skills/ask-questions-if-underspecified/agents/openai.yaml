interface:
  display_name: "ASK Questions IF Underspecified"
  short_description: "ALWAYS use this when the request matches ASK Questions IF Underspecified: Clarify requirements before implementing."
  brand_color: "#0F172A"
  default_prompt: "Use $ask-questions-if-underspecified to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
