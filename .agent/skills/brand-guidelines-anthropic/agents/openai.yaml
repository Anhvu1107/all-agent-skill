interface:
  display_name: "Brand Guidelines Anthropic"
  short_description: "ALWAYS use this when the request matches Brand Guidelines Anthropic: To access Anthropic's official brand identity and style resources, use this skil..."
  brand_color: "#EA580C"
  default_prompt: "Use $brand-guidelines-anthropic to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
