interface:
  display_name: "PDF"
  short_description: "ALWAYS use this when the task involves reading, extracting, splitting, merging, rotating, creating, validating, OCR-ing, form-filling, or otherwise p..."
  brand_color: "#EA580C"
  default_prompt: "Use $pdf to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
