interface:
  display_name: "Azure Speech TO Text Rest PY"
  short_description: "ALWAYS use this when the request matches Azure Speech TO Text Rest PY: Azure Speech to Text REST API for short audio (Python)."
  brand_color: "#EA580C"
  default_prompt: "Use $azure-speech-to-text-rest-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
