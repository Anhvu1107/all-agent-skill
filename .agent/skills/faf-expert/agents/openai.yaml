interface:
  display_name: "FAF Expert"
  short_description: "ALWAYS use this when the request matches FAF Expert: Advanced .faf (Foundational AI-context Format) specialist."
  brand_color: "#1D4ED8"
  default_prompt: "Use $faf-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
