interface:
  display_name: "Azure Postgres TS"
  short_description: "ALWAYS use this when the request matches Azure Postgres TS: Connect to Azure Database for PostgreSQL Flexible Server from Node.js/TypeScript using th..."
  brand_color: "#7C3AED"
  default_prompt: "Use $azure-postgres-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
