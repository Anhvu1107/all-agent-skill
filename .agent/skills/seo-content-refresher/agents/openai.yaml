interface:
  display_name: "SEO Content Refresher"
  short_description: "ALWAYS use this when the request matches SEO Content Refresher: Identifies outdated elements in provided content and suggests updates to maintain fre..."
  brand_color: "#0F766E"
  default_prompt: "Use $seo-content-refresher to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
