interface:
  display_name: "Convertkit Automation"
  short_description: "ALWAYS use this when the request matches Convertkit Automation: Automate ConvertKit (Kit) tasks via Rube MCP (Composio): manage subscribers, tags, br..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $convertkit-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
