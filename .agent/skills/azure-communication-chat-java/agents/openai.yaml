interface:
  display_name: "Azure Communication Chat Java"
  short_description: "ALWAYS use this when the request matches Azure Communication Chat Java: Build real-time chat applications with thread management, messaging, particip..."
  brand_color: "#2563EB"
  default_prompt: "Use $azure-communication-chat-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
