interface:
  display_name: "Whatsapp Cloud API"
  short_description: "ALWAYS use this when the request matches Whatsapp Cloud API: Integracao com WhatsApp Business Cloud API (Meta)."
  brand_color: "#1D4ED8"
  default_prompt: "Use $whatsapp-cloud-api to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
