interface:
  display_name: "Wiki Researcher"
  short_description: "ALWAYS use this when the request matches Wiki Researcher: You are an expert software engineer and systems analyst."
  brand_color: "#B91C1C"
  default_prompt: "Use $wiki-researcher to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
