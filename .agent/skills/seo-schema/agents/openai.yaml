interface:
  display_name: "SEO Schema"
  short_description: "ALWAYS use this when the request matches SEO Schema: Detect, validate, and generate Schema.org structured data."
  brand_color: "#BE123C"
  default_prompt: "Use $seo-schema to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
