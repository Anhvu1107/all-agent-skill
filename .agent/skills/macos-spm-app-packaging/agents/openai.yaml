interface:
  display_name: "Macos SPM APP Packaging"
  short_description: "ALWAYS use this when the request matches Macos SPM APP Packaging: Scaffold, build, sign, and package SwiftPM macOS apps without Xcode projects."
  brand_color: "#BE123C"
  default_prompt: "Use $macos-spm-app-packaging to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
