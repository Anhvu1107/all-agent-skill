interface:
  display_name: "FAL Workflow"
  short_description: "ALWAYS use this when the request matches FAL Workflow: Generate workflow JSON files for chaining AI models"
  brand_color: "#EA580C"
  default_prompt: "Use $fal-workflow to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
