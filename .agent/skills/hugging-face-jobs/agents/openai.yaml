interface:
  display_name: "Hugging Face Jobs"
  short_description: "ALWAYS use this when the request matches Hugging Face Jobs: Run workloads on Hugging Face Jobs with managed CPUs, GPUs, TPUs, secrets, and Hub persis..."
  brand_color: "#BE123C"
  default_prompt: "Use $hugging-face-jobs to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
