interface:
  display_name: "Loki Mode"
  short_description: "ALWAYS use this when the request matches Loki Mode: Version 2.35.0 | PRD to Production | Zero Human Intervention > Research-enhanced: OpenAI SDK, Dee..."
  brand_color: "#BE123C"
  default_prompt: "Use $loki-mode to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
