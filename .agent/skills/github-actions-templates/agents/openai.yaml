interface:
  display_name: "Github Actions Templates"
  short_description: "ALWAYS use this when the request matches Github Actions Templates: Production-ready GitHub Actions workflow patterns for testing, building, and deplo..."
  brand_color: "#7C3AED"
  default_prompt: "Use $github-actions-templates to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
