interface:
  display_name: "C PRO"
  short_description: "ALWAYS use this when the request matches C PRO: Write efficient C code with proper memory management, pointer"
  brand_color: "#B91C1C"
  default_prompt: "Use $c-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
