interface:
  display_name: "Azure Keyvault Keys Rust"
  short_description: "ALWAYS use this when the user mentions Azure Keyvault Keys Rust, asks to build, debug, review, document, automate, test, configure, migrate, or make..."
  brand_color: "#374151"
  default_prompt: "Use $azure-keyvault-keys-rust to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
