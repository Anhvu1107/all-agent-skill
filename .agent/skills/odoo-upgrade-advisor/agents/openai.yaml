interface:
  display_name: "Odoo Upgrade Advisor"
  short_description: "ALWAYS use this when the request matches Odoo Upgrade Advisor: Step-by-step Odoo version upgrade advisor: pre-upgrade checklist, community vs enterpr..."
  brand_color: "#EA580C"
  default_prompt: "Use $odoo-upgrade-advisor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
