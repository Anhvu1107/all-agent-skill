interface:
  display_name: "Firebase"
  short_description: "ALWAYS use this when the request matches Firebase: Firebase gives you a complete backend in minutes - auth, database, storage, functions, hosting."
  brand_color: "#374151"
  default_prompt: "Use $firebase to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
