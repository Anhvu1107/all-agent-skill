interface:
  display_name: "Salesforce Automation"
  short_description: "ALWAYS use this when the request matches Salesforce Automation: Automate Salesforce tasks via Rube MCP (Composio): leads, contacts, accounts, opportu..."
  brand_color: "#0F766E"
  default_prompt: "Use $salesforce-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
