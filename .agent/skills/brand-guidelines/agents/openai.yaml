interface:
  display_name: "Brand Guidelines"
  short_description: "ALWAYS use this when the request matches Brand Guidelines: Applies a consistent brand palette and typography system to artifacts that need a polished..."
  brand_color: "#7C3AED"
  default_prompt: "Use $brand-guidelines to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
