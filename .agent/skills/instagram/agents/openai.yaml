interface:
  display_name: "Instagram"
  short_description: "ALWAYS use this when the request matches Instagram: Integracao completa com Instagram via Graph API."
  brand_color: "#B91C1C"
  default_prompt: "Use $instagram to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
