interface:
  display_name: "Draw"
  short_description: "ALWAYS use this when the request matches Draw: Vector graphics and diagram creation, format conversion (ODG/SVG/PDF) with LibreOffice Draw."
  brand_color: "#0F172A"
  default_prompt: "Use $draw to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
