interface:
  display_name: "Hugging Face Datasets"
  short_description: "ALWAYS use this when the request matches Hugging Face Datasets: Create and manage datasets on Hugging Face Hub."
  brand_color: "#374151"
  default_prompt: "Use $hugging-face-datasets to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
