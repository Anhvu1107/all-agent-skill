interface:
  display_name: "Azure Eventhub Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure Eventhub Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or make dec..."
  brand_color: "#0F172A"
  default_prompt: "Use $azure-eventhub-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
