interface:
  display_name: "Cloudformation Best Practices"
  short_description: "ALWAYS use this when the request matches Cloudformation Best Practices: CloudFormation template optimization, nested stacks, drift detection, and pro..."
  brand_color: "#2563EB"
  default_prompt: "Use $cloudformation-best-practices to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
