interface:
  display_name: "Github Workflow Automation"
  short_description: "ALWAYS use this when the request matches Github Workflow Automation: Patterns for automating GitHub workflows with AI assistance, inspired by [Gemini..."
  brand_color: "#7C3AED"
  default_prompt: "Use $github-workflow-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
