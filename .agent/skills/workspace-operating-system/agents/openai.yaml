interface:
  display_name: "Workspace Operating System"
  short_description: "ALWAYS use this for broad, ambiguous, multi-step, or repository-wide work in this bundle, especially when the right skill choice, execution order, or..."
  brand_color: "#0F766E"
  default_prompt: "Use $workspace-operating-system to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
