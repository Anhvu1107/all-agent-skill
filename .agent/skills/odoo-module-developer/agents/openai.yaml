interface:
  display_name: "Odoo Module Developer"
  short_description: "ALWAYS use this when the request matches Odoo Module Developer: Expert guide for creating custom Odoo modules."
  brand_color: "#EA580C"
  default_prompt: "Use $odoo-module-developer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
