interface:
  display_name: "Python Testing Patterns"
  short_description: "ALWAYS use this when the request matches Python Testing Patterns: Implement comprehensive testing strategies with pytest, fixtures, mocking, and test..."
  brand_color: "#EA580C"
  default_prompt: "Use $python-testing-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
