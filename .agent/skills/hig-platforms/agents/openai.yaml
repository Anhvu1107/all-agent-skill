interface:
  display_name: "HIG Platforms"
  short_description: "ALWAYS use this when the request matches HIG Platforms: Apple Human Interface Guidelines for platform-specific design."
  brand_color: "#B91C1C"
  default_prompt: "Use $hig-platforms to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
