interface:
  display_name: "Zendesk Automation"
  short_description: "ALWAYS use this when the request matches Zendesk Automation: Automate Zendesk tasks via Rube MCP (Composio): tickets, users, organizations, replies."
  brand_color: "#374151"
  default_prompt: "Use $zendesk-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
