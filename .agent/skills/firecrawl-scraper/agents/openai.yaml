interface:
  display_name: "Firecrawl Scraper"
  short_description: "ALWAYS use this when the request matches Firecrawl Scraper: Deep web scraping, screenshots, PDF parsing, and website crawling using Firecrawl API."
  brand_color: "#1D4ED8"
  default_prompt: "Use $firecrawl-scraper to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
