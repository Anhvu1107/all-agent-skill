interface:
  display_name: "Azure Search Documents TS"
  short_description: "ALWAYS use this when the request matches Azure Search Documents TS: Build search applications with vector, hybrid, and semantic search capabilities."
  brand_color: "#15803D"
  default_prompt: "Use $azure-search-documents-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
