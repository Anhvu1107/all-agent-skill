interface:
  display_name: "Python PRO"
  short_description: "ALWAYS use this when the request matches Python PRO: Master Python 3.12+ with modern features, async programming, performance optimization, and produ..."
  brand_color: "#7C3AED"
  default_prompt: "Use $python-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
