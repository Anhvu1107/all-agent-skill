interface:
  display_name: "Quant Analyst"
  short_description: "ALWAYS use this when the request matches Quant Analyst: Build financial models, backtest trading strategies, and analyze market data."
  brand_color: "#0F172A"
  default_prompt: "Use $quant-analyst to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
