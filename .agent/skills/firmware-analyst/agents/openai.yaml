interface:
  display_name: "Firmware Analyst"
  short_description: "ALWAYS use this when the request matches Firmware Analyst: Expert firmware analyst specializing in embedded systems, IoT security, and hardware rever..."
  brand_color: "#0F766E"
  default_prompt: "Use $firmware-analyst to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
