interface:
  display_name: "GIT Hooks Automation"
  short_description: "ALWAYS use this when the request matches GIT Hooks Automation: Master Git hooks setup with Husky, lint-staged, pre-commit framework, and commitlint."
  brand_color: "#EA580C"
  default_prompt: "Use $git-hooks-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
