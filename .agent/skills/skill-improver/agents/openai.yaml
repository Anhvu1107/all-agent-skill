interface:
  display_name: "Skill Improver"
  short_description: "ALWAYS use this when the request matches Skill Improver: Iteratively improve a Claude Code skill using the skill-reviewer agent until it meets qualit..."
  brand_color: "#374151"
  default_prompt: "Use $skill-improver to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
