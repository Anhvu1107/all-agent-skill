interface:
  display_name: "Leiloeiro Avaliacao"
  short_description: "ALWAYS use this when the request matches Leiloeiro Avaliacao: Avaliacao pericial de imoveis em leilao."
  brand_color: "#B91C1C"
  default_prompt: "Use $leiloeiro-avaliacao to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
