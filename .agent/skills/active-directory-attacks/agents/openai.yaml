interface:
  display_name: "Active Directory Attacks"
  short_description: "ALWAYS use this when the request matches Active Directory Attacks: Provide comprehensive techniques for attacking Microsoft Active Directory environm..."
  brand_color: "#7C3AED"
  default_prompt: "Use $active-directory-attacks to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
