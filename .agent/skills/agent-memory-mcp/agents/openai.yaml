interface:
  display_name: "Agent Memory MCP"
  short_description: "ALWAYS use this when the request matches Agent Memory MCP: A hybrid memory system that provides persistent, searchable knowledge management for AI ag..."
  brand_color: "#BE123C"
  default_prompt: "Use $agent-memory-mcp to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
