interface:
  display_name: "FP Refactor"
  short_description: "ALWAYS use this when the request matches FP Refactor: Comprehensive guide for refactoring imperative TypeScript code to fp-ts functional patterns"
  brand_color: "#7C3AED"
  default_prompt: "Use $fp-refactor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
