interface:
  display_name: "SLO Implementation"
  short_description: "ALWAYS use this when the request matches SLO Implementation: Framework for defining and implementing Service Level Indicators (SLIs), Service Level O..."
  brand_color: "#0F172A"
  default_prompt: "Use $slo-implementation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
