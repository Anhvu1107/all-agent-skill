interface:
  display_name: "Monorepo Architect"
  short_description: "ALWAYS use this when the request matches Monorepo Architect: Expert in monorepo architecture, build systems, and dependency management at scale."
  brand_color: "#BE123C"
  default_prompt: "Use $monorepo-architect to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
