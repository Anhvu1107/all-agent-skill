interface:
  display_name: "Apify Content Analytics"
  short_description: "ALWAYS use this when the request matches Apify Content Analytics: Track engagement metrics, measure campaign ROI, and analyze content performance acr..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $apify-content-analytics to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
