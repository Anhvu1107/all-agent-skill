interface:
  display_name: "Airflow DAG Patterns"
  short_description: "ALWAYS use this when the request matches Airflow DAG Patterns: Build production Apache Airflow DAGs with best practices for operators, sensors, testi..."
  brand_color: "#0F766E"
  default_prompt: "Use $airflow-dag-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
