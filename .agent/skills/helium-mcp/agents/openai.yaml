interface:
  display_name: "Helium MCP"
  short_description: "ALWAYS use this when the request matches Helium MCP: Connect to Helium's MCP server for news research, media bias analysis, balanced perspectives, st..."
  brand_color: "#B91C1C"
  default_prompt: "Use $helium-mcp to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
