interface:
  display_name: "Hugging Face Vision Trainer"
  short_description: "ALWAYS use this when the request matches Hugging Face Vision Trainer: Train or fine-tune vision models on Hugging Face Jobs for detection, classifica..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $hugging-face-vision-trainer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
