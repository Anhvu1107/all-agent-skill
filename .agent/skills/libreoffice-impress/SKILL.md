---
name: libreoffice-impress
description: ALWAYS use this when the user or an upstream archive refers to `libreoffice-impress`, the nested skill path form for `impress`, or the equivalent slash path. This is a namespace-preserving alias that routes to `impress` while retaining strict validation, Senior Master execution, and Codex review standards.
---

# Libreoffice Impress

## Selective Reading Rule

Start with:

- `references/senior-master-standard.md`
- `references/usage-routing.md`
- `references/quality-checklist.md`
- `references/alias-target.md`

Then load the canonical target skill only when the task needs the full implementation guidance.

## Alias Contract

- Treat `libreoffice-impress` as a compatibility and search alias for `impress`.
- Use canonical skill `impress` for the substantive workflow.
- Do not duplicate large upstream instructions here; this folder exists to preserve nested ZIP path slugs such as `libreoffice/impress` while keeping the runtime bundle deduplicated.
- Apply the same Senior Master standard and Codex strict review gate before final output.
