interface:
  display_name: "Returns Reverse Logistics"
  short_description: "ALWAYS use this when the request matches Returns Reverse Logistics: Codified expertise for returns authorisation, receipt and inspection, disposition..."
  brand_color: "#B91C1C"
  default_prompt: "Use $returns-reverse-logistics to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
