interface:
  display_name: "Azure Eventgrid Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure Eventgrid Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or make de..."
  brand_color: "#7C3AED"
  default_prompt: "Use $azure-eventgrid-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
