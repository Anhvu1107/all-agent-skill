interface:
  display_name: "Google Docs Automation"
  short_description: "ALWAYS use this when the request matches Google Docs Automation: Lightweight Google Docs integration with standalone OAuth authentication."
  brand_color: "#0F766E"
  default_prompt: "Use $google-docs-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
