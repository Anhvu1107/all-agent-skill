interface:
  display_name: "Nextjs React Expert"
  short_description: "ALWAYS use this when the work is specifically about modern Next.js or React architecture, data flow, rendering strategy, routing, or performance-sens..."
  brand_color: "#0F766E"
  default_prompt: "Use $nextjs-react-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
