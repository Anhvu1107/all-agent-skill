interface:
  display_name: "Posthog Automation"
  short_description: "ALWAYS use this when the request matches Posthog Automation: Automate PostHog tasks via Rube MCP (Composio): events, feature flags, projects, user pr..."
  brand_color: "#BE123C"
  default_prompt: "Use $posthog-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
