interface:
  display_name: "Apify Market Research"
  short_description: "ALWAYS use this when the request matches Apify Market Research: Analyze market conditions, geographic opportunities, pricing, consumer behavior, and..."
  brand_color: "#7C3AED"
  default_prompt: "Use $apify-market-research to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
