interface:
  display_name: "HIG Components Content"
  short_description: "ALWAYS use this when the request matches HIG Components Content: Apple Human Interface Guidelines for content display components."
  brand_color: "#2563EB"
  default_prompt: "Use $hig-components-content to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
