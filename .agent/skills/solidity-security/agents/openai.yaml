interface:
  display_name: "Solidity Security"
  short_description: "ALWAYS use this when the request matches Solidity Security: Master smart contract security best practices, vulnerability prevention, and secure Solid..."
  brand_color: "#0F172A"
  default_prompt: "Use $solidity-security to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
