interface:
  display_name: "Game Development Multiplayer"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `game-development-multiplayer`, the nested skill path form for `multiplayer`, or the e..."
  brand_color: "#EA580C"
  default_prompt: "Use $game-development-multiplayer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
