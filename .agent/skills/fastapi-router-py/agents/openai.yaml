interface:
  display_name: "Fastapi Router PY"
  short_description: "ALWAYS use this when the request matches Fastapi Router PY: Create FastAPI routers following established patterns with proper authentication, respons..."
  brand_color: "#15803D"
  default_prompt: "Use $fastapi-router-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
