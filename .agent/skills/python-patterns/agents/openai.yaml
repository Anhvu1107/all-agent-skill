interface:
  display_name: "Python Patterns"
  short_description: "ALWAYS use this when the request matches Python Patterns: Python development principles and decision-making."
  brand_color: "#374151"
  default_prompt: "Use $python-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
