interface:
  display_name: "Design MD"
  short_description: "ALWAYS use this when the request matches Design MD: Analyze Stitch projects and synthesize a semantic design system into DESIGN.md files"
  brand_color: "#B91C1C"
  default_prompt: "Use $design-md to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
