interface:
  display_name: "Finishing A Development Branch"
  short_description: "ALWAYS use this when branch work looks complete and the next question is whether to push, open a PR, merge, keep, or safely clean up."
  brand_color: "#7C3AED"
  default_prompt: "Use $finishing-a-development-branch to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
