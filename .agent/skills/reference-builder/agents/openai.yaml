interface:
  display_name: "Reference Builder"
  short_description: "ALWAYS use this when the request matches Reference Builder: Creates exhaustive technical references and API documentation."
  brand_color: "#0F766E"
  default_prompt: "Use $reference-builder to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
