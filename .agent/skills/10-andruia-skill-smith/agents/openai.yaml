interface:
  display_name: "10 Andruia Skill Smith"
  short_description: "ALWAYS use this when the user mentions 10 Andruia Skill Smith, asks to build, debug, review, document, automate, test, configure, migrate, or make de..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $10-andruia-skill-smith to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
