interface:
  display_name: "Production Scheduling"
  short_description: "ALWAYS use this when the request matches Production Scheduling: Codified expertise for production scheduling, job sequencing, line balancing, changeo..."
  brand_color: "#B91C1C"
  default_prompt: "Use $production-scheduling to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
