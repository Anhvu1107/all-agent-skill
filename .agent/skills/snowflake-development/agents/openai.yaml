interface:
  display_name: "Snowflake Development"
  short_description: "ALWAYS use this when the request matches Snowflake Development: Comprehensive Snowflake development assistant covering SQL best practices, data pipel..."
  brand_color: "#0F172A"
  default_prompt: "Use $snowflake-development to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
