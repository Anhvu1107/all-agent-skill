interface:
  display_name: "Magic Animator"
  short_description: "ALWAYS use this when the request matches Magic Animator: AI-powered animation tool for creating motion in logos, UI, icons, and social media assets."
  brand_color: "#2563EB"
  default_prompt: "Use $magic-animator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
