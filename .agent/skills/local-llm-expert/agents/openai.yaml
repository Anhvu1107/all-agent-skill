interface:
  display_name: "Local LLM Expert"
  short_description: "ALWAYS use this when the request matches Local LLM Expert: Master local LLM inference, model selection, VRAM optimization, and local deployment using..."
  brand_color: "#7C3AED"
  default_prompt: "Use $local-llm-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
