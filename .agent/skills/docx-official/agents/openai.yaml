interface:
  display_name: "Docx Official"
  short_description: "ALWAYS use this when the user needs an official-style DOCX workflow for `.docx` creation, OOXML editing, redlining, document inspection, or validatio..."
  brand_color: "#7C3AED"
  default_prompt: "Use $docx-official to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
