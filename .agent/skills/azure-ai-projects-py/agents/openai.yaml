interface:
  display_name: "Azure AI Projects PY"
  short_description: "ALWAYS use this when the request matches Azure AI Projects PY: Build AI applications on Microsoft Foundry using the azure-ai-projects SDK."
  brand_color: "#374151"
  default_prompt: "Use $azure-ai-projects-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
