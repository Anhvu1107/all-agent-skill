interface:
  display_name: "Azure Identity Java"
  short_description: "ALWAYS use this when the request matches Azure Identity Java: Authenticate Java applications with Azure services using Microsoft Entra ID (Azure AD)."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-identity-java to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
