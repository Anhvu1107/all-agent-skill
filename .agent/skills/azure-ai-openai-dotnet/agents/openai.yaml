interface:
  display_name: "Azure AI Openai Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure AI Openai Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or make de..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $azure-ai-openai-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
