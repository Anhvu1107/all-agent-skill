interface:
  display_name: "Ffuf WEB Fuzzing"
  short_description: "ALWAYS use this when the request matches Ffuf WEB Fuzzing: Expert guidance for ffuf web fuzzing during penetration testing, including authenticated f..."
  brand_color: "#EA580C"
  default_prompt: "Use $ffuf-web-fuzzing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
