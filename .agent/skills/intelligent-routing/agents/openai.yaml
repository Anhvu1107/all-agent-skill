interface:
  display_name: "Intelligent Routing"
  short_description: "ALWAYS use this when the request matches Intelligent Routing: Automatic agent selection and intelligent task routing."
  brand_color: "#EA580C"
  default_prompt: "Use $intelligent-routing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
