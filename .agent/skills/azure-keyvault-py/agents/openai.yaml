interface:
  display_name: "Azure Keyvault PY"
  short_description: "ALWAYS use this when the user mentions Azure Keyvault PY, asks to build, debug, review, document, automate, test, configure, migrate, or make decisio..."
  brand_color: "#0F766E"
  default_prompt: "Use $azure-keyvault-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
