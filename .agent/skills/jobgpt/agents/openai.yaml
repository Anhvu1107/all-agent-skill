interface:
  display_name: "Jobgpt"
  short_description: "ALWAYS use this when the request matches Jobgpt: Job search automation, auto apply, resume generation, application tracking, salary intelligence, and..."
  brand_color: "#0F172A"
  default_prompt: "Use $jobgpt to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
