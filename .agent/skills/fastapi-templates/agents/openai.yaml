interface:
  display_name: "Fastapi Templates"
  short_description: "ALWAYS use this when the request matches Fastapi Templates: Create production-ready FastAPI projects with async patterns, dependency injection, and c..."
  brand_color: "#374151"
  default_prompt: "Use $fastapi-templates to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
