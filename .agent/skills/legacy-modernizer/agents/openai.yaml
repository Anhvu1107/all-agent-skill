interface:
  display_name: "Legacy Modernizer"
  short_description: "ALWAYS use this when the request matches Legacy Modernizer: Refactor legacy codebases, migrate outdated frameworks, and implement gradual modernizati..."
  brand_color: "#2563EB"
  default_prompt: "Use $legacy-modernizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
