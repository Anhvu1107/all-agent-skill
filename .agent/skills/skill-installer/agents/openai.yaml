interface:
  display_name: "Skill Installer"
  short_description: "ALWAYS use this when the request matches Skill Installer: Instala, valida, registra e verifica novas skills no ecossistema."
  brand_color: "#BE123C"
  default_prompt: "Use $skill-installer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
