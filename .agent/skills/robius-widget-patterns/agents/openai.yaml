interface:
  display_name: "Robius Widget Patterns"
  short_description: "ALWAYS use this when the request matches Robius Widget Patterns: CRITICAL: Use for Robius widget patterns."
  brand_color: "#7C3AED"
  default_prompt: "Use $robius-widget-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
