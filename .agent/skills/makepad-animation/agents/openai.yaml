interface:
  display_name: "Makepad Animation"
  short_description: "ALWAYS use this when the request matches Makepad Animation: CRITICAL: Use for Makepad animation system."
  brand_color: "#EA580C"
  default_prompt: "Use $makepad-animation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
