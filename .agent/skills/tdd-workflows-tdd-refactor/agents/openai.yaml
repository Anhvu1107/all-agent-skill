interface:
  display_name: "TDD Workflows TDD Refactor"
  short_description: "ALWAYS use this when the request matches TDD Workflows TDD Refactor: Use when working with tdd workflows tdd refactor"
  brand_color: "#0F766E"
  default_prompt: "Use $tdd-workflows-tdd-refactor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
