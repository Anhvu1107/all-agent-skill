interface:
  display_name: "Android UI Verification"
  short_description: "ALWAYS use this when the request matches Android UI Verification: Automated end-to-end UI testing and verification on an Android Emulator using ADB."
  brand_color: "#0F766E"
  default_prompt: "Use $android-ui-verification to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
