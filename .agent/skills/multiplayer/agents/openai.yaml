interface:
  display_name: "Multiplayer"
  short_description: "ALWAYS use this when the request matches Multiplayer: Multiplayer game development principles."
  brand_color: "#EA580C"
  default_prompt: "Use $multiplayer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
