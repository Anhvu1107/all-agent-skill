interface:
  display_name: "Odoo RPC API"
  short_description: "ALWAYS use this when the request matches Odoo RPC API: Expert on Odoo's external JSON-RPC and XML-RPC APIs."
  brand_color: "#0F766E"
  default_prompt: "Use $odoo-rpc-api to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
