interface:
  display_name: "Database Migrations SQL Migrations"
  short_description: "ALWAYS use this when the request matches Database Migrations SQL Migrations: SQL database migrations with zero-downtime strategies for PostgreSQL, My..."
  brand_color: "#0F766E"
  default_prompt: "Use $database-migrations-sql-migrations to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
