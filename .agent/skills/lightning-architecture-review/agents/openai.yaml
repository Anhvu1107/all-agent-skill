interface:
  display_name: "Lightning Architecture Review"
  short_description: "ALWAYS use this when the request matches Lightning Architecture Review: Review Bitcoin Lightning Network protocol designs, compare channel factory ap..."
  brand_color: "#B91C1C"
  default_prompt: "Use $lightning-architecture-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
