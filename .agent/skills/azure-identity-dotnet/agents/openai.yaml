interface:
  display_name: "Azure Identity Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure Identity Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or make dec..."
  brand_color: "#B91C1C"
  default_prompt: "Use $azure-identity-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
