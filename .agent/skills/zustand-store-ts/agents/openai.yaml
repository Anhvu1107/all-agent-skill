interface:
  display_name: "Zustand Store TS"
  short_description: "ALWAYS use this when the request matches Zustand Store TS: Create Zustand stores following established patterns with proper TypeScript types and midd..."
  brand_color: "#0F766E"
  default_prompt: "Use $zustand-store-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
