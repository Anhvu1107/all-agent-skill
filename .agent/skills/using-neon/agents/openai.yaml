interface:
  display_name: "Using Neon"
  short_description: "ALWAYS use this when the request matches Using Neon: Neon is a serverless Postgres platform that separates compute and storage to offer autoscaling..."
  brand_color: "#15803D"
  default_prompt: "Use $using-neon to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
