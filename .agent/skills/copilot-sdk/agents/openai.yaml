interface:
  display_name: "Copilot SDK"
  short_description: "ALWAYS use this when the request matches Copilot SDK: Build applications that programmatically interact with GitHub Copilot."
  brand_color: "#BE123C"
  default_prompt: "Use $copilot-sdk to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
