interface:
  display_name: "Obsidian CLI"
  short_description: "ALWAYS use this when the request matches Obsidian CLI: Use the Obsidian CLI to read, create, search, and manage vault content, or to develop and debu..."
  brand_color: "#0F766E"
  default_prompt: "Use $obsidian-cli to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
