interface:
  display_name: "File Uploads"
  short_description: "ALWAYS use this when the request matches File Uploads: Expert at handling file uploads and cloud storage."
  brand_color: "#EA580C"
  default_prompt: "Use $file-uploads to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
