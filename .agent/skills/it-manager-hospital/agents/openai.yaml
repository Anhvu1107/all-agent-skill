interface:
  display_name: "IT Manager Hospital"
  short_description: "ALWAYS use this when the request matches IT Manager Hospital: World-class Hospital IT Management Advisor specializing in clinical safety, digital mat..."
  brand_color: "#EA580C"
  default_prompt: "Use $it-manager-hospital to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
