interface:
  display_name: "Parallel Agents"
  short_description: "ALWAYS use this when the task contains multiple independent research questions, code slices, or verification tracks that can proceed in parallel whil..."
  brand_color: "#374151"
  default_prompt: "Use $parallel-agents to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
