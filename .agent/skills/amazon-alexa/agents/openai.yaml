interface:
  display_name: "Amazon Alexa"
  short_description: "ALWAYS use this when the request matches Amazon Alexa: Integracao completa com Amazon Alexa para criar skills de voz inteligentes, transformar Alexa..."
  brand_color: "#0F766E"
  default_prompt: "Use $amazon-alexa to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
