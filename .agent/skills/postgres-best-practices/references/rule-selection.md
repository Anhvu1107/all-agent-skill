# Rule Selection

Use this before reading the full Supabase Postgres rule set.

## Route By Problem

- slow queries: start with query and monitoring rules
- schema changes or indexes: start with schema rules
- connection or scaling issues: start with connection rules
- security or RLS concerns: start with security rules
