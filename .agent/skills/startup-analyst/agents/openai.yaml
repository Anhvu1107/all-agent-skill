interface:
  display_name: "Startup Analyst"
  short_description: "ALWAYS use this when the request matches Startup Analyst: Expert startup business analyst specializing in market sizing, financial modeling, competit..."
  brand_color: "#374151"
  default_prompt: "Use $startup-analyst to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
