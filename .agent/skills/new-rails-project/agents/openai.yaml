interface:
  display_name: "NEW Rails Project"
  short_description: "ALWAYS use this when the user mentions NEW Rails Project, asks to build, debug, review, document, automate, test, configure, migrate, or make decisio..."
  brand_color: "#BE123C"
  default_prompt: "Use $new-rails-project to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
