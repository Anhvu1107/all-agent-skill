interface:
  display_name: "Security Requirement Extraction"
  short_description: "ALWAYS use this when the request matches Security Requirement Extraction: Derive security requirements from threat models and business context."
  brand_color: "#15803D"
  default_prompt: "Use $security-requirement-extraction to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
