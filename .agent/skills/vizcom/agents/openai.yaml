interface:
  display_name: "Vizcom"
  short_description: "ALWAYS use this when the request matches Vizcom: AI-powered product design tool for transforming sketches into full-fidelity 3D renders."
  brand_color: "#BE123C"
  default_prompt: "Use $vizcom to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
