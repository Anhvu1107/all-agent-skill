interface:
  display_name: "XSS Html Injection"
  short_description: "ALWAYS use this when the request matches XSS Html Injection: Execute comprehensive client-side injection vulnerability assessments on web application..."
  brand_color: "#EA580C"
  default_prompt: "Use $xss-html-injection to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
