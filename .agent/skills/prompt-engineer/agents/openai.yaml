interface:
  display_name: "Prompt Engineer"
  short_description: "ALWAYS use this when the request matches Prompt Engineer: Transforms user prompts into optimized prompts using frameworks (RTF, RISEN, Chain of Thoug..."
  brand_color: "#0F172A"
  default_prompt: "Use $prompt-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
