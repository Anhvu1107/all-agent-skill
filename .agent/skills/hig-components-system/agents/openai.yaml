interface:
  display_name: "HIG Components System"
  short_description: "ALWAYS use this when the request matches HIG Components System: Apple HIG guidance for system experience components: widgets, live activities, notifi..."
  brand_color: "#B91C1C"
  default_prompt: "Use $hig-components-system to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
