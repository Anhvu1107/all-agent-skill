interface:
  display_name: "Conductor Setup"
  short_description: "ALWAYS use this when the request matches Conductor Setup: Configure a Rails project to work with Conductor (parallel coding agents)"
  brand_color: "#0F766E"
  default_prompt: "Use $conductor-setup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
