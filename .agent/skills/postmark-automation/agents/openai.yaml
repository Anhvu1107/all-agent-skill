interface:
  display_name: "Postmark Automation"
  short_description: "ALWAYS use this when the request matches Postmark Automation: Automate Postmark email delivery tasks via Rube MCP (Composio): send templated emails..."
  brand_color: "#0F172A"
  default_prompt: "Use $postmark-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
