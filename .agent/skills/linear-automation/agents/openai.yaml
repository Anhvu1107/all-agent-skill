interface:
  display_name: "Linear Automation"
  short_description: "ALWAYS use this when the request matches Linear Automation: Automate Linear tasks via Rube MCP (Composio): issues, projects, cycles, teams, labels."
  brand_color: "#0F766E"
  default_prompt: "Use $linear-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
