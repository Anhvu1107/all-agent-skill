interface:
  display_name: "Nextjs Supabase Auth"
  short_description: "ALWAYS use this when the request matches Nextjs Supabase Auth: Expert integration of Supabase Auth with Next.js App Router"
  brand_color: "#15803D"
  default_prompt: "Use $nextjs-supabase-auth to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
