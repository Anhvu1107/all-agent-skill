interface:
  display_name: "Brand Guidelines Community"
  short_description: "ALWAYS use this when the request matches Brand Guidelines Community: To access Anthropic's official brand identity and style resources, use this skil..."
  brand_color: "#BE123C"
  default_prompt: "Use $brand-guidelines-community to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
