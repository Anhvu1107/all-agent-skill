interface:
  display_name: "Environment Setup Guide"
  short_description: "ALWAYS use this when the request matches Environment Setup Guide: Guide developers through setting up development environments with proper tools, dep..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $environment-setup-guide to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
