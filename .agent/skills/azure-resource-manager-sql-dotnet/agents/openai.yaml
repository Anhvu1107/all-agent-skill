interface:
  display_name: "Azure Resource Manager SQL Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Resource Manager SQL Dotnet: Azure Resource Manager SDK for Azure SQL in .NET."
  brand_color: "#BE123C"
  default_prompt: "Use $azure-resource-manager-sql-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
