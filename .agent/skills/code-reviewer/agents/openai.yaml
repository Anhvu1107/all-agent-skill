interface:
  display_name: "Code Reviewer"
  short_description: "ALWAYS use this when the request matches Code Reviewer: Elite code review expert specializing in modern AI-powered code"
  brand_color: "#B91C1C"
  default_prompt: "Use $code-reviewer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
