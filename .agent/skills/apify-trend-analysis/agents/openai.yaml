interface:
  display_name: "Apify Trend Analysis"
  short_description: "ALWAYS use this when the request matches Apify Trend Analysis: Discover and track emerging trends across Google Trends, Instagram, Facebook, YouTube..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $apify-trend-analysis to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
