interface:
  display_name: "Expo Cicd Workflows"
  short_description: "ALWAYS use this when the request matches Expo Cicd Workflows: Helps understand and write EAS workflow YAML files for Expo projects."
  brand_color: "#EA580C"
  default_prompt: "Use $expo-cicd-workflows to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
