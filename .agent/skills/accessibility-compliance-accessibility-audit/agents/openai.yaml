interface:
  display_name: "Accessibility Compliance Accessibility Audit"
  short_description: "ALWAYS use this when the request matches Accessibility Compliance Accessibility Audit: You are an accessibility expert specializing in WCAG complianc..."
  brand_color: "#EA580C"
  default_prompt: "Use $accessibility-compliance-accessibility-audit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
