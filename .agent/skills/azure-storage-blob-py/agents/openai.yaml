interface:
  display_name: "Azure Storage Blob PY"
  short_description: "ALWAYS use this when the user mentions Azure Storage Blob PY, asks to build, debug, review, document, automate, test, configure, migrate, or make dec..."
  brand_color: "#15803D"
  default_prompt: "Use $azure-storage-blob-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
