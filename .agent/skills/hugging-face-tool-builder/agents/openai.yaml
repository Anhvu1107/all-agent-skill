interface:
  display_name: "Hugging Face Tool Builder"
  short_description: "ALWAYS use this when the request matches Hugging Face Tool Builder: Your purpose is now is to create reusable command line scripts and utilities for..."
  brand_color: "#15803D"
  default_prompt: "Use $hugging-face-tool-builder to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
