interface:
  display_name: "Issues"
  short_description: "ALWAYS use this when the request matches Issues: Interact with GitHub issues - create, list, and view issues."
  brand_color: "#0F766E"
  default_prompt: "Use $issues to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
