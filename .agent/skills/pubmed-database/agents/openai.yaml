interface:
  display_name: "Pubmed Database"
  short_description: "ALWAYS use this when the user mentions Pubmed Database, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions..."
  brand_color: "#7C3AED"
  default_prompt: "Use $pubmed-database to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
