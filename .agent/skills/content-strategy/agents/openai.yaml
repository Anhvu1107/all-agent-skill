interface:
  display_name: "Content Strategy"
  short_description: "ALWAYS use this when the request matches Content Strategy: Plan a content strategy, topic clusters, editorial roadmap, and content mix for traffic, a..."
  brand_color: "#BE123C"
  default_prompt: "Use $content-strategy to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
