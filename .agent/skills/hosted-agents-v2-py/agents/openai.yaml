interface:
  display_name: "Hosted Agents V2 PY"
  short_description: "ALWAYS use this when the request matches Hosted Agents V2 PY: Build hosted agents using Azure AI Projects SDK with ImageBasedHostedAgentDefinition."
  brand_color: "#EA580C"
  default_prompt: "Use $hosted-agents-v2-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
