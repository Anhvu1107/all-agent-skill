interface:
  display_name: "Build"
  short_description: "ALWAYS use this when the user mentions Build, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in this d..."
  brand_color: "#BE123C"
  default_prompt: "Use $build to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
