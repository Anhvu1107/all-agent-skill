interface:
  display_name: "Game Development Mobile Games"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `game-development-mobile-games`, the nested skill path form for `mobile-games`, or the..."
  brand_color: "#2563EB"
  default_prompt: "Use $game-development-mobile-games to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
