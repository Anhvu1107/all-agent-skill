interface:
  display_name: "Convex"
  short_description: "ALWAYS use this when the request matches Convex: Convex reactive backend expert: schema design, TypeScript functions, real-time subscriptions, auth..."
  brand_color: "#BE123C"
  default_prompt: "Use $convex to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
