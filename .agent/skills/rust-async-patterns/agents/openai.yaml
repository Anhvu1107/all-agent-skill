interface:
  display_name: "Rust Async Patterns"
  short_description: "ALWAYS use this when the request matches Rust Async Patterns: Master Rust async programming with Tokio, async traits, error handling, and concurrent..."
  brand_color: "#2563EB"
  default_prompt: "Use $rust-async-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
