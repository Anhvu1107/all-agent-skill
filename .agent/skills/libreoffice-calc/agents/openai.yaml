interface:
  display_name: "Libreoffice Calc"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `libreoffice-calc`, the nested skill path form for `calc`, or the equivalent slash pat..."
  brand_color: "#BE123C"
  default_prompt: "Use $libreoffice-calc to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
