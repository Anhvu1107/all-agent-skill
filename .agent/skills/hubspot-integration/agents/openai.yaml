interface:
  display_name: "Hubspot Integration"
  short_description: "ALWAYS use this when the request matches Hubspot Integration: Expert patterns for HubSpot CRM integration including OAuth authentication, CRM objects..."
  brand_color: "#B91C1C"
  default_prompt: "Use $hubspot-integration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
