interface:
  display_name: "Github Issue Creator"
  short_description: "ALWAYS use this when the request matches Github Issue Creator: Turn error logs, screenshots, voice notes, and rough bug reports into crisp, developer..."
  brand_color: "#BE123C"
  default_prompt: "Use $github-issue-creator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
