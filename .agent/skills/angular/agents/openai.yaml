interface:
  display_name: "Angular"
  short_description: "ALWAYS use this when the request matches Angular: Modern Angular (v20+) expert with deep knowledge of Signals, Standalone Components, Zoneless applic..."
  brand_color: "#7C3AED"
  default_prompt: "Use $angular to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
