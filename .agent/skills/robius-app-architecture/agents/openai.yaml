interface:
  display_name: "Robius APP Architecture"
  short_description: "ALWAYS use this when the request matches Robius APP Architecture: CRITICAL: Use for Robius app architecture patterns."
  brand_color: "#EA580C"
  default_prompt: "Use $robius-app-architecture to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
