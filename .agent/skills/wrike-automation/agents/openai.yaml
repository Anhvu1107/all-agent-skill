interface:
  display_name: "Wrike Automation"
  short_description: "ALWAYS use this when the request matches Wrike Automation: Automate Wrike project management via Rube MCP (Composio): create tasks/folders, manage pr..."
  brand_color: "#15803D"
  default_prompt: "Use $wrike-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
