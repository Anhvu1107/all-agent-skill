interface:
  display_name: "Carrier Relationship Management"
  short_description: "ALWAYS use this when the request matches Carrier Relationship Management: Codified expertise for managing carrier portfolios, negotiating freight rat..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $carrier-relationship-management to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
