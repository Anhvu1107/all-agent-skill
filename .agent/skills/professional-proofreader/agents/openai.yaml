interface:
  display_name: "Professional Proofreader"
  short_description: "ALWAYS use this when the request matches Professional Proofreader: Use when a user asks to \"proofread\", \"review and correct\", \"fix grammar\", \"improve..."
  brand_color: "#0F172A"
  default_prompt: "Use $professional-proofreader to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
