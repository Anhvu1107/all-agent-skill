interface:
  display_name: "Internal Comms Anthropic"
  short_description: "ALWAYS use this when the request matches Internal Comms Anthropic: To write internal communications, use this skill for:"
  brand_color: "#EA580C"
  default_prompt: "Use $internal-comms-anthropic to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
