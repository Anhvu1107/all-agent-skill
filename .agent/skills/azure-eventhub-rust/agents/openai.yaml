interface:
  display_name: "Azure Eventhub Rust"
  short_description: "ALWAYS use this when the user mentions Azure Eventhub Rust, asks to build, debug, review, document, automate, test, configure, migrate, or make decis..."
  brand_color: "#374151"
  default_prompt: "Use $azure-eventhub-rust to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
