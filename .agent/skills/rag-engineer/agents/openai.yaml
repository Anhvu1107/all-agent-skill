interface:
  display_name: "RAG Engineer"
  short_description: "ALWAYS use this when the request matches RAG Engineer: Expert in building Retrieval-Augmented Generation systems."
  brand_color: "#7C3AED"
  default_prompt: "Use $rag-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
