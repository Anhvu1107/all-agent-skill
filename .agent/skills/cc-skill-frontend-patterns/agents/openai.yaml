interface:
  display_name: "CC Skill Frontend Patterns"
  short_description: "ALWAYS use this when the request matches CC Skill Frontend Patterns: Frontend development patterns for React, Next.js, state management, performance..."
  brand_color: "#7C3AED"
  default_prompt: "Use $cc-skill-frontend-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
