interface:
  display_name: "Conductor Revert"
  short_description: "ALWAYS use this when the request matches Conductor Revert: Git-aware undo by logical work unit (track, phase, or task)"
  brand_color: "#EA580C"
  default_prompt: "Use $conductor-revert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
