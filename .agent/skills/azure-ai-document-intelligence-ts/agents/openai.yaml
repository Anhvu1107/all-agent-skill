interface:
  display_name: "Azure AI Document Intelligence TS"
  short_description: "ALWAYS use this when the request matches Azure AI Document Intelligence TS: Extract text, tables, and structured data from documents using prebuilt a..."
  brand_color: "#15803D"
  default_prompt: "Use $azure-ai-document-intelligence-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
