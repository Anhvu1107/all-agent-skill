interface:
  display_name: "Deployment Pipeline Design"
  short_description: "ALWAYS use this when the request matches Deployment Pipeline Design: Architecture patterns for multi-stage CI/CD pipelines with approval gates and de..."
  brand_color: "#EA580C"
  default_prompt: "Use $deployment-pipeline-design to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
