interface:
  display_name: "Polars"
  short_description: "ALWAYS use this when the request matches Polars: Fast in-memory DataFrame library for datasets that fit in RAM."
  brand_color: "#374151"
  default_prompt: "Use $polars to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
