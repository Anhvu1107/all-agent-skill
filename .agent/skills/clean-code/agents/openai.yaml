interface:
  display_name: "Clean Code"
  short_description: "ALWAYS use this when the request matches Clean Code: Pragmatic coding standards - concise, direct, no over-engineering, no unnecessary comments"
  brand_color: "#0F766E"
  default_prompt: "Use $clean-code to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
