interface:
  display_name: "Memory Safety Patterns"
  short_description: "ALWAYS use this when the request matches Memory Safety Patterns: Cross-language patterns for memory-safe programming including RAII, ownership, smart..."
  brand_color: "#BE123C"
  default_prompt: "Use $memory-safety-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
