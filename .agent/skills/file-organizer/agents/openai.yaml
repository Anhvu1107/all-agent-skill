interface:
  display_name: "File Organizer"
  short_description: "ALWAYS use this when the user mentions File Organizer, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions..."
  brand_color: "#374151"
  default_prompt: "Use $file-organizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
