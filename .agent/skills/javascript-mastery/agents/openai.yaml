interface:
  display_name: "Javascript Mastery"
  short_description: "ALWAYS use this when the request matches Javascript Mastery: 33+ essential JavaScript concepts every developer should know, inspired by [33-js-concep..."
  brand_color: "#0F766E"
  default_prompt: "Use $javascript-mastery to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
