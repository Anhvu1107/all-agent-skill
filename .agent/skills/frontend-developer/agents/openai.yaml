interface:
  display_name: "Frontend Developer"
  short_description: "ALWAYS use this when the request matches Frontend Developer: Build React components, implement responsive layouts, and handle client-side state manag..."
  brand_color: "#B91C1C"
  default_prompt: "Use $frontend-developer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
