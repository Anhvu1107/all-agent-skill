interface:
  display_name: "Sendgrid Automation"
  short_description: "ALWAYS use this when the request matches Sendgrid Automation: Automate SendGrid email delivery workflows including marketing campaigns (Single Sends)..."
  brand_color: "#0F766E"
  default_prompt: "Use $sendgrid-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
