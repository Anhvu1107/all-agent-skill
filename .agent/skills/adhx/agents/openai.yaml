interface:
  display_name: "Adhx"
  short_description: "ALWAYS use this when the request matches Adhx: Fetch any X/Twitter post as clean LLM-friendly JSON."
  brand_color: "#B91C1C"
  default_prompt: "Use $adhx to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
