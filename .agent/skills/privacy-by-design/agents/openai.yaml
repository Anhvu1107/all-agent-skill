interface:
  display_name: "Privacy BY Design"
  short_description: "ALWAYS use this when the request matches Privacy BY Design: Use when building apps that collect user data."
  brand_color: "#1D4ED8"
  default_prompt: "Use $privacy-by-design to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
