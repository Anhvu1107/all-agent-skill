interface:
  display_name: "ON Call Handoff Patterns"
  short_description: "ALWAYS use this when the request matches ON Call Handoff Patterns: Effective patterns for on-call shift transitions, ensuring continuity, context tra..."
  brand_color: "#7C3AED"
  default_prompt: "Use $on-call-handoff-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
