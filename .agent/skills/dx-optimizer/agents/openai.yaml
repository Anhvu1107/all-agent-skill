interface:
  display_name: "DX Optimizer"
  short_description: "ALWAYS use this when the user mentions DX Optimizer, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in..."
  brand_color: "#15803D"
  default_prompt: "Use $dx-optimizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
