interface:
  display_name: "AWT E2e Testing"
  short_description: "ALWAYS use this when the request matches AWT E2e Testing: AI-powered E2E web testing — eyes and hands for AI coding tools."
  brand_color: "#374151"
  default_prompt: "Use $awt-e2e-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
