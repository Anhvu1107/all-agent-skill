interface:
  display_name: "Html Injection Testing"
  short_description: "ALWAYS use this when the request matches Html Injection Testing: Identify and exploit HTML injection vulnerabilities that allow attackers to inject m..."
  brand_color: "#7C3AED"
  default_prompt: "Use $html-injection-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
