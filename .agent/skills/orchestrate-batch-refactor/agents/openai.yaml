interface:
  display_name: "Orchestrate Batch Refactor"
  short_description: "ALWAYS use this when the request matches Orchestrate Batch Refactor: Plan and execute large refactors with dependency-aware work packets and parallel..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $orchestrate-batch-refactor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
