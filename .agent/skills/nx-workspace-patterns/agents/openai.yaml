interface:
  display_name: "NX Workspace Patterns"
  short_description: "ALWAYS use this when the request matches NX Workspace Patterns: Configure and optimize Nx monorepo workspaces."
  brand_color: "#7C3AED"
  default_prompt: "Use $nx-workspace-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
