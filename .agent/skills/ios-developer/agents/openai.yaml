interface:
  display_name: "IOS Developer"
  short_description: "ALWAYS use this when the request matches IOS Developer: Develop native iOS applications with Swift/SwiftUI."
  brand_color: "#1D4ED8"
  default_prompt: "Use $ios-developer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
