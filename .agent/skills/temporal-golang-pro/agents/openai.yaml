interface:
  display_name: "Temporal Golang PRO"
  short_description: "ALWAYS use this when the request matches Temporal Golang PRO: Use when building durable distributed systems with Temporal Go SDK."
  brand_color: "#EA580C"
  default_prompt: "Use $temporal-golang-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
