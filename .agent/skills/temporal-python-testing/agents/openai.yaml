interface:
  display_name: "Temporal Python Testing"
  short_description: "ALWAYS use this when the request matches Temporal Python Testing: Comprehensive testing approaches for Temporal workflows using pytest, progressive d..."
  brand_color: "#0F766E"
  default_prompt: "Use $temporal-python-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
