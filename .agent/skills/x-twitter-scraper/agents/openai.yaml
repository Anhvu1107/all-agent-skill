interface:
  display_name: "X Twitter Scraper"
  short_description: "ALWAYS use this when the request matches X Twitter Scraper: X (Twitter) data platform skill — tweet search, user lookup, follower extraction, engagem..."
  brand_color: "#7C3AED"
  default_prompt: "Use $x-twitter-scraper to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
