interface:
  display_name: "MMX CLI"
  short_description: "ALWAYS use this when the request matches MMX CLI: Use mmx to generate text, images, video, speech, and music via the MiniMax AI platform."
  brand_color: "#1D4ED8"
  default_prompt: "Use $mmx-cli to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
