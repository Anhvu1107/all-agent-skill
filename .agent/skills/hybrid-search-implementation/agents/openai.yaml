interface:
  display_name: "Hybrid Search Implementation"
  short_description: "ALWAYS use this when the request matches Hybrid Search Implementation: Combine vector and keyword search for improved retrieval."
  brand_color: "#7C3AED"
  default_prompt: "Use $hybrid-search-implementation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
