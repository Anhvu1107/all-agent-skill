interface:
  display_name: "Haskell PRO"
  short_description: "ALWAYS use this when the request matches Haskell PRO: Expert Haskell engineer specializing in advanced type systems, pure"
  brand_color: "#0F172A"
  default_prompt: "Use $haskell-pro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
