interface:
  display_name: "Shodan Reconnaissance"
  short_description: "ALWAYS use this when the request matches Shodan Reconnaissance: Provide systematic methodologies for leveraging Shodan as a reconnaissance tool durin..."
  brand_color: "#7C3AED"
  default_prompt: "Use $shodan-reconnaissance to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
