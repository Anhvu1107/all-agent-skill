---
name: libreoffice-draw
description: ALWAYS use this when the user or an upstream archive refers to `libreoffice-draw`, the nested skill path form for `draw`, or the equivalent slash path. This is a namespace-preserving alias that routes to `draw` while retaining strict validation, Senior Master execution, and Codex review standards.
---

# Libreoffice Draw

## Selective Reading Rule

Start with:

- `references/senior-master-standard.md`
- `references/usage-routing.md`
- `references/quality-checklist.md`
- `references/alias-target.md`

Then load the canonical target skill only when the task needs the full implementation guidance.

## Alias Contract

- Treat `libreoffice-draw` as a compatibility and search alias for `draw`.
- Use canonical skill `draw` for the substantive workflow.
- Do not duplicate large upstream instructions here; this folder exists to preserve nested ZIP path slugs such as `libreoffice/draw` while keeping the runtime bundle deduplicated.
- Apply the same Senior Master standard and Codex strict review gate before final output.
