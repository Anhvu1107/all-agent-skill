interface:
  display_name: "FP Pragmatic"
  short_description: "ALWAYS use this when the request matches FP Pragmatic: A practical, jargon-free guide to functional programming - the 80/20 approach that gets result..."
  brand_color: "#B91C1C"
  default_prompt: "Use $fp-pragmatic to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
