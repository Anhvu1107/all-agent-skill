interface:
  display_name: "Conductor Status"
  short_description: "ALWAYS use this when the request matches Conductor Status: Display project status, active tracks, and next actions"
  brand_color: "#374151"
  default_prompt: "Use $conductor-status to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
