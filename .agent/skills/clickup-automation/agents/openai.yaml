interface:
  display_name: "Clickup Automation"
  short_description: "ALWAYS use this when the request matches Clickup Automation: Automate ClickUp project management including tasks, spaces, folders, lists, comments, a..."
  brand_color: "#BE123C"
  default_prompt: "Use $clickup-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
