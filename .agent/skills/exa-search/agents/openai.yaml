interface:
  display_name: "EXA Search"
  short_description: "ALWAYS use this when the request matches EXA Search: Semantic search, similar content discovery, and structured research using Exa API."
  brand_color: "#7C3AED"
  default_prompt: "Use $exa-search to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
