interface:
  display_name: "Auth Implementation Patterns"
  short_description: "ALWAYS use this when the request matches Auth Implementation Patterns: Build secure, scalable authentication and authorization systems using industry..."
  brand_color: "#15803D"
  default_prompt: "Use $auth-implementation-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
