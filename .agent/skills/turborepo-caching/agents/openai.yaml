interface:
  display_name: "Turborepo Caching"
  short_description: "ALWAYS use this when the request matches Turborepo Caching: Configure Turborepo for efficient monorepo builds with local and remote caching."
  brand_color: "#EA580C"
  default_prompt: "Use $turborepo-caching to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
