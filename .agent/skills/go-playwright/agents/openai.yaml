interface:
  display_name: "GO Playwright"
  short_description: "ALWAYS use this when the request matches GO Playwright: Expert capability for robust, stealthy, and efficient browser automation using Playwright Go."
  brand_color: "#EA580C"
  default_prompt: "Use $go-playwright to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
