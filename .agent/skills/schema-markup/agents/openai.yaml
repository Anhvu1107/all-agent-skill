interface:
  display_name: "Schema Markup"
  short_description: "ALWAYS use this when the request matches Schema Markup: Design, validate, and optimize schema.org structured data for eligibility, correctness, and m..."
  brand_color: "#EA580C"
  default_prompt: "Use $schema-markup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
