interface:
  display_name: "Telegram"
  short_description: "ALWAYS use this when the request matches Telegram: Integracao completa com Telegram Bot API."
  brand_color: "#7C3AED"
  default_prompt: "Use $telegram to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
