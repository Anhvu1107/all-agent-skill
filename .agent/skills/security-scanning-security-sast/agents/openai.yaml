interface:
  display_name: "Security Scanning Security Sast"
  short_description: "ALWAYS use this when the request matches Security Scanning Security Sast: Static Application Security Testing (SAST) for code vulnerability analysis..."
  brand_color: "#15803D"
  default_prompt: "Use $security-scanning-security-sast to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
