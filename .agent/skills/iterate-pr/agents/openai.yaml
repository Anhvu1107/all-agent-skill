interface:
  display_name: "Iterate PR"
  short_description: "ALWAYS use this when the user mentions Iterate PR, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in t..."
  brand_color: "#BE123C"
  default_prompt: "Use $iterate-pr to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
