interface:
  display_name: "Documentation"
  short_description: "ALWAYS use this when the request matches Documentation: Documentation generation workflow covering API docs, architecture docs, README files, code co..."
  brand_color: "#0F766E"
  default_prompt: "Use $documentation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
