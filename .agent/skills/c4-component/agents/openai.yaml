interface:
  display_name: "C4 Component"
  short_description: "ALWAYS use this when the request matches C4 Component: Expert C4 Component-level documentation specialist."
  brand_color: "#0F766E"
  default_prompt: "Use $c4-component to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
