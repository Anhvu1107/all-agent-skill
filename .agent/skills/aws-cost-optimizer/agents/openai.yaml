interface:
  display_name: "AWS Cost Optimizer"
  short_description: "ALWAYS use this when the request matches AWS Cost Optimizer: Comprehensive AWS cost analysis and optimization recommendations using AWS CLI and Cost..."
  brand_color: "#B91C1C"
  default_prompt: "Use $aws-cost-optimizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
