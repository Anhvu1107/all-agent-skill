interface:
  display_name: "Makepad Layout"
  short_description: "ALWAYS use this when the request matches Makepad Layout: CRITICAL: Use for Makepad layout system."
  brand_color: "#1D4ED8"
  default_prompt: "Use $makepad-layout to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
