interface:
  display_name: "Expo Tailwind Setup"
  short_description: "ALWAYS use this when the request matches Expo Tailwind Setup: Set up Tailwind CSS v4 in Expo with react-native-css and NativeWind v5 for universal st..."
  brand_color: "#EA580C"
  default_prompt: "Use $expo-tailwind-setup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
