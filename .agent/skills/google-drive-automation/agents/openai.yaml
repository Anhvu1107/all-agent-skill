interface:
  display_name: "Google Drive Automation"
  short_description: "ALWAYS use this when the request matches Google Drive Automation: Lightweight Google Drive integration with standalone OAuth authentication."
  brand_color: "#2563EB"
  default_prompt: "Use $google-drive-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
