interface:
  display_name: "Azure AI ML PY"
  short_description: "ALWAYS use this when the request matches Azure AI ML PY: Azure Machine Learning SDK v2 for Python."
  brand_color: "#15803D"
  default_prompt: "Use $azure-ai-ml-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
