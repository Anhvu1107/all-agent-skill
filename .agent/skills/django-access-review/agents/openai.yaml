interface:
  display_name: "Django Access Review"
  short_description: "ALWAYS use this when the user mentions Django Access Review, asks to build, debug, review, document, automate, test, configure, migrate, or make deci..."
  brand_color: "#B91C1C"
  default_prompt: "Use $django-access-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
