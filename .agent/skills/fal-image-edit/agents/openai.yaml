interface:
  display_name: "FAL Image Edit"
  short_description: "ALWAYS use this when the request matches FAL Image Edit: AI-powered image editing with style transfer and object removal"
  brand_color: "#EA580C"
  default_prompt: "Use $fal-image-edit to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
