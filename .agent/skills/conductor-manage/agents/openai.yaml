interface:
  display_name: "Conductor Manage"
  short_description: "ALWAYS use this when the request matches Conductor Manage: Manage track lifecycle: archive, restore, delete, rename, and cleanup"
  brand_color: "#2563EB"
  default_prompt: "Use $conductor-manage to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
