interface:
  display_name: "ARM Cortex Expert"
  short_description: "ALWAYS use this when the request matches ARM Cortex Expert: Senior embedded software engineer specializing in firmware and driver development for ARM..."
  brand_color: "#BE123C"
  default_prompt: "Use $arm-cortex-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
