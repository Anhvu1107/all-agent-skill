interface:
  display_name: "Metasploit Framework"
  short_description: "ALWAYS use this when the request matches Metasploit Framework: ⚠️ AUTHORIZED USE ONLY > This skill is for educational purposes or authorized security..."
  brand_color: "#B91C1C"
  default_prompt: "Use $metasploit-framework to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
