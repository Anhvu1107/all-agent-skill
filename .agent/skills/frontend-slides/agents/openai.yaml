interface:
  display_name: "Frontend Slides"
  short_description: "ALWAYS use this when the request matches Frontend Slides: Create stunning, animation-rich HTML presentations from scratch or by converting PowerPoint..."
  brand_color: "#EA580C"
  default_prompt: "Use $frontend-slides to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
