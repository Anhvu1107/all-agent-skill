interface:
  display_name: "Azure AI Voicelive Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure AI Voicelive Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or make..."
  brand_color: "#EA580C"
  default_prompt: "Use $azure-ai-voicelive-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
