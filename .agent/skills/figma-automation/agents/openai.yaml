interface:
  display_name: "Figma Automation"
  short_description: "ALWAYS use this when the request matches Figma Automation: Automate Figma tasks via Rube MCP (Composio): files, components, design tokens, comments..."
  brand_color: "#0F766E"
  default_prompt: "Use $figma-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
