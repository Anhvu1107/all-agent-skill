interface:
  display_name: "Upgrading Expo"
  short_description: "ALWAYS use this when the user mentions Upgrading Expo, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions..."
  brand_color: "#B91C1C"
  default_prompt: "Use $upgrading-expo to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
