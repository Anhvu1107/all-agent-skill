interface:
  display_name: "Error Handling Patterns"
  short_description: "ALWAYS use this when the request matches Error Handling Patterns: Build resilient applications with robust error handling strategies that gracefully..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $error-handling-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
