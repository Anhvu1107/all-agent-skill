interface:
  display_name: "Create Issue Gate"
  short_description: "ALWAYS use this when the request matches Create Issue Gate: Use when starting a new implementation task and an issue must be created with strict acce..."
  brand_color: "#7C3AED"
  default_prompt: "Use $create-issue-gate to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
