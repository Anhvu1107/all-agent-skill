interface:
  display_name: "Kubernetes Deployment"
  short_description: "ALWAYS use this when the request matches Kubernetes Deployment: Kubernetes deployment workflow for container orchestration, Helm charts, service mesh..."
  brand_color: "#15803D"
  default_prompt: "Use $kubernetes-deployment to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
