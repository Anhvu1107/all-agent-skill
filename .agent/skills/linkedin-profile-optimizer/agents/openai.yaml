interface:
  display_name: "Linkedin Profile Optimizer"
  short_description: "ALWAYS use this when the request matches Linkedin Profile Optimizer: High-intent expert for LinkedIn profile checks, authority building, and SEO opti..."
  brand_color: "#B91C1C"
  default_prompt: "Use $linkedin-profile-optimizer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
