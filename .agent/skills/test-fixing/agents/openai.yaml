interface:
  display_name: "Test Fixing"
  short_description: "ALWAYS use this when the request matches Test Fixing: Systematically identify and fix all failing tests using smart grouping strategies."
  brand_color: "#15803D"
  default_prompt: "Use $test-fixing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
