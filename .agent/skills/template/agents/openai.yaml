interface:
  display_name: "Template"
  short_description: "ALWAYS use this when the user or an upstream archive refers to `template`, the nested skill path form for `template-skill`, or the equivalent slash p..."
  brand_color: "#0F766E"
  default_prompt: "Use $template to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
