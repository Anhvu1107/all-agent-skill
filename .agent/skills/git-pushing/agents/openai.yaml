interface:
  display_name: "GIT Pushing"
  short_description: "ALWAYS use this when the request matches GIT Pushing: Stage all changes, create a conventional commit, and push to the remote branch."
  brand_color: "#0F766E"
  default_prompt: "Use $git-pushing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
