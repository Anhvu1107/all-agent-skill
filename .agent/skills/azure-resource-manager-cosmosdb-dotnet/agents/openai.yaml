interface:
  display_name: "Azure Resource Manager Cosmosdb Dotnet"
  short_description: "ALWAYS use this when the request matches Azure Resource Manager Cosmosdb Dotnet: Azure Resource Manager SDK for Cosmos DB in .NET."
  brand_color: "#374151"
  default_prompt: "Use $azure-resource-manager-cosmosdb-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
