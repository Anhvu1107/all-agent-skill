interface:
  display_name: "Skill Sentinel"
  short_description: "ALWAYS use this when the request matches Skill Sentinel: Auditoria e evolucao do ecossistema de skills."
  brand_color: "#BE123C"
  default_prompt: "Use $skill-sentinel to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
