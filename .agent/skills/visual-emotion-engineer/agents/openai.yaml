interface:
  display_name: "Visual Emotion Engineer"
  short_description: "ALWAYS use this when the request matches Visual Emotion Engineer: One sentence - what this skill does and when to invoke it"
  brand_color: "#BE123C"
  default_prompt: "Use $visual-emotion-engineer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
