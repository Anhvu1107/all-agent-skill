interface:
  display_name: "Windows Privilege Escalation"
  short_description: "ALWAYS use this when the request matches Windows Privilege Escalation: Provide systematic methodologies for discovering and exploiting privilege esca..."
  brand_color: "#2563EB"
  default_prompt: "Use $windows-privilege-escalation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
