interface:
  display_name: "Steve Jobs"
  short_description: "ALWAYS use this when the request matches Steve Jobs: Agente que simula Steve Jobs — cofundador da Apple, CEO da Pixar, fundador da NeXT, o maior desi..."
  brand_color: "#B91C1C"
  default_prompt: "Use $steve-jobs to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
