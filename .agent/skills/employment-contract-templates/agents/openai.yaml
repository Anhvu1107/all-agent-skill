interface:
  display_name: "Employment Contract Templates"
  short_description: "ALWAYS use this when the request matches Employment Contract Templates: Templates and patterns for creating legally sound employment documentation in..."
  brand_color: "#15803D"
  default_prompt: "Use $employment-contract-templates to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
