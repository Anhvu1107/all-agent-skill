interface:
  display_name: "React Patterns"
  short_description: "ALWAYS use this when the request matches React Patterns: Modern React patterns and principles."
  brand_color: "#0F766E"
  default_prompt: "Use $react-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
