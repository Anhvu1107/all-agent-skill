interface:
  display_name: "Evaluation"
  short_description: "ALWAYS use this when the request matches Evaluation: Build evaluation frameworks for agent systems."
  brand_color: "#2563EB"
  default_prompt: "Use $evaluation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
