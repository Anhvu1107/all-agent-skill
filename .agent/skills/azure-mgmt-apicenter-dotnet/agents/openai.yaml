interface:
  display_name: "Azure Mgmt Apicenter Dotnet"
  short_description: "ALWAYS use this when the user mentions Azure Mgmt Apicenter Dotnet, asks to build, debug, review, document, automate, test, configure, migrate, or ma..."
  brand_color: "#374151"
  default_prompt: "Use $azure-mgmt-apicenter-dotnet to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
