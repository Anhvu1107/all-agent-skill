---
name: skills-x402-express-wrapper
description: ALWAYS use this when the user or an upstream archive refers to `skills-x402-express-wrapper`, the nested skill path form for `x402-express-wrapper`, or the equivalent slash path. This is a namespace-preserving alias that routes to `x402-express-wrapper` while retaining strict validation, Senior Master execution, and Codex review standards.
---

# Skills X402 Express Wrapper

## Selective Reading Rule

Start with:

- `references/senior-master-standard.md`
- `references/usage-routing.md`
- `references/quality-checklist.md`
- `references/alias-target.md`

Then load the canonical target skill only when the task needs the full implementation guidance.

## Alias Contract

- Treat `skills-x402-express-wrapper` as a compatibility and search alias for `x402-express-wrapper`.
- Use canonical skill `x402-express-wrapper` for the substantive workflow.
- Do not duplicate large upstream instructions here; this folder exists to preserve nested ZIP path slugs such as `skills/x402/express/wrapper` while keeping the runtime bundle deduplicated.
- Apply the same Senior Master standard and Codex strict review gate before final output.
