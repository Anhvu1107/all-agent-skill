interface:
  display_name: "Azure AI Textanalytics PY"
  short_description: "ALWAYS use this when the request matches Azure AI Textanalytics PY: Azure AI Text Analytics SDK for sentiment analysis, entity recognition, key phras..."
  brand_color: "#15803D"
  default_prompt: "Use $azure-ai-textanalytics-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
