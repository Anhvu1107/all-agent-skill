interface:
  display_name: "Multi Advisor"
  short_description: "ALWAYS use this when the request matches Multi Advisor: Conselho de especialistas — consulta multiplos agentes do ecossistema em paralelo para analis..."
  brand_color: "#374151"
  default_prompt: "Use $multi-advisor to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
