interface:
  display_name: "Zapier Make Patterns"
  short_description: "ALWAYS use this when the request matches Zapier Make Patterns: No-code automation democratizes workflow building."
  brand_color: "#0F172A"
  default_prompt: "Use $zapier-make-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
