interface:
  display_name: "Lightning Factory Explainer"
  short_description: "ALWAYS use this when the request matches Lightning Factory Explainer: Explain Bitcoin Lightning channel factories and the SuperScalar protocol — scal..."
  brand_color: "#BE123C"
  default_prompt: "Use $lightning-factory-explainer to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
