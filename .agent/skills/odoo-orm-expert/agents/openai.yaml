interface:
  display_name: "Odoo ORM Expert"
  short_description: "ALWAYS use this when the request matches Odoo ORM Expert: Master Odoo ORM patterns: search, browse, create, write, domain filters, computed fields, a..."
  brand_color: "#BE123C"
  default_prompt: "Use $odoo-orm-expert to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
