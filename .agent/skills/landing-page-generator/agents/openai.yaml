interface:
  display_name: "Landing Page Generator"
  short_description: "ALWAYS use this when the request matches Landing Page Generator: Generates high-converting Next.js/React landing pages with Tailwind CSS."
  brand_color: "#EA580C"
  default_prompt: "Use $landing-page-generator to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
