interface:
  display_name: "Deployment Validation Config Validate"
  short_description: "ALWAYS use this when the request matches Deployment Validation Config Validate: You are a configuration management expert specializing in validating..."
  brand_color: "#EA580C"
  default_prompt: "Use $deployment-validation-config-validate to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
