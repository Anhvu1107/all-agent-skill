interface:
  display_name: "Skill Check"
  short_description: "ALWAYS use this when the request matches Skill Check: Validate Claude Code skills against the agentskills specification."
  brand_color: "#EA580C"
  default_prompt: "Use $skill-check to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
