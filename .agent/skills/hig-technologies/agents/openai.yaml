interface:
  display_name: "HIG Technologies"
  short_description: "ALWAYS use this when the request matches HIG Technologies: Check for .claude/apple-design-context.md before asking questions."
  brand_color: "#15803D"
  default_prompt: "Use $hig-technologies to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
