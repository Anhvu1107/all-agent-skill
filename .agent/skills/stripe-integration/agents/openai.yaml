interface:
  display_name: "Stripe Integration"
  short_description: "ALWAYS use this when the request matches Stripe Integration: Master Stripe payment processing integration for robust, PCI-compliant payment flows inc..."
  brand_color: "#7C3AED"
  default_prompt: "Use $stripe-integration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
