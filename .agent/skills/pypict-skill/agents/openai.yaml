interface:
  display_name: "Pypict Skill"
  short_description: "ALWAYS use this when the user mentions Pypict Skill, asks to build, debug, review, document, automate, test, configure, migrate, or make decisions in..."
  brand_color: "#0F766E"
  default_prompt: "Use $pypict-skill to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
