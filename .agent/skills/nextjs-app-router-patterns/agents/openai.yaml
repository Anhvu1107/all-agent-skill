interface:
  display_name: "Nextjs APP Router Patterns"
  short_description: "ALWAYS use this when the request matches Nextjs APP Router Patterns: Comprehensive patterns for Next.js 14+ App Router architecture, Server Component..."
  brand_color: "#2563EB"
  default_prompt: "Use $nextjs-app-router-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
