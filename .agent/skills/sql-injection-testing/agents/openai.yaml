interface:
  display_name: "SQL Injection Testing"
  short_description: "ALWAYS use this when the request matches SQL Injection Testing: Execute comprehensive SQL injection vulnerability assessments on web applications to..."
  brand_color: "#B91C1C"
  default_prompt: "Use $sql-injection-testing to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
