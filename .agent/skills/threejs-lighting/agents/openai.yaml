interface:
  display_name: "Threejs Lighting"
  short_description: "ALWAYS use this when the request matches Threejs Lighting: Three.js lighting - light types, shadows, environment lighting."
  brand_color: "#B91C1C"
  default_prompt: "Use $threejs-lighting to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
