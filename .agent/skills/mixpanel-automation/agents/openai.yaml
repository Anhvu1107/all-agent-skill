interface:
  display_name: "Mixpanel Automation"
  short_description: "ALWAYS use this when the request matches Mixpanel Automation: Automate Mixpanel tasks via Rube MCP (Composio): events, segmentation, funnels, cohorts..."
  brand_color: "#B91C1C"
  default_prompt: "Use $mixpanel-automation to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
