interface:
  display_name: "GHA Security Review"
  short_description: "ALWAYS use this when the request matches GHA Security Review: Find exploitable vulnerabilities in GitHub Actions workflows."
  brand_color: "#1D4ED8"
  default_prompt: "Use $gha-security-review to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
