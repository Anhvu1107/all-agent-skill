# Worktree Layouts

Preferred patterns:

- repository-adjacent `../repo-worktrees/<branch>`
- project-local ignored `.worktrees/<branch>`

Before creation:

- confirm the directory is ignored
- choose a descriptive branch name
- verify the base branch is the right one for the task
