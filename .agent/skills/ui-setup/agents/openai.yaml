interface:
  display_name: "UI Setup"
  short_description: "ALWAYS use this when the request matches UI Setup: Interactive StyleSeed setup wizard for choosing app type, brand color, visual style, typography, a..."
  brand_color: "#B91C1C"
  default_prompt: "Use $ui-setup to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
