interface:
  display_name: "React UI Patterns"
  short_description: "ALWAYS use this when the request matches React UI Patterns: Modern React UI patterns for loading states, error handling, and data fetching."
  brand_color: "#0F172A"
  default_prompt: "Use $react-ui-patterns to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
