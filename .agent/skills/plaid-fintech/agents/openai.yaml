interface:
  display_name: "Plaid Fintech"
  short_description: "ALWAYS use this when the request matches Plaid Fintech: Expert patterns for Plaid API integration including Link token flows, transactions sync, iden..."
  brand_color: "#1D4ED8"
  default_prompt: "Use $plaid-fintech to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
