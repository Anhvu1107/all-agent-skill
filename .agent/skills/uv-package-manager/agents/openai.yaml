interface:
  display_name: "UV Package Manager"
  short_description: "ALWAYS use this when the request matches UV Package Manager: Comprehensive guide to using uv, an extremely fast Python package installer and resolver..."
  brand_color: "#2563EB"
  default_prompt: "Use $uv-package-manager to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
