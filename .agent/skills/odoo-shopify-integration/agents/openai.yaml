interface:
  display_name: "Odoo Shopify Integration"
  short_description: "ALWAYS use this when the request matches Odoo Shopify Integration: Connect Odoo with Shopify: sync products, inventory, orders, and customers using t..."
  brand_color: "#0F766E"
  default_prompt: "Use $odoo-shopify-integration to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
