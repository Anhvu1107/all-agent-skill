interface:
  display_name: "Azure Mgmt Apicenter PY"
  short_description: "ALWAYS use this when the request matches Azure Mgmt Apicenter PY: Azure API Center Management SDK for Python."
  brand_color: "#0F766E"
  default_prompt: "Use $azure-mgmt-apicenter-py to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
