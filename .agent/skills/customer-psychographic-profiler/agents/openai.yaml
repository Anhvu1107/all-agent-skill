interface:
  display_name: "Customer Psychographic Profiler"
  short_description: "ALWAYS use this when the request matches Customer Psychographic Profiler: One sentence - what this skill does and when to invoke it"
  brand_color: "#B91C1C"
  default_prompt: "Use $customer-psychographic-profiler to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
