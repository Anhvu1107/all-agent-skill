interface:
  display_name: "Clarvia AEO Check"
  short_description: "ALWAYS use this when the request matches Clarvia AEO Check: Score any MCP server, API, or CLI for agent-readiness using Clarvia AEO (Agent Experience..."
  brand_color: "#EA580C"
  default_prompt: "Use $clarvia-aeo-check to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
