interface:
  display_name: "Onboarding CRO"
  short_description: "ALWAYS use this when the request matches Onboarding CRO: You are an expert in user onboarding and activation."
  brand_color: "#0F172A"
  default_prompt: "Use $onboarding-cro to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
