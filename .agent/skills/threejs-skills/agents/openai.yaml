interface:
  display_name: "Threejs Skills"
  short_description: "ALWAYS use this when the request matches Threejs Skills: Create 3D scenes, interactive experiences, and visual effects using Three.js."
  brand_color: "#2563EB"
  default_prompt: "Use $threejs-skills to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
