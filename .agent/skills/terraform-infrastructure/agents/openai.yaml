interface:
  display_name: "Terraform Infrastructure"
  short_description: "ALWAYS use this when the request matches Terraform Infrastructure: Terraform infrastructure as code workflow for provisioning cloud resources, creati..."
  brand_color: "#B91C1C"
  default_prompt: "Use $terraform-infrastructure to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
