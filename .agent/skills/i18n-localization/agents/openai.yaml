interface:
  display_name: "I18n Localization"
  short_description: "ALWAYS use this when the request matches I18n Localization: Internationalization and localization patterns."
  brand_color: "#0F766E"
  default_prompt: "Use $i18n-localization to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
