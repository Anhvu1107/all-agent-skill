interface:
  display_name: "Architecture Decision Records"
  short_description: "ALWAYS use this when the request matches Architecture Decision Records: Comprehensive patterns for creating, maintaining, and managing Architecture D..."
  brand_color: "#0F766E"
  default_prompt: "Use $architecture-decision-records to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
