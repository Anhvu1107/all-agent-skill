interface:
  display_name: "Biopython"
  short_description: "ALWAYS use this when the request matches Biopython: Biopython is a comprehensive set of freely available Python tools for biological computation."
  brand_color: "#BE123C"
  default_prompt: "Use $biopython to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
