interface:
  display_name: "Azure Storage Blob TS"
  short_description: "ALWAYS use this when the request matches Azure Storage Blob TS: Azure Blob Storage JavaScript/TypeScript SDK (@azure/storage-blob) for blob operation..."
  brand_color: "#7C3AED"
  default_prompt: "Use $azure-storage-blob-ts to handle this task with the relevant workflow, references, scripts, and quality checks."

policy:
  allow_implicit_invocation: true
